# Skill Card: 开放世界增量分类
# OpenWorld Class Incremental Learning

**论文来源**: OpenCML: Open-world Continual Learning for Multimodal and Multilingual Data (ACL 2025)
**理论基础**: Prototype-based Open World Learning + 增量类别发现 + 记忆回放防遗忘
**适用领域**: 标签体系动态扩展后的分类器更新、新反馈类型自动识别、开放世界文本分类

---

## ① 算法原理

### 核心思想

传统文本分类器训练时假设**所有类别在训练时已知**（封闭世界假设）。但 VOC 场景中，新痛点、新品类、新场景不断涌现，已有标签体系会不断扩展。

OpenCML 的核心洞察是：**分类器应该像标签体系一样持续进化**。当 AutoTag 发现新标签后，分类器不需要从头重训练，而是增量学习新类别，同时不遗忘旧类别的知识。

### 技术架构

```
┌─────────────────────────────────────────────────────────────┐
│                    阶段1: 基础训练（封闭世界）                  │
│                                                             │
│   已知类别样本 → 计算类别原型（centroid = 类内均值）           │
│                                                             │
│   类别A原型 ◆────────┐                                      │
│   类别B原型 ◆────────┼── 决策边界：最近原型 + 距离阈值         │
│   类别C原型 ◆────────┘                                      │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                    阶段2: 新类别检测（开放世界）                │
│                                                             │
│   新样本 → 计算到所有已知原型的距离                            │
│        → 最小距离 > 阈值 × 平均类内距离                       │
│        → 标记为 "unknown"（新类别候选）                       │
│                                                             │
│   已知样本    已知样本    未知样本                             │
│      ●          ●           ?                                │
│         ◆ A       ◆ B        （远离所有已知原型）              │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                    阶段3: 增量学习（发现新类别）                │
│                                                             │
│   积累足够多 "unknown" 样本 → 聚类分析                        │
│   每个聚类中心 = 新类别原型                                    │
│   扩展原型集合，分类器能力自动增强                             │
│                                                             │
│   记忆回放：用历史样本微调所有原型，防止灾难性遗忘              │
└─────────────────────────────────────────────────────────────┘
```

### Prototype-based 分类

| 步骤 | 操作 | 公式 |
|------|------|------|
| 训练 | 计算每个类别的原型向量 | `μ_c = mean(x_i for x_i in class c)` |
| 预测 | 找最近原型 | `ŷ = argmin_c ||x - μ_c||` |
| 新类检测 | 距离阈值判断 | `is_novel = min_dist > τ × avg_intra_dist` |
| 增量学习 | 聚类未知样本 | `new_μ = mean(unknown_cluster)` |
| 防遗忘 | 回放更新 | `μ_c ← (n_c × μ_c + n_r × μ_r) / (n_c + n_r)` |

### 关键参数

| 参数 | 作用 | 推荐值 |
|------|------|--------|
| `novelty_threshold` (τ) | 新类别检测严格程度 | 1.2-1.8（越大越严格） |
| `min_samples_for_discovery` | 发现新类别所需最少样本 | 10-30 |
| `replay_buffer_size` | 记忆回放缓冲区 | 200-500 |

### 与 InsightNet 的核心差异

| 维度 | InsightNet | OpenCML |
|------|-----------|---------|
| **世界假设** | 封闭世界（标签预定义） | 开放世界（新标签动态发现） |
| **类别数量** | 固定 | 可增长 |
| **训练方式** | 一次性训练 | 增量学习 |
| **遗忘问题** | 不存在（不更新） | 用回放防止灾难性遗忘 |
| **适用场景** | 标签体系稳定期 | 标签体系扩展期 |

**互补关系**: InsightNet 负责"高效分类已知标签"，OpenCML 负责"发现新标签后扩展分类能力"。

---

## ② 母婴出海应用案例

### 场景：新品上市后标签体系扩展

**业务问题**

春季防蚊产品上市后，TaxoAdapt 自动发现新标签"驱蚊效果""气味刺鼻""出汗脱落"。但 AutoTag 的分类器是在冬季标签体系上训练的，不认识这些新标签，导致大量新品评论被错分或漏分。

传统做法：收集足够多新标签样本 → 重新训练整个分类器（1-2 周）
OpenCML 做法：增量学习新标签 → 当天完成分类器扩展

**流程**

```
Day 1: TaxoAdapt 发现新标签 "气味刺鼻"
       ↓
       人工审核确认标签有效
       ↓
Day 1: 用 OpenCML 收集"气味刺鼻"相关评论（标记为 unknown）
       ↓
Day 2-3: 积累 20+ 条样本后，触发增量学习
       ↓
Day 3: 分类器自动扩展，新评论可正确分类到 "气味刺鼻"
```

**对比**

| 指标 | 传统重训练 | OpenCML 增量 |
|------|-----------|-------------|
| 响应时间 | 1-2 周 | 2-3 天 |
| 旧标签准确率 | 需验证是否退化 | 回放保证不下降 |
| 人力成本 | 重新标注 + 调参 | 仅需审核新标签 |
| 系统中断 | 需停服切换模型 | 热更新，无中断 |

### 场景：多市场差异化标签

**业务问题**

同一产品在不同出海市场（美国、欧洲、东南亚）面临不同的反馈模式。美国用户关注"FDA认证"，欧洲用户关注"环保材质"，东南亚用户关注"性价比"。用统一分类器无法覆盖市场特异性标签。

**OpenCML 方案**

为每个市场维护一个 OpenWorldClassifier 实例：
- 基础层：全球市场共享的标签（质量、物流、服务等）
- 市场层：各市场独立发现的差异化标签

```python
# 全局基础分类器（已知标签）
global_clf = OpenWorldClassifier(...)
global_clf.fit(global_features, global_labels)

# 美国市场分类器（继承全局 + 本地增量）
us_clf = OpenWorldClassifier.from_dict(global_clf.to_dict())
us_clf.collect_unknown(us_texts)  # 收集 "FDA认证" 等 unknown
us_clf.discover_new_classes()     # 增量学习美国特异性标签
```

---

## ③ 代码模板

见 `paper2skills-code/nlp_voc/openworld_class_incremental/` 目录：
- `openworld_classifier.py` — 核心开放世界分类器
- `__init__.py` — 模块导出

运行测试：
```bash
cd paper2skills-code/nlp_voc/openworld_class_incremental
python openworld_classifier.py
```

---

## ④ 技能关联

### 前置技能
- **Skill-AutoTag-SelfEvolving-Label-System** — AutoTag 发现新标签后，OpenCML 负责分类器增量更新
- **Skill-TaxoAdapt-Taxonomy-Evolution** — TaxoAdapt 扩展标签结构，OpenCML 扩展分类能力

### 延伸技能
- **Skill-AltAnnotatorTest-Label-Quality** — AltTest 验证 LLM 标注质量，OpenCML 处理标注中发现的新类别

### 可组合
- **OpenCML + AutoTag + TaxoAdapt**: 三者形成闭环
  1. TaxoAdapt 发现标签结构缺口
  2. AutoTag 用 LLM 生产新标签样本
  3. OpenCML 增量学习新类别，扩展分类器
- **OpenCML + TELEClass**: TELEClass 从种子词构建初始标签体系，OpenCML 持续扩展

---

## ⑤ 商业价值评估

### ROI 预估

| 指标 | 现状（重训练） | 实施后（OpenCML） | 节省/提升 |
|------|---------------|------------------|----------|
| 新标签分类器更新 | 1-2 周 | 2-3 天 | **85%↓** |
| 分类器维护人力 | 0.5 名算法工程师 | 0.1 名（审核即可） | **80%↓** |
| 新品上市数据盲区 | 2-3 周 | 2-3 天 | **90%↓** |
| 旧标签退化风险 | 需全量回归测试 | 回放机制保证 | **风险消除** |

**年化收益估算**（年上新 12 次、每次 3-5 个新标签的商家）：

| 收益项 | 计算 | 价值 |
|--------|------|------|
| 算法工程师节省 | 0.4 名 × 40 万/年 | **16 万/年** |
| 新品数据盲区缩短 | 更快发现问题 → 减少退货 | **10 万/年** |
| 分类器稳定性 | 避免重训练导致的旧标签退化 | **8 万/年** |
| **合计** | | **~34 万/年** |

### 实施难度

⭐⭐⭐☆☆（3/5星）

**难点分析**：
1. 新类别检测阈值需要调参（太严格漏掉新类别，太宽松误报）
2. 特征提取质量决定原型分离度（需要好的文本 embedding）
3. 极端不平衡：新类别样本少，旧类别样本多

**降低难度路径**：
- 从简单场景开始（明确的新品类上市）
- 新类别先人工确认再触发增量学习（人机协作）
- 与 AutoTag LLM 标注结合，快速积累新类别样本

### 优先级评分

⭐⭐⭐⭐☆（4/5星）

**评估依据**：
- **痛点强度**: 标签扩展后的分类器更新是实际瓶颈
- **独特价值**: 与现有技能形成"发现→标注→学习"闭环
- **可落地性**: 代码模板已提供，prototype-based 方法易于理解和调参
- **依赖关系**: 需在 AutoTag 和 TaxoAdapt 稳定后实施

---

## 附录：论文信息

| 项目 | 内容 |
|------|------|
| **主论文** | OpenCML: Open-world Continual Learning for Multimodal and Multilingual Data |
| **作者** | Jitendra Parmar 等 |
| **会议** | ACL 2025 |
| **核心思想** | 开放世界持续学习：识别新类别 + 增量学习 + 防遗忘 |
| **与 InsightNet 关系** | 互补：InsightNet 封闭世界高效分类，OpenCML 开放世界动态扩展 |
| **代码状态** | 论文声明开源但仓库 404（本实现为自研简化版，基于 Prototype-based OWL） |
