# ---------------------------------------------------------------------------
# paper2skills 版本控制忽略规则
# 目标：仓库只跟踪「文本资产」（卡片 / 脚本 / 文档 / 数据清单 / 报告），
#       不跟踪体积巨大的二进制与中间产物。首次纳管时间 2026-09-12。
# ---------------------------------------------------------------------------

# --- 密钥与凭证（硬性禁止入库）---
# 背景：2026-09-12 首次纳管时发现仓库根目录存在未加密的 DDDD.pem。
# ⚠️ 2026-09-13 更正：DDDD.pem 的首行是 PEM 私钥头（`BEGIN RSA PRIVATE KEY`），
#    即它是 **RSA 私钥**，不是所有者以为的「SSH 公钥」。持有它即可登入对应服务器。
#    本仓库从未 commit 过它（27 个 commit 全扫过，0 命中），但它另有 3 份副本散落在
#    ~/Downloads/、~/project/思维模型/、~/project/Agent/lute-momcozy-audit/。
#    本条目的用途是「永久拦截」，不是「曾经的清理记录」。
# （此处刻意不写完整的 PEM 头字面量 —— 写了会被 scan_secrets.py 判成私钥命中，
#   而为此把 PEM 头加进白名单等于放行所有私钥，是本末倒置。）
# 任何 *.pem / *.key / 凭证类文件一律不得入库。
*.pem
*.key
*.p12
*.pfx
*.jks
*.keystore
id_rsa*
id_ed25519*
*_rsa
*.ppk
credentials.json
secrets.json
secrets.y*ml
*secret*.json
*credential*.json
# 2026-09-13 补：凭证的常见旁路文件名（真正的防线是 pre-push 扫描，见 paper2skills-skills/paper-维护/scripts/scan_secrets.py）
*.pem.*
*.key.*

# --- 论文与数据资产：二进制 / 表格 / 图片 / 压缩包（体积大，不入库）---
# 说明：这些是「原始数据资产」，可由采集脚本重新生成，或本就属于业务侧数据仓库。
#       需要长期归档请走对象存储，不要塞进 git。
*.pdf
*.xlsx
*.xls
*.xlsm
*.docx
*.pptx
*.zip
*.tar
*.gz
*.7z
*.png
*.jpg
*.jpeg
*.gif
*.webp
*.svg
*.mp4
*.mov
*.log
*.bak
*.sql
*.parquet

# --- 依赖与构建产物 ---
node_modules/
__pycache__/
*.py[cod]
.venv/
venv/
.env
*.egg-info/
.pytest_cache/
.DS_Store

# --- 论文 PDF 全文（体积大；按需从 arXiv/出版商重新获取）---
# 保留目录结构与 README，仅忽略二进制正文
paper2skills-vault/papers/**/*.pdf
paper2skills-vault/07-NLP-VOC/papers/**/*.pdf
*.pdf

# --- NLP-VOC 子项目的历史大体积中间产物（已迁出子项目，此处仅留档）---
# 这些 jsonl 单文件 500MB+，git 无法有效管理；如需归档请走对象存储
paper2skills-vault/07-NLP-VOC/research/**/*.jsonl
paper2skills-vault/07-NLP-VOC/research/**/*.parquet
paper2skills-vault/07-NLP-VOC/research/**/*.csv
paper2skills-vault/07-NLP-VOC/research/**/*.db
paper2skills-vault/07-NLP-VOC/research/**/*.sqlite*

# --- 演示与媒体 ---
paper2skills-vault/07-NLP-VOC/presentation/**/*.{mp4,mov,png,jpg,zip}
*.mp4
*.mov

# --- 调研中间产物（可复现，不入库；只入 reports/ 与 data/*.md）---
paper2skills-research/data/**/*.json
paper2skills-research/data/**/*.csv

# --- 本地备份 ---
_backup/
*.tar.gz

# ---------------------------------------------------------------------------
# 显式保留（即使被上面的规则命中）
# 说明：候选池 JSON/CSV 是「本轮检索的可复现证据」，体积可控，必须入库。
# ---------------------------------------------------------------------------
!paper2skills-research/data/arxiv_candidates.csv
!paper2skills-research/data/arxiv_candidates.json
!paper2skills-research/data/journal_candidates.csv
!paper2skills-research/data/journal_candidates.json
!paper2skills-research/data/recommendations.csv
!paper2skills-research/data/recommendations.json
!paper2skills-research/data/skill_audit.json
# 缺口账与靶区工单（PHASE6 F3）：**不是中间产物** —— S3/S4/S12 直接消费它的顺序与格号。
# 提升入库的理由同 skill_audit.json：它是一份要被下游引用的账，重算需要跨仓库依赖
# （产品侧 dsh-paper2skills/data/classification.json），不是随手可再生的中间态。
!paper2skills-research/data/gap-ledger.json
# 53 张卡的分类结论（PHASE6 F5）：这是**输入**而非中间产物 —— 它是 5 个分类子代理的
# 逐卡结论，重跑要重读 53 张卡全文再判一次，不是脚本能再生的东西。
# 产物（卡端事实源）在 paper2skills-vault/07-资源库/card-classification.json。
!paper2skills-research/data/card-classification-inbox.json
# backlog→L3 路由输入（PHASE6 S3 / 任务板 B9）：**这一层是人工判断，不是中间产物** ——
# 「哪篇论文能服务哪个 L3 责任」在仓库里没有机读事实源可 join（技术域 ≠ 业务责任名，
# 而卡的 L3 落点只覆盖**已经有卡**的论文），故它是 S3 唯一的源文件：丢了就要重判 12 篇 × 22 条。
# 提升入库的理由同 card-classification-inbox.json。
!paper2skills-research/data/backlog-l3-map.json
# S3 的接管表产物（位次 + 覆盖率账）：下游「出卡排期」要消费它，理由同 gap-ledger.json。
!paper2skills-research/data/backlog-routing.json
# S9 独立站 holdback 的**判决产物**（验收面 L11a 的被检对象）：门禁 `--check` 就是拿它
# 与现算逐字段比对。⚠️ 原先被 `data/**/*.json` 挡住 ⇒ **新克隆里这个文件不存在**，
# L11a 会停在 `exit 2 没拿到输入`（不是判红，也不是通过 —— 台账 #12 那一族：
# 「交付物不在仓库里」＝ 那条门禁在别人机器上从来不跑）。理由同 gap-ledger.json。
!paper2skills-research/data/holdback-design.json
# 「同名同物」判定的可见豁免清单（验收面 L8a 的**输入**）：它是人的判定 + 到期条件，
# 不是脚本能再生的东西。⚠️ 不入库的代价是**新克隆必然没有它** ⇒ L8a 直接 exit 2
# （「baseline 不存在」= 没测，不是通过），陷阱对新人 100% 命中 —— 与门禁 bug #12
# （`k1_l5.json` 被 gitignore 导致 gate_check 全库每张卡都红）**完全同型**。
!paper2skills-research/data/card-identity-baseline.json
# 材料引文核验产物（PHASE6 S1 收口期）：**它是一份账，不是中间态** ——
# 逐词记录「哪些引文在材料里查无实据、最长可命中片段是什么、材料原句是哪句」。
# 重算需要材料目录（在仓库之外，且它自己是个 git 仓库）⇒ 不是随手可再生的东西。
# 提升入库的理由同 gap-ledger.json。判据=`scripts/check_material_citations.py`。
!paper2skills-research/data/contracts/material-citations.json
# 缺口驱动的三段式检索式（PHASE6 S4）：**它是下一段的输入，不是中间态** ——
# S4 只生成检索式、不执行检索；发查询/收割/打分要直接消费这份文件里的向量与契约挂载点。
# 重算需要缺口账 + 图谱 + candidate_filter 的负向词分歧矩阵三处同时到位。
# 判据=`scripts/build_search_queries.py --check`。
!paper2skills-research/data/search-queries.json
# venue_tier 回填的**证据账**（PHASE6 S10）：逐张卡记录「取值 + 来源」。
# 它不是中间态而是**溯源凭证** —— 本仓库铁律 1 要求 venue 类声明必须由 arXiv 元数据 /
# 出版方 DOI 判定，不得用底本正文；这张账就是那些判定的落点。
# 重算需要现场打 arXiv abs 页 + Crossref（有网络依赖，且外部元数据会变）。
!paper2skills-research/data/venue_tier_backfill.json
# 旧方案页收割产物（PHASE6 S2）：**它是负知识唯一的落点，不是中间态** ——
# 19 个旧方案页是**第四套平行分类**（不绑定任何 AGT/SCN），其「三大架构陷阱」是
# 已被作者验证过的负知识，**不可能从 64 格骨架推出来**；194 条卡位索引已显式丢弃并登记。
# 丢了这个文件，被丢弃的那 194 条与 54 条陷阱就都从账上消失了。
# 重算需要 playbook/solutions/ 下 20 个 HTML 的现场指纹。判据=`scripts/harvest_legacy_solutions.py --check`。
!paper2skills-research/data/legacy-solutions.json
!paper2skills-research/data/dedup_report.json
!paper2skills-research/data/bundles/**

# 凭证暴露面白名单：公开证书必须逐条写明理由，否则门禁判红（不被忽略就等于没交付）
!paper2skills-research/data/key-exposure-whitelist.json

# 凭证历史扫描的两份**判据输入**（S8 第三缝，2026-09-13）：
# ① 已知暴露值登记表 —— 只存 len+sha256；丢了它 ⇒ 判据 C 静默退化成「登记表 0 条 ⇒ 全绿」，
#    那正是它要防的那类假绿。② 可见豁免表 —— 每条必须写 reason+evidence+expires_when。
# 两份都不入库＝新克隆没有＝等于没交付。判据=`scripts/check_history_secrets.py --check`。
!paper2skills-research/data/exposed-credential-registry.json
!paper2skills-research/data/history-secrets-whitelist.json
