---
title: PHASE6 —— 五层能力图谱与需求驱动扩充：最终方案与执行 TODO
doc_type: plan
module: 00-项目管理
status: locked_2026-09-13_decisions_complete
created: 2026-09-13
owner: self
source: human+ai
decisions: D1–D5（骨架）+ Q1–Q12（细化），共 17 项，全部已由所有者拍板
board_revision: 6（PHASE6 = 1 主卡 + 18 子卡）
supersedes: 本文件取代 2026-09-13 早先的 draft_pending_decisions 版本
---

# PHASE6 · 五层能力图谱与需求驱动扩充（最终方案）

> **把「顶刊 Paper → skills」从关键词驱动的采购，改成缺口驱动的建账 —— 给 151 个岗位责任建供给契约。**
>
> 事实源：`_survey_card_inventory.md`（146 张卡）· `_survey_org_model.md`（需求侧 940 行）·
> `_survey_product_form.md`（产品形态 791 行，全部现场探活）· `CLAUDE.md`（门禁与铁律）·
> `PHASE5-明日执行TODO.md`（遗留项）
>
> **状态：`locked_2026-09-13_decisions_complete`。** 17 项决策已定，骨架不再变。

---

## 0. 一句话

**不是缺论文，是缺链。** 供给侧有 1338 + 146 张卡、6 道门禁、2,087 条逐字引文；需求侧有 50 岗位 / 151 责任名 / 64 个「流程 × 阶段」格。但中间三层各说各话——材料自报 `required_skill_contract_refs` **全为空数组**、`skill_contract_mapping_status = not_instantiated`；19 个解决方案页**不绑定任何岗位或场景**；**53/146 张精选卡不在任何分类里**。

---

## 1. 四个推翻既有假设的实测发现

**发现 1：论文卡页面已经存在，本期不是建页面。**
`packages/surfaces/dsh-algo-skills-local` 就是 paper→skills 页。探活 `GET /api/dsh-algo-skills/health` → HTTP 200：`skills:1338, placed:1327, unplaced:11, wired:127, wiredAnywhere:258, emptyRoles:2`。join 键 `card.l3_business → role.responsibilities`，源码明确「**owns no classification fact of its own … The card IS the data**」。
⇒ 接入缝改 **4 个文件**（`src/collect.ts` 的 `parseSkill()` 是唯一解析点）。**不要**扩建 `dsh-overseas-skills`（需 +1338 条 taxonomy mapping、会把 1338 张卡灌进每 2 秒轮询的 `/list`、两套 `wiredIndex` 语义打架）。

**发现 2：需求侧自报「Skill Contract = 0」。** 八条 FLOW 的 `required_skill_contract_refs` 全是空数组。**这就是本项目的真实定位：不是多出几张卡，是给 151 个责任建供给契约。**

**发现 3：解决方案层是孤儿。** `playbook/solutions/` 有 19 个方案页（L1–L6 架构 + 路线图 + 三大陷阱 + 194 卡位索引），grep `AGT-0` / `SCN-0` **零命中**。这是第四套平行分类。

**发现 4（Q6 探查时撞出）：已安装的 1338 张 p2s 卡全是源站预览版。** 含「原文引用」的 = **0/1338** —— PHASE4 建的 2,087 条逐字引文**一条都没进产品**。

---

## 2. 五层链现状

| 层 | 唯一事实源 | 规模 | 机读 | 已实测的缺口 |
|---|---|---|---|---|
| L1 岗位 | `role-catalog.json` | 50 | ✅ | — |
| L2 责任域 | `organization-graph.json` | 4 面 / 8 域 | ✅ | `plane_id` 只在 org-graph；只读 role-catalog 会**静默丢平面维度** |
| L2b 细分业务 | `role-catalog.skills[]` | **151**（全局唯一，每条唯一属一岗） | ✅ | 供给侧 Skill Contract = 0 |
| L3 业务场景 | `collaboration-graph.json` | 20 SCN / **FLOW×STG = 64** | ✅ | FLOW-01/05/07 当前无法自动定主岗位 |
| L4 解决方案 | `playbook/solutions/*.html` | 19 个方案 / 194 卡位 | ❌ 只有 HTML | 不绑定任何 AGT / SCN / L3 |
| L5 算法模型 | vault 146 + playbook 1338 | 146 / 1338 | 部分 | 53 张精选卡无归属；98/151 责任名无精选卡 |

**两条 join 陷阱**：① `plane_id` 只在 `organization-graph.json/roles[]`（两视角刻意正交：AGT-050 域=数据与AI运行/平面=独立控制）；② **岗位↔场景两口径差 5.05 倍**（直连 `role.scenarios` **127** 条 vs flows→scenario_ids 闭包 **641** 条）。

> ⚠️ **2026-09-13 数字订正（F2 实测）**：本节原写「118 / 701（5.9 倍）」，**该数字不成立**。
> 由 `build_capability_graph.py` 实测：直连 **127**（`role-catalog.json` 逐岗相加，去重后同值）、
> 闭包 **641**（**三种独立算法全得 641**：按 `flow.scenario_ids` 并集 / 按 `flow_scenario_bindings` /
> 按 `SCN.eligible_flow_ids ∩ role.flows`）。**结论方向不变**（SCN 仍是带重数的覆盖层，
> 「降为标签」的决定不受影响），错的是量值。教训同「基线数字可能是过期照片」：
> 方案文档里的数字也要能被脚本复算，否则它会带着错误一路传下去。

**已校验一致**：8 FLOW 贡献岗位（19/26/13/20/14/12/19/17=140）、51 条 FLOW×SCN、64 条 FLOW×STG、`role.flows` vs `role_contributions`（50/50）、`domain` vs `group`（50/50）。
**真不一致仅 1 条**：AGT-012 声明 `scenarios` 含 SCN-020，但其 `flows`=FLOW-02/07，而 SCN-020 的 `eligible_flow_ids` 只有 FLOW-06/08。

---

## 3. MECE 设计（D1 + D2）

### 3.1 前两层是划分，后三层只能是覆盖

| 层 | MECE 性质 | 依据 |
|---|---|---|
| 50 岗位 / 8 域 | **划分** | 4 面 5+35+5+5=50；`domain` vs `group` 50/50 |
| **151 细分业务** | **划分**（缺口账级） | 151 条全局唯一、每条唯一属一岗 |
| **8 FLOW × 8 STG = 64** | **划分** | `flow_stage_bindings = 64`，已机读 |
| 20 SCN | **覆盖，多重数** | **127 vs 641**（2026-09-13 F2 实测订正，原写 118/701）；**降为标签** |
| N 个解决方案 | **覆盖** | 一个方案跨多格 |
| N 张卡 | **覆盖** | 一卡可挂 1–3 个 L3；1338 张里 **269 张跨面** |

### 3.2 64 格分型（本期最重要的设计结论）

64 格里**只有 24 格能挂算法模型**。按 `protocol.stages[].model_participation` 实测分型：

| 型 | 阶段 | 格数 | 该格放什么 |
|---|---|---:|---|
| **M · 方法格** | STG-04 证据收集与诊断 / STG-05 方案与标准产物 / STG-08 结果核验与异步学习 | **24** | 算法模型在此接入 |
| **R · 规则格** | STG-02 范围确定与Case创建 / STG-06 Assurance接收门禁 | **16** | 契约与判据；模型只做**有界分类** |
| **D · 确定性格** | STG-01 信号接收 / STG-03 上下文装配 / STG-07 受控动作 | **24** | 无模型参与，**不消耗检索预算** |

> ⚠️ 这推翻了「64 格 = 64 个解决方案」的直觉读法。已立为风险 N4：D/R 格出现算法模型即判错。

---

## 4. 缺口账

> ✅ **2026-09-13 F3 落地：本节全部数字已由脚本复算，并逐格断言相等。**
> 生成器 `paper2skills-research/scripts/build_gap_ledger.py`（判据 J1–J7），
> 人读产物 `PHASE6-F3-缺口账与靶区工单.md`（36 条工单按位次排序），
> 机读产物 `paper2skills-research/data/gap-ledger.json`。
> 复算：`python3 paper2skills-research/scripts/build_gap_ledger.py --check`。
> **下表 15 个格子 + 靶区一的 7 个域计数 + 三个靶区的条数，全是断言**；
> 任一格改了而工单没重跑，`--check` 就红。这是本节被 F2 教训（「方案里的数字也要能被脚本复算」）
> 兑现的地方 —— 在此之前，下面这些数字只是一张没法被质疑的照片。

> ⚠️ **2026-09-13 F5 更新：下表是 F5 时点值（146/146 张卡有 L3 落点）。**
> F3 首次落库时精选线只有 93 张卡有落点，故当时是另一组数。**差额全部来自上游补分类，
> 没有一条 L3 的语义、A/B/C 判定或边界条目被改动**。两套数并列，不擦掉旧值：

| 算法可服务性 | 零供给 | 仅 legacy 供给 | 有精选卡 | 合计 |
|---|---:|---:|---:|---:|
| **A · 可算法化** | 4 (5) | 27 (31) | 42 (37) | **73** |
| **B · 半可算法化** | 8 (9) | 33 (41) | 25 (16) | **66** |
| **C · 难算法化** | 3 (3) | 9 (9) | 0 (0) | **12** |
| **合计** | **15** (17) | **69** (81) | **67** (53) | **151** |

> 括号内 = F3 时点（93/146 有落点）。差额：有精选卡 +14、仅 legacy −12、零供给 −2。
> 断言只认现行值；F3 值留在脚本常量 `EXPECT_TABLE_F3` 里作历史基线。

- **靶区一**：A 类 ∩ 无精选卡 = **31 条**（供应与履约 9 / 产品与创新 6 / 渠道经营 4 / 财务与合规 6 / 品牌与增长 3 / 经营与组织 2 / 数据与AI运行 1）。⚠️ F3 时点为 **36 条**（11/7/6/6/3/2/1）—— 缩掉的 5 条正是 F5 新分类的卡已经供给上的。
- **靶区二**：**15** 条零供给中 **9 条是结构性空白**（硬件研发/工业设计/工程制造，AGT-009/010/011/012/013）→ 登记「走 SOP，不占检索预算」。（F3 时点：17 / 10）
- **靶区三**：A 类 73 是「算法可作主引擎」口径的诚实计数，**不是 40**；10 条边界条目（供应商评估/产能调查/测试方案/研发计划/履约跟踪/到货异常追踪/术语治理/内容实验/平台运营/失败恢复）改用更严口径时优先降级。**A 名单随阈值变，B 的 66 条不变** ⇒ 下游把 A 视为「优先匹配」、B 视为「需补外部证据后匹配」。
- **低优先级位**：10 个岗位无任何 A 类责任（AGT-004/031/036/037/038/039/042/044/047/050）—— 不是缺陷，是定位。

### 4.1 F3 实测的三条登记（**不改本节结论，但改它的适用范围**）

> ⚠️ **另有第四条（2026-09-13 F5 追加，单独列在 §4.2）**：本节的数字随上游补分类而变过一次。
> 那不属于「F3 的三条登记」（那三条都是 F3 当场发现的判据/材料问题），故另起一节。

**① 材料内部矛盾：`产能调查` 在 §F.5 被列为 A 类边界条目，而 §F.3/§F.4 判它 B。**
上一条 bullet 的 10 条边界条目名单里，实际只有 **9 条**是 A 类。
处置：以 §F.3 为准（它是唯一「岗位+责任名+理由」俱全的逐条表），`产能调查` 按 B 处理、
**不计入靶区一**（靶区一是按「A ∩ 无精选卡」定义的）。
这条是给图谱加断言时**当场抓到**的（原断言：「边界条目必须本身是 A 类」）；
处置不是删断言，而是改成「非 A 的边界条目必须逐条登记在 `a_scope.contradictions`」——
已知的 1 条被显式承认，新出现的仍会打红。同 JT-3 的处理口径：**记进字段，不悄悄吞掉，也不替人重判。**

**② 「不占检索预算」有两种读法，差 2 条 —— 本账默认取严口径（本节原文），差额留人裁决。**
本节说的是「17 条零供给中的 10 条」，即 **L3 级**的名单；靶区一里落在这些条目上的只有
**3 条**（BOM与材料分析 / 商业案例 / 研发计划）⇒ 占检索预算 **33 位**。
若改按**岗位**读（AGT-009..013），靶区一里还有 `测试方案`、`可用性验证` 两条会被划走
（它们**有** legacy 卡，故不在那 10 条里）⇒ 31 位。
脚本把两个数都算出来并写明 `default: strict` —— 悄悄放宽一个已定的定义，正是本仓库
反复记录的失败模式（漏洞 #11 / C2 / 两条事实核验铁律）。

**③ 排序权重只用于排序，已用扫描把它挡在门禁之外（风险 N1）。**
`build_gap_ledger.py` 的 J7 会扫 8 个门禁脚本（含 `sync.py` 与两个 `--check` 门禁），
命中产物名即失败；并对三种引用写法各喂一份注入样本，证明该扫描不是摆设。
⚠️ 首跑时它报过一次**假阳性**：`build_capability_graph.py` 的文档字符串里写了「供缺口账 join」——
那是上游的说明，不是数据流。修法是把 needle 收窄到**能定位产物的标识**
（当时的 needle 表：`gap-ledger` / `gap_ledger` / `缺口账.json` / `PHASE6-F3`），而不是为了过闸去删文档。

### 4.2 F5 追加：本节数字随上游补分类变过一次（**上游变了，不是判据变了**）

F5 给精选线剩下 53 张卡补上 L3 落点后，**缺口账的每一格都变了**：有精选卡 53 → **67**、
仅 legacy 81 → **69**、零供给 17 → **15**、靶区一 36 → **31**、零供给结构空白 10 → **9**。
这是一次**正常的、必须发生的**变化 —— 上游确实多了 53 张有归属的卡，
而 A/B/C 判定、边界条目、L3 语义**一个都没动**（三处都是同一份材料解析出来的）。

处置（三条，都是为了让「数字变了」和「判据坏了」区分得开）：
1. **两套数并列**：上表用括号保留 F3 时点值；脚本里 `EXPECT_TABLE_F3` 留作历史基线，
   现行断言只认 F5 时点值。旧值不擦掉 —— 擦掉就看不出「变化有多大」。
2. **差额必须能归因**：±14/−12/−2/−5/−1 全部由「F5 补了 53 张卡的 L3」这一个原因解释。
   若下次再变而原因不是这个，就必须先解释清楚再改断言。
3. **工单报告自动带差额表**：`PHASE6-F3-缺口账与靶区工单.md` 里新增「与 F3 首次落库时的差额」
   一节，档期、原因、逐项差值都在产物里，而不是只在提交信息里。

⚠️ **J7 的第二次假阳性（同族，当场登记）**：F5 在图谱脚本里加了一句改动说明
「F3 修在 `build_gap_ledger.py`」，**裸模块名 needle `gap_ledger` 当场把它判成「分数进了门禁」**。
同 F3 的处置：继续收窄 needle —— 只留**产物标识**（`gap-ledger` / `缺口账.json` / `PHASE6-F3`）
与**能表达数据流的语句**（`import gap_ledger` / `from build_gap_ledger`）；
裸模块名被移除，因为它同时是「脚本文件名」和「产物名」，**无法区分两者**。
并补一条**反向探针**：只提脚本名的散文**不得**判红 ——
**豁免条款必须比拦截条款测得更严**（这条是 F3 留下的纪律，F5 第一次真的用上）。

---

## 5. 必须直面的负结果

`dsh-paper2skills/docs/eval-report.md` 实测（**六组对比方向完全一致**）：

| 对比 | Δ(treat−control) | p |
|---|---:|---:|
| run1 holdout | **−0.85** | 0.0038 |
| v2 train · C-vs-S | −1.25 | 0.0245 |
| v2 holdout · C-vs-T | **−0.95** | 0.084 |
| v2 holdout · C-vs-S | **−1.95** | 0.0001 |

判官噪声 ±0.6~0.7 ⇒ 都是真信号。机制：**卡里装的是论文的方法与参数，而任务问的是这家公司该怎么干** —— 模型把「信息熵 ≥ 0.65」当业务阈值，把「欺诈召回由基线 42% 提升至 ≥60%」当事实（用户从未给过这个基线）。

SkillOpt 补丁被 held-out 门**拒绝并回滚**：它让 26/40 份产出挂上对冲措辞（对照组 0/40），Δ 从 −0.95 变成 **−1.10**。

⇒ **算法模型层不能直接对着交付物暴露。「解决方案」不是分类的一级，它是强制翻译层**：卡供方法，契约供业务取值与标定规则。这与 PHASE5 的 X1「作者外推」三态机制是**同一条纪律的反方向**，共用同一套 selftest 纪律。

---

## 6. 决策记录（17 项，全部已定）

### 6.1 骨架决策 D1–D5

| # | 决策 | 结论 | 影响 |
|---|---|---|---|
| **D1** | 职责域粒度 | **两级都留**：8 域做导航，**151 细分业务做缺口账** | 两者必须同一生成器产出，禁各自维护 |
| **D2** | 业务场景 MECE 底 | **8 FLOW × 8 STG = 64 格**；SCN 降为标签 | **暴露 M24 / R16 / D24 分型**（§3.2） |
| **D3** | 解决方案层来源 | **按 64 格重新设计**（偏离推荐项） | 经 Q1 修正为「8 方案域 + 139 契约」，M 格降为索引；旧 19 页转素材收割 |
| **D4** | 两语料收编 | **精选线作前台主供给，legacy 降为宽底 + `quality_tier`** | 93 张同 ID 由精选覆盖 |
| **D5** | T-E 是否纳入本期 | **纳入且第一优先** | 翻译契约写进契约层，与 T-D1 同一份交付物 |

### 6.2 细化决策 Q1–Q12

| # | 问题 | 结论 | 关键后果 |
|---|---|---|---|
| **Q1** | 解决方案层交付物颗粒 | **8 份方案域（=FLOW）+ 139 份标定契约；24 M 格降为索引维度** | 入卡挂载点从 24 个粗桶变成 73 个 MECE 格子 |
| **Q2** | 契约覆盖范围 | **139 = 151 − 12(C类)**，两种模板 | A 模板拦「参数移植」；B 模板拦「完整性虚报」 |
| **Q3** | 分批顺序 | **按 FLOW 纵向切片，先 FLOW-01 全做** | ⚠️ **本行数字已于 2026-09-13（S1 开工）订正**：原写「1 方案域 + **39 契约（25 可写 / 14 待卡）**」，实测复算不出来。真值由 `build_contracts.py --flow FLOW-01 --batch-only` 现算 = **54 份（A 40 / B 14）**；39 = 25（A 类·有卡，F5 前快照）+ 14（**B 类总数**，被当成「待卡数」）—— 两个不同口径的数被相加，还被当成了批次规模。可写/待卡按 S1 定死的卡片口径为 **52 / 2**（只认精选线时 33 / 21） |
| **Q4** | 入卡门槛 | **弱门槛**：卡的 L3 属 A/B 类即放行 | 实测 93/93 已分类卡可过，不拒收存量；放弃的防护转由 Q5 承担 |
| **Q5** | 消费侧闸门 | **白名单加「且该卡已被契约引用」** | 入卡宽、消费严，闸门落在真正出事的那一步 |
| **Q6** | p2s 安装源 | **换底 + 全文入 `references/`** | 逐字引文、K1 验证过的代码、溯源字段一次性进产品 |
| **Q7** | 顶刊准入线 | **先统一三套词表 + 回填 121 张卡，再按「方法论/工程类 vs 商科实证类」分域定线** | 现状 venue_tier 覆盖率仅 17%，测不出来的政策不是政策 |
| **Q8** | `DDDD.pem` 处置 | **登记为独立安全条目，本轮不动** | 已确认为**腾讯云生产 SSH 私钥**，四份同哈希副本 |
| **Q9** | 增量测量主体 | **自有独立站为主** | FLOW-01 主岗位由 AGT-021（Amazon）改为 **AGT-023 自航（独立站）** |
| **Q10** | holdback 现状 | **没有但可以建 —— 需先建** | 6 条 A 类契约（增量分析/广告实验/预算分配/投放诊断/实验设计/内容实验）标注「方法待启用」 |
| **Q11** | 出海历史 | **2 个完整年度** | 季节因子可从自身估形状，但须与品类/行业先验对账；「先验从哪来」成为新增取数项 |
| **Q12** | 契约的数据判据 | **另建「本业务数据可得性」五维判据**，与 registry 的 `data_availability` 分开 | 避开类别错误：论文数据公不公开 ≠ 本公司拿不拿得到 |

### 6.3 四条纪律（决策过程中立下）

1. **决策前提可能被后续分析改写** —— D3 的原前提是「19 → 64」，而 D2 落地后实测为 M24/R16/D24。**前提变了就把问题重开**，不将错就错。
2. **风险不会消失，只会转移** —— Q4 取弱门槛不是消除风险，是把风险推到消费口；因此 Q5 必须在同一轮里补上。任何「放宽」都要同时指明承接方。
3. **词汇不新造，取自材料** —— 数据可得性五维用 `AGT-046` 的「质量状态·来源·新鲜度」与 `AGT-045` 的「指标契约」，不发明第三套术语。
4. **同一份东西不要设计两次** —— D3 选重设计后，D5 的翻译契约就写进契约层，而不是在旧方案结构里另嵌一套。

---

## 7. 最终交付物结构

```
L0 岗位        50 个 AGT（4 面 / 8 域）
L1 责任        151 细分业务（MECE 划分，每条唯一属一岗）
L2 场景        8 FLOW × 8 STG = 64 格（M24 / R16 / D24）
L3 方案        8 份方案域（= 8 FLOW）              ← 人类可读的长文
L4 契约        139 份标定契约（A73 + B66）           ← 机器可校验的短表
L5 模型        N 张算法卡（挂载点 = L4 契约）
```

**139 份契约的两种模板**

| | A 类契约（73） | B 类契约（66） |
|---|---|---|
| 拦什么 | **参数移植**（−0.95 的机制） | **完整性虚报**（算法只做一段，却宣称整条责任已覆盖） |
| 核心字段 | 方法来源（卡）/ **数据要求（五维）** / 标定规则 / 重标定触发条件 / 适用 FLOW（多值） | 算法可达部分 / 不可达部分 / **必须的外部证据与责任岗位** / **该格何时必须冻结** |
| 成本 | 高（每条要读卡、定标定规则） | 低（判定理由在 `_survey_org_model.md` F.3 已逐条写好，可半自动生成） |

**契约的两种状态**：`可写`（有卡，53 条 = A 37 + B 16）| `待卡`（无卡，86 条）。**「待卡」的契约就是扩充工单本身** —— 不需要另造 gap ledger。契约先存在，卡去认领（认领制为 Q4 放弃项，故仅登记不拦截）。

**数据可得性五维（Q12）**：① 获取路径（自有埋点/平台后台/第三方 API/需授权/不可得）② 粒度 ③ 回溯深度 ④ 新鲜度 ⑤ 口径归属（是否已被 AGT-045 主数据契约定名）。反后门：**不得只写「数据可得」，必须点名获取路径**；「不可得」是合法结论，不得被改写成「partial」。

---

## 8. 执行 TODO（任务板 revision 6，PHASE6 = 1 主卡 + 18 子卡）

### 8.1 依赖图

```
F1 基线钉住 ──→ F2 图谱 ──┬─→ F3 缺口账 ──→ S4 检索式 ──→ (扩充)
                          │
                          ├─→ F6 契约模板（第一优先）──→ S1 8方案域+139契约
                          │        ↑
                          │   S9 holdback / S10 venue / S11 数据五维
                          │
                          └─→ S12 消费口闸门 ✅已交付 / S13 入卡弱门槛
F4 分类轴 ──→ F5 分类 53 张卡
S5 p2s 换底（独立线）
S8 安全（独立线，本轮不动）
```

**三条硬顺序约束**
1. **F2 必须先行** —— 没有唯一事实源，F4/F3/S1 会各自造第二份分类表（产品侧已吃过 R4：归位判定的校验器不在包内，`--check` 只会说「一致」）。
2. **S1 必须排在 F6 之后** —— 模板定错则 139 份全返工。
3. **S4 必须排在 S1 之后** —— 入卡挂载点就是契约，契约没有就没有挂载点。

### 8.2 第一批任务卡（P0）

> ✅ **P0 已全部清空（2026-09-13）**：F1 `890237a`+`f4a8c5e` · F2 `f8d2a0d` · F3 `4e1b498` ·
> F4+F5 `59429b9` · F6 `4b4aec7` · F7 `79d4be0`。七张卡各自都有**能失败的判据**（selftest / 变异样本 /
> 对照测量），没有一张是「写完了所以算完成」。
> ⚠️ 三条硬顺序约束已兑现：F2 先行（图成为唯一事实源，F4/F3/S1 都从图上取）→ F6 先于 S1（模板定错则 139 份全返工）
> → S4 待 S1（入卡挂载点就是契约）。
| 板号 | 任务 | 为什么排这里 | 预计 |
|---|---|---|---|
| **F1** | 基线钉住：R1（README 222↔**223**/13↔**14**/22↔**32**）+ R4（归位语义校验入包）+ 门禁 bug #11/#12 + `verified_at` | **唯一会让人在错误基线上做决策**的风险 | 2h |
| **F2** | T-A `capability-graph.json`：MECE 声明 + multiplicity + **64 格 M/R/D 分型入库** | 图不定，后面全返工 | 1d |
| **F3** | 缺口账脚本 + **36 条靶区工单** | 靶区能算，才能接管 backlog | 0.5d |
| **F4** | 五层分类轴；技术域降为 facet；`venue_tier`/`quality_tier` 升为可筛面 | 53 张卡等着落地 | 0.5d |
| **F5** | 分类 53 张未分类精选卡（**含 PHASE3/4 新卡，质量最高**） | 最高质量资产没有归属 | 1d |
| **F6** | **【第一优先】** 契约双模板（A/B）+ M 格方案规格模板 + 3–5 卡小样本 L7 重跑 | 唯一有实测负结果处；模板错则全返工 | 1d |
| **F7** | 优先级打分 `P = 业务价值 × 缺口度 × 证据可得性 ÷ 用错风险` | 防空转：**改权重则排序必须变** | 0.5d |

### 8.3 第二批（P1）

> **进度（2026-09-13，S1 开工）**：S1 的第一批（FLOW-01）已交付 ——
> 1 份方案域（`07-资源库/solutions/FLOW-01-存量GMV联合经营.md`）+ **54 份契约**（A 40 / B 14）。
> 方案层由此从「空槽位」变成**图谱派生**：`cells[].solution_refs` 现由 `solutions/*.md` 算出，
> 把源目录换空 ⇒ 64 格 refs 全清空（selftest 变异 A）。其余 7 条 FLOW 仍为空，**空 = 还没做，不是缺陷**。
> ⚠️ ~~**S1 还没拦住任何东西**：契约现在是文档，闸门在 **S12**~~ ⇒ **2026-09-13 已不成立**：
> S12 已交付，**契约层真的拦住了东西** —— 硬拦模式下模型目录的 p2s 条目 147 → 32（−115），
> 被移走的每一条都能在账上追到「它没有任何契约引用」。见 `reports/PHASE6-F8-S12-消费口闸门.md`。

| 板号 | 任务 |
|---|---|
| **S1** | **8 份方案域 + 139 份契约（先 FLOW-01 全做）** —— ✅ **FLOW-01 批次已交付**（1 方案域 + 54 契约，见 `reports/PHASE6-F8-S1第一批-FLOW-01方案域与54份契约.md`）；剩 7 条 FLOW + 85 份契约。⚠️ **本行原写「主岗位 = AGT-023 自航」**：正确口径是「**按渠道四选一**」（Amazon→AGT-021；独立站→AGT-023；其他平台→AGT-024；B2B/零售→AGT-025），AGT-023 只是**独立站实例**的主岗位 —— 依据 `FLOW-CATALOG.md` 与 Q9。写成单一主岗位会让另外三个渠道实例没有负责人 |
| **S2** | 19 个旧方案页内容收割（L1–L6 架构 + 三大陷阱 → M 格素材；丢弃 194 卡位索引） |
| **S3** | 12 篇 extract backlog 交给靶区排序接管 |
| **S4** | 缺口驱动三段式检索式（**负向词按域生效**） |
| **S5** | p2s 换底 + 两语料收编 + `references/` 挂全文 |
| **S6** | 产品侧 `dsh-algo-skills-local` 补 4 个呈现字段（改 4 个文件） |
| **S7** | 产品侧 R2（死字段恒 0）/ R6（硬链接陷阱，必须走 `pipeline.sh` 的 tmp+mv） |
| **S8** | 【安全】生产 SSH 私钥轮换与收敛（**本轮不动**，顺序不可颠倒） |
| **S9** | 建立独立站 holdback 能力（6 条契约的前置） |
| **S10** | venue 三词表统一 + 回填 121 张卡 + 分域定线 |
| **S11** | 本业务数据可得性五维判据 |
| **S12** | ✅ **已交付（2026-09-13，见 `reports/PHASE6-F8-S12-消费口闸门.md`）**：消费口白名单加「必须被契约引用」。判据一处实现（`dsh-paper2skills/lib/contract-gate.js`），独立核对器 + 白名单生成器共用。**账现算**：1338 已装线 = **已挂契约 29 + 待挂契约 114 + 未接线 1195**；白名单 p2s 去重 143。三态 `count`（默认，过渡期只计数，页面显示「待挂契约」）/ `enforce`（硬拦：147→**32** 条，跨 47 岗）/ `off`。**对照测量六条判据 A–F 全绿，含因果翻面**（摘掉引用某卡的契约 ⇒ 该卡确实退出模型目录）。⚠️ 本卡撞出**三条新门禁缺陷 #23–#25**（最重的是 `x_lute.skills.subset` 假绿账）。⚠️ 板上原文记的「wired 127 / wiredAnywhere 258 / 未接线 978」中，**258 复现成功**（= 白名单全部 distinct 技能：p2s 143 + 平台原生 115），**127 与 978 复算不出来**（且 127+978=1105≠1338，彼此不构成划分） |
| **S13** | 入卡弱门槛落地（L3 属 A/B 类即放行） |

---

## 9. 风险与反后门

| # | 风险 | 对策 |
|---|---|---|
| **N1** | T-C 的优先级分数变成新洗白工具 | 分数只用于**排序**，不得进入任何门禁判定 |
| **N2** | 新增层造出第二份事实源（R4 已证明这类判定会腐烂） | 每层判据**只能有一处** |
| **N3** | T-E 的翻译契约变成新一轮对冲措辞（SkillOpt 补丁就这么死的） | 验收是 **Δ ≥ 0 的 held-out 实测**，不是「条款写上了」 |
| **N4** | D/R 格被误当方法格（40 格白做） | `cell_kind` 进 F2 的**机读字段**；D/R 格出现算法模型即判错 |
| **N5** | 本仓库已发生 4 次「提升来自修门禁而非改资产」 | 每次盘点**主动去撞工具**；每个新判据必须有**能失败**的 selftest |
| **N6** | 基线数字可能是过期照片（X5 已实测推翻过一次） | 引用数字**连口径一起引** |
| **N7** | Q4 弱门槛放掉的「没家的卡」 | 承接方是 S12（消费口）。✅ **已承接（2026-09-13）**：闸门落地并实测可硬拦；过渡期口径下 114 张「待挂契约」是**可见的账**（页面徽标 + manifest 字段 + 独立核对器），不是静默暴露。⚠️ **硬拦的实际启用**要等 139 份契约收敛到足够覆盖 —— 现在硬拦等于让 80% 的目录替一份未完成的工作背锅 |

**元级纪律**：任何**新增豁免**都必须同时写清「什么情况下不许豁免」—— 门禁的豁免条款必须比它的拦截条款**测得更严**。

---

## 10. 开放事实（未答项，不阻塞开工）

| # | 事项 | 状态 | 影响 |
|---|---|---|---|
| **O1** | 材料自承「**其余 20% 渠道构成未明**」（Amazon 65% / 独立站 15% 之外的渠道） | 已问两次未答，记为开放事实 | 影响契约的「适用渠道」字段要开几个值 |
| **O2** | **品类/行业季节性先验从哪来**（Q11 的 2 个年度不足以稳健分离趋势/季节） | 新增取数项 | 影响需求预测/生命周期/促销规划/库存分层/补货模拟 的标定规则 |
| **O3** | `DDDD.pem` 用途已确认（腾讯云生产 SSH 私钥），但**轮换未执行** | 见 B14 / S8 | 已登记，风险在册 |

---

## 11. PHASE5 遗留的归属（重新排序，不另起清单）

| 遗留项 | 归属 | 理由 |
|---|---|---|
| 甲类剩 8 张（A4/A5/A6/A7/A8/A11/A12/A13） | 并入 T-C 排序 | ⚠️ **A6 的修法是「补抓正式版全文」而非改卡** —— 先按铁律 2 查版本 |
| 乙类 7 张 | 并入 T-C | 性质较轻 |
| 两个 `model.py` 与卡片不符 | S5/T-F | 卡内代码已是论文口径，被指向的文件仍旧 |
| 门禁 bug #11/#12 | **F1** | 都是「调用方式陷阱、不是卡片缺陷」 |
| `verified_by` 无时间锚点（22 张声明含 G2，**8 张实测已红**） | **F1** | 加 `verified_at` |
| 61 处悬空 `Skill-*.md` 引用（31 张卡） | 做成 repo_health **C9** | 目前**没有任何门禁检查它** |
| 12 篇 extract backlog | **S3** | 按旧清单出卡可能不在靶区内 |
| 原 PHASE4（现 PHASE6）内容扩展 T4-1～T4-4 | 并入第一批 | 排序由 T-C 定 |
| §4 业务四问 Q1（pem）/ Q2（投放主体）/ Q3（历史年限）/ Q4（holdback） | **全部已答** | 见决策记录 Q8/Q9/Q11/Q10 |

### ⚠️ 任务板订正记录（2026-09-13）

`board_get` 实测任务板曾是 **PHASE4 的 6 张卡**（2026-09-12 创建），其中 F1/F5 标完成、F2/F3/F4/F6 未标 —— 而 PHASE4-A 早已完成，之后还做了 PHASE5 的 X/Y/Z 三段（推送至 `9749583`）。**任务板落后仓库两个阶段**，已订正（revision 2 → 6）：PHASE4 五张卡闭合并附证据指针；新建 PHASE6 主卡 + 18 张子卡。

---

## 附：本文件引用的三个调查报告

| 文件 | 内容 | 规模 |
|---|---|---|
| `_survey_card_inventory.md` | 146 张精选卡全量清点 | 32 KB |
| `_survey_org_model.md` | 需求侧全量抽取（含 A/B/C 判定与逐条理由、16 项一致性核验） | 119 KB |
| `_survey_product_form.md` | 产品形态与接入缝（全部现场探活，含 R1–R10 风险） | 57 KB |
