# PHASE6 工具详解与缺陷史（CLAUDE.md 搬出归档）

> **为什么有这个文件**：2026-09-13 工作区 instruction 预算（65536 B，harness 的 `maxBytes`）被撑爆，
> 症状是 **`CLAUDE.md` 被静默截断 + `AGENTS.md` 与 `~/.dsh/AGENTS.md` 整份没进上下文**
> （实测 marker：`omitted ~/.dsh/AGENTS.md, AGENTS.md; truncated CLAUDE.md from 66175 to 65244 bytes`）。
> 于是把 `CLAUDE.md → Key Files` 里四条**最长的行**搬到这里。
>
> **搬出方式：逐字节复制，一个字符都没改**（同 PHASE3 / PHASE4-A 两次先例）。
> `CLAUDE.md` 侧只留一段摘要 + 指向本文件的指针。
> 门禁 `check_instruction_budget.py` 现在就守着这条线 —— 再撑爆会**判红**，不再静默丢。
>
> 复现搬出前的原状：`git show <搬出前的提交>:CLAUDE.md`。

## `check_contracts.py` + `build_contract_workpack.py` —— 契约层判据与作业包

> 以下为 `CLAUDE.md` 该行的**逐字原文**（含全部 ⚠️ 与实测数字）。

| 文件 | 用途 |
|------|------|
| `check_contracts.py` + `build_contract_workpack.py` | **契约层的门禁与作业包（PHASE6 F6/S1）**。`check_contracts.py` 判据 **J1–J13**（`--file` / `--all` / `--selftest` **123/123 用例 · 含 27 份变异样本 · 变异测试 21/21 抓住**）；退出码 **0 全过 / 1 判红 / 2 输入没拿到（≠ 通过）/ 3 门禁内部错误（≠ 判红）**。J13 是 **v2 正面义务**：§3（A）/§1（B）必须给取值、缺口必须带处置、B §2 每条不可达必须带 `→ 替代路径：`；**v1 免检但份数与名单是一等输出**。⚠️ **S1 期间由撰写人撞出 6 条门禁缺陷（#17–#22，见台账）**：两条假红（#17 五维标签子串劫持、#19 J3 误杀禁令句）、一条**假绿**（#18「取值来源」行冒充取值）、一条退出码不可分（#21）—— 四条已修并各配能打红的用例。⚠️ **#26/#27（2026-09-13，续批开工铺骨架时撞出）**：全集原先按 `rglob("CTR-*.md")` 收，把 `contracts/v2/` 里 3 份 F7 试点样本算成 139 的成员 ⇒ `J10 计数超界` **只打印不计退出码**（`🔴` 下一行就是「✅ 全部判据通过」，exit 0），`--check` 打出**核对率 102.2%** 也照样通过。已修：**正式位置只有 `contracts/A` 与 `contracts/B`**，别处的 `CTR-*.md` 单独报「样本/存档（不计入 139）」；超界与「同一责任两份正式契约」都进退出码；`cov > 1.0` 判红。**教训：判据只写单边（只警告 <90%）等于没有判据。** `build_contract_workpack.py` 生成**撰写作业包**（批次 + 材料摘要 + 逐份的格/理由/卡候选/status），`--check` **2042/2042**、`--selftest` **16 用例**；`--pending` 只装**待撰写**的（**首归 FLOW** 口径：未撰写 **且** 不属于更小 FLOW 的批次 —— 契约按责任建，8 条 FLOW 批次规模相加 = **397** 是**覆盖**不是**划分**），`--audit-pending` 跑恒等式 **Σ pending = 139 − 已撰写**（2026-09-13 实测 85）。⚠️ **「契约进提示词能提升产出」已被两轮证伪**（F6 中位 3 vs 对照 19.5；F7 削到只剩取值仍只有单卡臂的 47%）⇒ 契约层是**人的标定物 + 消费口闸门条件（S12）**，不是载荷。⚠️ **D27/D28（2026-09-13 收口，`--all` 139 份 exit 0）**：D27 是**潜伏的假绿** —— 「邻句豁免」只认**词**不认**话题**，于是任意带「替换/改用」的**无关样板话**都能救活缺口句；实测最小复现＝B-040 §1 末插一行真缺口，邻句 `换卡时只替换方法实现，其余要求不变。` 含「替换」即发豁免 ⇒ exit=0。该样板在 **63/66** 份 B 契约的 §1 末句 ⇒ 尾部一格被自己的样板砍掉。修法：豁免**收紧到同一条目块**（与 §2 逐条配对同颗粒度），**不用样板话黑名单**（那是把这一句硬编码）。⚠️ **口径不许写成「已发现漏判」**：真语料里 B §1「缺口句自身无处置」**靠样板豁免的 = 0 条** —— **是潜伏的洞，不是已经在漏**。D28 是修 D27 时**自己造出来的假红**：6 份契约被并发撰写人改成**两列**、缺口与处置**合并写进取值格**，而原判据是**按列**判 ⇒ 8 格已履行义务却被判红。改为**按整行**判、但先摘掉陈述缺口的句子（缺口不能自己救自己）。**教训**：`assert`/判据的颗粒度（列 vs 行 vs 块）一变，同一份语料上的判决就会翻面 —— 而两次都是**假**的那一面。 |
## `check_material_citations.py` —— 材料引文核验器

> 以下为 `CLAUDE.md` 该行的**逐字原文**（含全部 ⚠️ 与实测数字）。

| 文件 | 用途 |
|------|------|
| `check_material_citations.py` | **材料引文核验器（PHASE6 S1 收口期，2026-09-13）**：契约里凡声称「材料『X』」的 X，必须真的在材料里。**实测 139 份有 125 条材料引文 / 48 个词，其中 11 个在材料里查无实据。** ⚠️ **污染源是我们自己的 v2 范本**：`contracts/v2/B/CTR-B-023` 第 31 行把自造词「季度经营策略」写成「材料已定名的经营节奏」，范本是被设计成会被抄的东西 ⇒ 两天长成 **108 处 / 67 份**（`git log -S` 指向 `79d4be0` F7）。**同一形态的第二种**：`build_contract_workpack.py` 把**我们自己综述**（`reports/_survey_org_model.md` §F.3）的判定标成「**材料 §F.3 原文**」，139 份作业包全带着它发下去 —— 已改为标注「二手来源，不是材料原文」并重生成（`--check` 1568/1568）。**三种判定必须分开**（`attrib` 核验 / `non_attrib` 材料只是普通名词 / `unclassified` 不猜⇒**exit 3**）、**两种罪必须分开**（`missing` 凭空 vs `altered` 声称原文却改字，后者会**回捞材料原句**打印）。⚠️ **它自己先后被修掉 4 个缺陷**：①限定语窗口不看左邻 ⇒ `外发材料…只能写「X」`/`原材料成本冲击` 造成约 20 条假红（与 #17/D10 同族）；②一行多引号时 head 片段串行；③**`str.translate({c: None})` 拿 dict 表是按 ordinal 查键的，字符键一个都命不中** ⇒ markdown 强调符一个都没删、7 个逐字相同的引文被判「改了字」；④`context_of` 只给针去标点没给草垛去 ⇒ 原句回捞恒为空。另修一处**假绿风险**：材料目录**自己是个 git 仓库**，首版把 `.git/` 读进了语料（历史 blob 里的词会被判「有据」）。纪律同仓库先例：**活体探针每次跑**（不通⇒exit 3）、**材料读不到⇒exit 2 不是 0**、**`non_attrib` 的份数与名单是一等输出**。`--selftest` 11/11。✅ **W3 已落地（2026-09-13）**：它现在是 `run_phase6_gates.py` 里的**独立门禁 L4e**（**不与 J1–J13 同号** —— 引用纪律是 S1 收口后才立的），并配**端到端**判据（`subprocess` 跑真 CLI，不 import 库函数）：注入 `材料「季度经营策略」`⇒**exit 1** · 干净夹具⇒**exit 0** · 材料根不存在⇒**exit 2**；**反向控制 3/3**（`if bad:`→`if False:` 恒真放行 / →`if True:` 恒红 / 材料根分支 `return 2`→`return 0` 各由对应用例抓住）。⚠️ **数字随语料变过**：初测 **108 处 / 67 份**，同质化整改后重测为 **30 处 / 24 份**（受影响文件共 **32 份 / 11 个词**）—— **是契约变了不是判据变了**，两套数并列。⚠️ 污染源分**四类**（不是一类）：①范本种子 `季度经营策略`（材料真词是「月度经营复盘」）；②**二手来源被标成材料原文**（`§F.3`/`§F.5` **材料里根本没有这两个章节号**，那是我方综述 `_survey_org_model.md` 的章节）；③改写后仍称「原文/原话」；④本项目决策（`holdback`＝决策 Q10）被标成材料。⚠️ **模板是干净的**（`TEMPLATE-A/B` 实测无这些词），种子只在冻结的 `contracts/v2/` 样本里 —— **冻结不等于无害：被设计成会被抄的东西，留在原地就会继续被抄**。逐份改标工单见 `reports/PHASE6-F8-S1-材料引文核验.md` |
## `paper2skills-research/scripts/run_phase6_gates.py` —— PHASE6 验收面

> 以下为 `CLAUDE.md` 该行的**逐字原文**（含全部 ⚠️ 与实测数字）。

| 文件 | 用途 |
|------|------|
| `paper2skills-research/scripts/run_phase6_gates.py` | **PHASE6 验收面（2026-09-13）**：**一条命令跑完 22 条门禁**，也是本阶段第一次有「整体状态」这个读数 —— 此前 11 个门禁脚本分散在三种命令形态里，每个数字只活在产它的那份报告里（CLAUDE.md 记着「三个门禁必须分开报」，而分开报的前提是**有一个地方能把它们放在一起报**）。三条硬纪律：① **退出码不许合并成一个分数**，逐门禁给码、汇总只给各类几条，危险性排序 **3 > 2 > 1 > 0**（「没测到」比「测了是红的」更危险：红会有人去修，没测到没人知道）；② **一个门禁都没跑到 = exit 3，不是 0**（同 `scan_secrets.py`：扫到 0 个文件判失败）；③ **豁免必须可见、带到期条件**，且**转绿时提示「请取消豁免」**（永久豁免＝台账 #5 那种腐烂）。`--only` / `--fast` / `--list` / `--json-out`。`--selftest` **11/11**（含 ④–⑧ 用假门禁验证退出码**传播**：判红⇒1、跑不起来⇒**2 不是 0**、混合⇒取最危、空列表⇒**3**）；`--mutate` **3/3** 把 `check_material_citations.py` 改坏，验证端到端用例有劲。⚠️ **它自己被抓出两个缺陷**（都是 `--mutate` 干的，这是本文件最该留的部分）：首版 `cli()` 硬编码了原文件路径 ⇒ **变异体从没被跑过**，而读数看起来像「用例是摆设」；首版用裸 `    if bad:` 做变异锚点，而该文本在文件里出现两次（打印分支在前、退出码分支在后），`replace(...,1)` 只改到打印那处 ⇒ 变异没施上力。**先证明变异改变了真实取值，再谈判据有没有劲**（同 S4 缺陷⑥）。另有一条 `... or True` 的恒真断言被改成真断言（本仓库第 4 次抓到这一形态）。**实测读数（2026-09-13 主控复跑）**：**22 条 ✅22 · 🔴0 · ❓0 · 💥0，集合 exit=0** —— 本阶段第一次全绿（19 条那次是 ✅16 · 🔴3：L4e 材料引文待清 · L5a 已豁免 · L5b S13 施工中）。⚠️ **全绿本身不算数**：它是**并发写入者（S13 写 `l3_*`、S10 写 `venue_tier`，同一批 146 个文件）没有互相抹掉**这一结论的旁证之一，主控另外**直接扫了 146 份 frontmatter** 实测 **146/146 两者都在** —— 门禁说绿、独立仪器也说绿，才算数 |
## `paper2skills-research/scripts/check_contract_dedup.py` —— 跨契约同质化机检

> 以下为 `CLAUDE.md` 该行的**逐字原文**（含全部 ⚠️ 与实测数字）。

| 文件 | 用途 |
|------|------|
| `paper2skills-research/scripts/check_contract_dedup.py` | **跨契约同质化机检（PHASE6 S1 收口期，2026-09-13）**：判据 D1（A §2 / B §5 五维表**缺值处置列**跨契约**同维**重复）/ D2（B §2 `→ 替代路径：` 行重复）/ D3（正文长行重复，**默认关闭**）/ D4（覆盖率，含「某判据开着却 0 条可比条目」⇒**exit 2**）/ D5（**每次运行**的活体探针：正探针≥n 抓到、反探针 n−1 不误报、12 条专名遮罩必须活着 ⇒ 死了 **exit 3**）。算法是 n 字滑窗纯字符串比较，**无模糊/语义相似度**；另有一份**不复用检测器任何函数**的暴力实现独立复核 D1，逐维数字一致。**整改前后（同一冻结快照）**：D1 **2113 对 / 涉事 291 格 / 125 份 → 0 对**；D2 **538 对 / 55 份 → 0 对**；整改 **135 份 / 483 格**（15 位并行撰写人自报 448 格），改动的 135 份 `check_contracts.py --file` 全 exit 0、全库 `--all` exit 0。`--selftest` **27 用例 · 7 篡改样本 · 检测器变异 11/11**（含反向控制：两份措辞不同但都合格的处置格必须判绿）。⚠️ **整改过程中被撰写人撞出一条假绿通路**：首版归一化只去空白 ⇒ 把 `替换` 写成 `**替换**` 或换全角/半角标点就能把 18 字窗口切成两段、检测器看不见；补 NFKC + 去 `*` 后 **D1 当场 1554 → 2074（+520 对原先靠加粗藏起来的）**。⚠️ **9 条带空格的专名遮罩是死模式**（`normalize()` 先去空白、`MASK_RE` 后匹配，两边不同尺）：硬证据是检测器真吐出 `,相关动作停在ActionIntent`；已修并挂进 D5 每次运行验活。⚠️ **n=18 不是数据选出来的阈值**（曲线 12→6354 / 14→4082 / 16→2985 / **18→2113** / 20→1591 / 24→924 / 28→490 / 40→75，**平滑无天然拐点**），唯一硬依据是它与规范/现场实测同尺 —— **如实登记，不许写成「由数据确定」**。⚠️ **一条独立的、全语料级欠账**：D3 默认关闭时正文全扫 n_body=30 给出 **10236 条 / 833 种重复文本 / 139 份全覆盖**，剔除模板原句后仍有 **6934 条**（= D1+D2 的三倍），主体是**模板与规范要求逐字写的句子**与**材料开放事实 O1 的复述** ⇒ 改它等于违反模板义务，**已单开工单** |
