# PHASE6 · 门禁缺陷台账与验证声明锚点（从 CLAUDE.md 搬出）

> **为什么这份文件存在**：2026-09-12/13 两天里，门禁与体检工具被测出并修掉了
> **14 个自身缺陷 + 1 个新增检查（C10）**，逐条记录在 CLAUDE.md 里。
> 到 2026-09-13 收工时 CLAUDE.md 已达 **73,104 字节**，超出 workspace instruction
> 预算（65,536）约 8 KB —— 即**每次会话都有 8 KB 指令被静默丢掉**，
> 而被丢掉的恰好是「Skill Card Format / Code Standards / Domain Mapping /
> Quality Standards」这一段，**正是 F5/S1 出卡时要照着做的规格**。
>
> 故按本仓库既有先例（`PHASE3-交付归档.md` 同因搬出、内容未改），
> 把这两块**台账型**内容搬到这里；CLAUDE.md 只留**索引 + 状态**。
>
> **搬出的是叙述，不是判据**：每条缺陷的编号、一句话后果、状态仍留在 CLAUDE.md 的索引表里，
> 所以 `#11`/`#12`/`C10` 这类引用照样指得到；要细节才来这里。
>
> 日期：2026-09-13 · 搬运方式：逐字节复制，未改写

---

## 一、门禁自身的 bug（27 条）

| # | bug | 后果 | 锁定方式 |
|---|-----|------|----------|
| 1 | `looks_like_python` 启发式过宽(把含 `if`/`for` 的**文档**判为代码) | 3 个假阳性,报告出现不存在的「语法错误」 | — |
| 2 | 围栏正则不支持**变长反引号**(只认 3 个) | 嵌套块被截断;4 反引号块被整个漏掉(表现为"该卡无代码") | — |
| 3 | `extract_all_python` 回退分支误用 `m.group(1)` | 同上 | — |
| 4 | 索引阶段 `if "nlp_voc" in p.parts: continue` | **9 张卡被误判 `ORPHAN_DEP`** 并列入「必须修卡片」,而模块其实都在仓库里 | K1 `--selftest` 四用例 |
| 5 | **L3 探针不注册 `sys.modules`** | CPython 3.14 下凡「`from __future__ import annotations` + `@dataclass`」的模块崩在 `dataclasses._is_type`,被记成卡片的 `import 时崩溃` —— **影响全仓库所有 dataclass 卡** | K1 `--selftest` 端到端用例 |
| 6 | **`gate_check` 找不到 evidence.md** | 只在卡片同目录找,而约定是 `papers/<域>/<p2s-id>/evidence.md` → **PHASE3 全部新卡的 evidence.md 对 G2 毫无作用**,漏洞 #3 的加固成了空转 | 见下 |
| 7 | **`quote_check` 把「没有引用块」算作「通过」** | `NO_QUOTES` 的卡被并进「N 通过」汇总 —— 「没东西可查」≠「出处为真」,又一个假绿灯 | 汇总行现单列 |
| 8 | **`repo_health --json-out` 因父目录不存在而崩溃** ✅**2026-09-13 已修**(X3a) | 体检报告**全部正常打印完**、结论「✅ 无 CRITICAL」,最后一步写 JSON 时 `FileNotFoundError` → **退出码 1**。后果:① PHASE5 T5-4「周日体检 + 趋势追踪」拿不到可比较的历史文件;② 这个失败**看起来像体检失败**,而体检其实是过的 —— 与 #6 同类的「静默退回/假失败」 | **待修**(`json_out.parent.mkdir(parents=True, exist_ok=True)`) |
| 9 | **`repo_health` C2 不认 `author-practice` 口径** ✅**2026-09-13 已修**(X3b) | C2 报「有 frontmatter 但缺 v2 必填字段 **126 张**」,其中 **48 张是 `evidence_basis: author-practice` 卡** —— 它们**按设计就不该有**来源字段(漏洞 #8/#10 的三类口径)。真实欠账:无 frontmatter **2 张**;有来源卡中缺 `venue` **65** / `venue_tier` **73** / `evidence_grade` **77**(2026-09-13 复测)。**口径未跟上口径**,与漏洞 #11(参考区词汇表)同源 | ✅**2026-09-13 已修**(X3b);豁免后仍需用构造样本自证会抓真缺陷 |
| 10 | **`quote_check` 的 `paper_version:`(`declared`)分支永远拿不到 `MATCH`** ✅**2026-09-13 已修** | `version_check()` 原写 `if kind == "explicit" and claim == vlabel` —— 于是 frontmatter `paper_version:` 这条路径**永远拿不到 MATCH**,直落 `else` 被贴上「卡片声称**正式发表版**」的标签判 `MISMATCH`:**版本一致却报黄灯**。判据与自身 docstring(把 `declared` 称为「**最强**」的声明强度)**自相矛盾**。**最要命的是门禁印给用户的处置建议原文就是「补 `paper_version:`」** —— 即**门禁在推荐一条会把门禁自己搞出假黄灯的路径**。修法:`declared` 与 `explicit` 同权。**用例 6k/6l + 一条消息断言**;**变异 3/3 抓住且各由对应断言抓住**(只测 6k 则「恒 MATCH」也能全绿;只断 verdict 则消息退回也能全绿) |
| 11 | ~~**`verify_skill_code.py --card` 对「无代码块」的退出码是 `0`**~~ ✅**2026-09-13 实测:此条不成立,早已修好** | **订正(并非待修)**:实测 `--card` 对无代码块卡返回 **`exit=2`**,不是 0(`verify_skill_code.py` 第 911 行 `return 2`)。本条是**文档过期**。由 PHASE6 F1 主动撞工具时发现。**教训**:「登记待修」这句话本身也会腐烂 —— 登记时必须写清**登记日期与被测版本**,否则下一轮会去修一个不存在的东西(与 #13 同源) |
| 12 | **`gate_check --card` 不带 `--k1` 时全库每张卡都红 `[G1-NO-EVIDENCE]`** ✅**2026-09-13 已修(PHASE6 F1)** | 脚本**没有默认 K1 产物路径**,于是「按 CLAUDE.md 的三条命令逐条跑」的人会看到 G1 全红,而那是**调用方式陷阱、不是卡片缺陷**。**由两个子代理各自独立撞上并都选择如实报告**(而不是去改卡)—— 说明这条已经浪费过两轮排查时间。**实测根因比记录更重**:`k1_l5.json` 被 `.gitignore:93` 忽略,故**新克隆的仓库必然没有它**,该陷阱对新人是 100% 命中。修法:`--k1` 默认指向 `paper2skills-research/data/verification/k1_l5.json`;三态诊断 `diagnose_k1_source()`(OK / EXPLICIT_MISSING=路径写错 / DEFAULT_MISSING=**不是卡片缺陷**+生成命令)。⚠️ **只改诊断,不改判定**:三态里没有任何一态把 G1 变绿;凭证缺失时 G1 仍判红。**未放宽同步门禁**已实测:`sync.py` 本就显式传 `--k1`,两场景行为逐项未变。selftest **用例 23**;变异 **4/4 抓住且各由对应断言抓住** |
| 13 | **`gate_check` 的 `relative_to(REPO_ROOT)` 对仓库外路径抛 `ValueError`** ✅**2026-09-13 已修** | 生产中证据都在库内故不咬人,但一次「把底本放到临时目录」的自检就把它撞崩了。现按 `repo_health.rel()` 的既有口径 try/except 退回绝对路径 —— **崩掉比打印一个绝对路径糟得多**,与 #6/#8 同属「静默退回/假失败」家族。⚠️ **2026-09-13 F3 实测:同族第二次出现** —— 新写的 `build_gap_ledger.py` 在 `--out-json /tmp/...` 时**又**踩了同一个 `relative_to(REPO)`。本次不再只修一处,而是固化成 `_rel()` 函数 + 自检里的**回归守卫**(3 个探针:库内/库外/系统路径)。**一条修好的 bug 会在新代码里以同一形态复发** —— 这类「同族复发」应当被记成家族,而不是逐条记 |
| 14 | **`verify_skill_code.py --level 2` 的「误导性绿灯」** ✅**2026-09-13 已修(PHASE6 F1)** | 实测 `--all --level 2`:**101 个单元 / ✅ PASS 0 / ❌ FAIL 0 / ⚪ 未执行 101 / exit 0** —— 一次**什么都没验证**的扫描,用退出码报了「没问题」。根因:判定链 `if max_level < 3: verdict = "UNVERIFIED"`,故 L1/L2 下**没有任何单元能得到 PASS**,而退出码只看 `FAIL==0 and ORPHAN==0`。与 #11 同族(**没东西可查 ≠ 查过了没问题**),且**此前未被任何门禁报警**。修法:**只补口径提示,不动退出码** —— `--level 2` 作为「秒级语法门禁」是合法用法,改成非 0 会让每次 pre-commit 都红,从而逼人绕过这道门(**用错的方式修对的问题**)。现输出显式写明「exit 0 只代表没有失败,不代表 K1 通过」。验收:L2 出现该提示、L3/L5 零命中、三个级别的退出码均未变 |
| 15 | **F6 判分汇总脚本 `l7c-verdict.mjs` 的 Δ 符号按「X/Y 槽位」取分，而非按臂名** ✅**2026-09-13 当场修** | 盲配对为了抵消位置偏差，X/Y 的位置**由用例奇偶决定** —— 于是「按槽位算 Δ」会让**半数用例的符号反过来**。首版就是这么写的，它把 `control − contract` 当成 `contract − control`，**直接算出一个假通过**：主判据 holdout Δ = **+2.4（应为 −0.8）**、K−T = **+2.1（应为 −1.3）**。**抓出它的是同一个数在两种口径下不自洽** —— 组内成对 Δ 的均值（+1.6 / +2.1 / 0）与按臂汇总的三维均分（契约 11.40 < 对照 11.80 < 单卡 12.15）**符号相反**。修法：按 `x_arm`/`y_arm` 映射到对比定义里的 a/b 臂；并给该脚本加 `--selftest`（**4 组构造样本，覆盖奇偶两种槽位 × 胜/负两个方向**）锁死符号约定。**这类 bug 比 #12/#14 危险一级**：那两条是把红的看成绿的，本条是**把负结果直接翻成正结果**，而验收判据正是一个 Δ 的符号。**教训：凡「验收 = 某个差值的符号」的脚本，必须先证明它对正负两个方向都判得对。** |
| 16 | **F7 的 D3 度量（缺口处置率）`GAP_RE` 有 21% 假阳性** ⚠️**已登记未修** | D3 用 `GAP_RE` 找「缺口句」，而表里的 `不产` 会命中治理句 **「不产生 Outbox」**、`缺(失\|口\|少)` 会命中名词性的 **「日志缺口」**。实测 40 份产出里判为缺口句 14 条，**其中 3 条是假缺口 = 21%**。按原口径处置率 5/14 = **0.357**；剔除假缺口后 5/11 = **0.455** —— **两种口径都低于 0.6 阈值，故 D3 的判定结论不受影响**，但它不是一个可信的度量。与漏洞 #18（`A100` 里的 `100` 洗白同名断言）**完全同型**：子串匹配把「词的一部分」当成了词。**S1 若要用缺口处置率做验收，必须先修 `GAP_RE` 并把假阳性样本加进变异表。** |

> 📌 **本表自身的一条教训(2026-09-13,PHASE6 F1)**:修 #11/#12 时,一次 `edit` 的 `old_string`
> 把 **#13 那一行连带吞掉**,`new_string` 里没写回来 —— 即**「修文档」的动作把另一条记录删了**。
> 由「改完立刻 grep 关键词」抓到(`relative_to(REPO_ROOT)` 命中数变 0)。
> **纪律:批量改表时,改完必须用每条受影响行的唯一关键词回查命中数**,
> 而不是只看新写进去的内容在不在。与「凭证清理操作打坏了抓该凭证的测试」同源。

**#5 的最小复现**(`from __future__ import annotations` 是触发条件 —— 它让所有注解变成字符串,
`dataclasses._is_type` 才会走 forward-ref 分支去查 `sys.modules[cls.__module__]`):
```python
spec = u.spec_from_file_location('unit', 'unit.py')
m = u.module_from_spec(spec)
spec.loader.exec_module(m)        # ❌ AttributeError: 'NoneType' object has no attribute '__dict__'
sys.modules['unit'] = m           # ✅ 补这一行即通过
```
三个子代理各自撞上并**各自改卡片绕开**(去掉 future import)。这说明:
### #16 附带说清的一件事：判据判红，也可能是判据不准

本台账此前 16 条大多是「把红的看成绿的」。**#16 是反方向**：判据把**正常的写作**判成了缺口。
两条要分开记，因为补救动作相反 —— 前者要收紧判据，后者要修度量本身。
F7 报告里另记了一条**同族但更严重**的：Q1 判据（Δ 的 95% 区间下界 ≥ −0.7）在 holdout 只有 4 对时，
区间半宽 ≈ 4.8，等价于要求 Δ̄ ≥ +4.1 —— **该判据事实上只能判红、不能判绿**。
它这次判红是对的，但不是有价值的判红。**「判据标定错」与「资产有问题」必须在报告里分列。**

**门禁缺陷会以「大家都学会绕路」的形式被消化掉,而不是被报告** —— 所以 `--selftest` 比文档更可靠。

**#6 的定位过程本身是个教训**:第一版修复按 registry 的 `paper_id` 匹配,全部落空且**静默退回旧行为**。
根因是本仓库一个命名坑:**卡片 frontmatter 的 `paper_id` 是 arXiv ID(如 `2608.25277`),
registry 的 `paper_id` 是项目内 ID(如 `p2s-2026-0018`)** —— 同名字段、不同语义。
现按 `registry.paper_id` / `identifiers.arxiv` / `outputs.skill_card` 三条线索依次认领。
> **修门禁时必须验证「修复真的生效」,而不是「门禁还绿着」。** 第一版修复后门禁照样绿,
> 因为它压根没找到文件 —— 绿得和没修一样。

### #17–#22 与两条结构性发现（S1 第一批，2026-09-13）

> 这一批的特殊之处：**缺陷是 21 位撰写人在真活里撞出来的**（54 份契约、每人各自跑 `--file`），
> 而不是我坐在那里想出来的。三条被**两位以上撰写人独立报告** ⇒ 它们是结构性的，不是个案。

| # | 缺陷 | 方向 | 症状（实测） | 状态 |
|---|---|---|---|---|
| **#17** | `data_dims()` / `dim_cells()` 用 `re.search` 取标签**首次子串出现** | 🔴 **假红** | ① 的处置格里写「日**粒度**不足」⇒ ② 的取值被读成空串，J5 报「数据要求缺维：粒度」。撰写人只能**改措辞**绕开（两位各自撞到） | ✅ 已修：标签**锚定表行行首**（`^\|\s*[①-⑤]?\s*标签\|`）+ 1 正向 1 反向 selftest |
| **#18** | J13 的 `has_value()` 认「取值**来源**」行 | 🔴 **假绿** | 把 §3 的取值行全删、只留 `- 取值来源：…（近 2 个完整年度）`（同行有数字）⇒ **exit=0**。探针实测（三位报告、我复现） | ✅ 已修：标签后紧跟「来源」的行不计 + 1 份变异样本 |
| **#19** | J3 对 R/D 格**逐行字符串匹配**「算法\|模型」 | 🔴 双向 | 假红：「只放判据、**不接入**算法」（禁令句）、「**模型**候选 + 待签」（业务措辞）、引材料原文「模型不得选参」全被误杀（B-004 首轮因此判红，作者把「模型候选」改成「预筛候选」—— **正是 #5 记的消化方式**）。假绿：反向收紧时又会漏掉「上挂分类模型」 | ✅ 已修：要求**接入语义**（算法/模型 与 接入/挂载/上挂 同句相近）+ 否定词白名单；**保留原变异样本并新增反向探针**（首版修完把原变异打漏了、`--selftest` 37/38，由撰写人当场报出） |
| **#20** | J6 只在五维 ① 上判「不可得」 | 🟡 缺口无账 | ②–⑤ 的缺口**没有机器记账入口**：规范 §4 说「任一维举不出替代 ⇒ 数据待盘」，机器只查 ①。撰写人三份的缺口全落在 ②④⑤ ⇒ 这条缝只靠人守 | 📌 登记不修（要改字段设计） |
| **#21** | **门禁自己崩了与判红退出码同为 1** | 🔴 假红 | 脚本被并发改到一半时 `NameError: _dim_row`，退出码 1；同事把仪器故障误读成「我写错了」（两位报告） | ✅ 已修：四态退出码 **0 全过 / 1 判红 / 2 输入没拿到 / 3 门禁内部错误**；`--file` 指向不存在的路径也归 2 |
| **#22** | B §2 的「替代路径」是**整段计数**而非逐条配对 | 🟡 偏松 | 一条写两个 `→ 替代路径：` 可给另一条没写的顶账；`marks >= len(items)` 只看总数 | 📌 登记不修（逐条配对要按条目切块，改动面大） |

### #23–#25：S12 闸门自己撞出来的三条（2026-09-13）

> 这一批的共同点：**三条都不是别人撞出来的，是新仪器自己在真数据上跑出来的**。
> 这也说明一件事 —— 「新写一个门禁」这个动作本身就会制造下一批缺陷，
> 所以**新门禁的第一个 selftest 用例应该是「把门禁自己改坏，看它报不报」**。

| # | 缺陷 | 方向 | 症状（实测） | 状态 |
|---|---|---|---|---|
| **#23** | `readPresetSubsets()` 只读 `manifest.skills.subset` | 🔴 **假绿** | preset manifest 的 skill-subset 实际在 **`x_lute.skills.subset`**（平台扩展命名空间），顶层没有 `skills`。首版读不到 ⇒ **50 个岗位全部读空**，而闸门照样打出「已挂契约 **0** / 未接线 **1338**」这张**看起来很正常的账**。若不是先手工算过一遍，这就是一个会被写进报告的假数字 | ✅ 已修：两个已知落点都试；**两个都拿不到 ⇒ exit 2**（不是「零接线」）；变异 9/9 双向钉住（`x_lute` 能读到 / 落在未知键下必须 missing） |
| **#24** | `generate.mjs` **整文件重写** `agent.cordis.yml` | 🔴 **删别人的数据** | `agt-033` 里按 ADR-0061 手插的 `product-kol-hunter` 行（本机装配，不进仓库出货物）**被静默删掉** —— 页面表现就是 AGENTS.md 记的那条「产品卡点了打不开」。文件里其实有注释预告了这件事（「重写本文件时需重新插入本行」）：**写了警告不等于有了判据** | ✅ 已修 + 已恢复：非生成器产出的行块**逐块带过、插回原位（前驱行 id 决定位置）**、末尾点名；位置退化也必须报出来。8 条用例 / 变异 **3/3**。恢复后 50 个 `agent.cordis.yml` 与事故前**逐字节一致** |
| **#25** | 新门禁自己的 selftest 里有**三处摆设** | 🟡 假仪表 | 三轮变异测试逐轮暴露：首轮 **8/11** 抓住 ⇒ 补 3 条；第二轮 **10/11** ⇒ 再补；第三轮 **13/13**。漏网的分别是 `main()` 里的「preset 读不到就 exit 2」守卫、「契约目录没有 A/B 就 exit 2」守卫、「unresolvable 必须判红」—— **把守卫改成 `if (false)`，selftest 照样全绿** | ✅ 已修：改用**子进程跑真 CLI + 端到端夹具**，直接断言退出码；并加一条**反向控制**「干净夹具必须 exit 0」，防「什么输入都判红」的假仪表 |


### #26–#27：S1 续批开工前撞出来的两条（2026-09-13）

> 这两条**不是撰写人报的，也不是我想出来的** —— 它们是我为了给续批生成作业包、把 139 份骨架
> 一次性铺到磁盘上时，**被「文件总数第一次超过 139」这个动作翻出来的**。
> 在此之前它们已经存在很久，一直「看起来正常」。
>
> 📌 教训（比修复更重要）：**一个判据的「正常」可能只是分母还没长大。**
> 覆盖率 > 100% 这种自相矛盾的读数，本来应该是第一眼就被拦下的；而它之所以能潜伏，
> 是因为脚本只写了「< 90% 要警告」这一侧 —— **判据只写了单边**。

| # | 缺陷 | 方向 | 症状（实测） | 状态 |
|---|---|---|---|---|
| **#26** | `check_contracts.py` 的 J10「计数超界」**只打印、不计入退出码** | 🔴 **假绿** | `contracts/v2/B/` 里 3 份 F7 试点样本被 `rglob` 当成了 139 的成员 ⇒ 输出 **`🔴 J10 计数超界：A 73/73、B 69/66`**，**紧接着下一行就是「✅ 全部判据通过」、exit 0**。任何用退出码判绿的消费者（撰写人 / `sync.py` / CI）都会把超界当成通过。同一个文件里还有一次同型：`--check` 打出「核对率 **102.2%**」也照样过 | ✅ 已修：① 全集定义写死为 `contracts/A` + `contracts/B`，`A/ B/` 之外的 `CTR-*.md` 单独报一行「样本/存档（**不计入 139**）」；② 超界与「同一条责任两份正式契约」都进 `problems`（影响退出码）；③ 判据抽成纯函数 `count_problems()`；④ **反同质化**：两条判据各配一份**只打其中一条**的样本（首版共用一份反向控制 ⇒ 两条互相顶账，删掉任一条自检都照样全绿 —— 实测确认） |
| **#27** | `build_contracts.py --check` 的覆盖率**只写了下界** | 🔴 **假绿** | `139 份契约的核对率 = 已核对 / 139`，但分子来自 `rglob("CTR-*.md")`（含 `v2/`）⇒ 铺完骨架后读数变成 **142/139 = 102.2%**，脚本对 > 100% **一言不发**（它只在 < 90% 时警告）。**一个 >100% 的覆盖率会把真实缺口平均掉**，而它的成因恰好与 CLAUDE.md 记过的「v1 底本留原地会与 v2 同 id 重复入账」是同一条 | ✅ 已修：分子分母都收窄到 `A/ B/`；`cov > 1.0` 直接**判红**（带红因文案「分子里有不属于这 139 份的文件」）；变异实测：把收集退回 `rglob` ⇒ `--check` exit 1，改回 ⇒ exit 0 |

> **#26/#27 的同一条根因**：**「全集」是被默认出来的，不是被写下来的。**
> `rglob` 看起来比 `glob("A/*")+glob("B/*")` 更「全」，而这份资产里恰恰有**不该算进去的同类文件**
> （F6/F7 试点样本、v1 底本）。这与本仓库已有的三条教训同族：
> 判据只认一种字段名（#11）、判据只认一种路径（K1 `ORPHAN_DEP`）、
> 判据用错了信息源（铁律 1/2）—— **这一次是「判据用错了集合」。**


**两条结构性发现（都不是缺陷，但都会让下游判错）**

1. **规范里的「示例」被下游当成「材料事实」。** `业务规则字典` / `标签分配结果表` 这两个名字出自
   **F6 试点契约的自造**，经 `撰写规范.md §2`（作为「命名的数据系统」示例）与 S1 作业包
   （被我写进「材料词汇（不要自己造词）」）**两层传播**，最终被 6 份契约当作既有/待签发产物引用。
   对 `/Users/lute/project/AI组织变革/` 全文 grep：**0 命中**。
   ⇒ 处置：改规范（示例换成材料里能 grep 到的名字）+ 加规则「**待建/未签发**产物可以点名，
   但必须写「（尚未签发）」并给替换条件，**不得**当既有系统直出取值」。
   **教训：示例一旦写进规范就会被当成事实引用，所以示例本身也要能被核。**
2. **卡面行数与实物行数恒差 5。** 四位撰写人各自独立发现「9/9 张卡的 `implementation.py`
   卡面记 N 行、包内实测 N+5」。**根因已定位**：包内文件头有 **5 行溯源头**
   （`# dsh-paper2skills · 完整实现…` / 卡 id / 出处 / 核对 / 生成），第 6 行起才是卡面节选
   —— 文件自己就写着「核对：卡面节选 = 本文件第 6 行起」。
   ⇒ 不是缺陷，是**计行口径**；但任何用「行数」当实现体量证据的判据都必须先减 5。
   **同一现象被四个人独立报告 ⇒ 它是结构性的，不是巧合。**


---

## 二、验证声明的时间锚点（C10 + `verified_at`，PHASE6 F1）

**问题**:卡片 frontmatter 的 `verified_by` 是一句**没有时间的断言** ——
`verify_skill_code.py（K1 L5 PASS）+ quote_check.py（…VERBATIM）+ gate_check.py G2 passed`。
读者无法分辨这是**昨天验的**还是**半年前验的**,而两者含义完全不同。

**实测(建 C10 的当天现场测的)**:

| 事实 | 实测 |
|---|---|
| 有 `verified_by` 的卡 | **25 张** |
| 其中**有时间锚点**(`verified_at`) | **0 张**(该字段此前根本不存在) |
| 其中**按当前门禁产物已是红灯** | **9 张** |
| 这 9 张里**明确声称了「G2 passed」** | **7 张** ⇒ 声明与实测**矛盾** |
| 另 **2 张**点了 `gate_check.py` 但**没写判了哪个门禁** | 不可复核(单列 🟡,**不判矛盾**) |

⚠️ **那 9 张当初是真的** —— PHASE5 的 C6(引用编号 `[8]` 洗白数字)与
C9(`A100` 洗白 `100`)把 G2a 改严了,**卡片并不知道**。
⇒ **声明腐烂时,卡片自己不会说话。**

**处置**:25 张卡全部补 `verified_at: 2026-09-12` ——
日期**取自 git**(`git log -S"verified_by"` 的作者日期),
**不是**用当天回填。25 张的首次提交日同为 2026-09-12,逐个核对无冲突。
⚠️ **补一个没发生过的验证日期,正是本仓库反复封堵的那类假绿灯。**

`repo_health.py` 新增 **C10**,四条判据(可机械执行):

| 判据 | 罚 | 抓什么 |
|---|---|---|
| 有 `verified_by` 无 `verified_at` | 🟡 | 无锚点的断言(欠账,**不是撒谎**) |
| `verified_at` 晚于今天 / 早于该卡首次提交 | 🔴 | 锚点不实(验了一个还没发生/卡还不存在的验证) |
| 声明点名的门禁**当前实测为红** | 🔴 | 声明与测量矛盾 |
| 点名了 `gate_check` 但未写 G1/G2/G3 | 🟡 | 不可复核(**不得**判成矛盾) |

**⚠️ 两条反后门(都是实测逼出来的,不是预设的)**

1. **判据的适用范围不许被默认成全体。** 9 张矛盾的卡**全部只因 `G2-UNSOURCED-METRIC`(G2a)变红**,
   而它们的 `quote_check` 逐字核验至今仍是真的 —— 若把「G2 聚合红」直接算成
   「引文声明被推翻」,就会报 **9 条假矛盾**。故 `G2b` 只在**引文层自己的**红灯上被判
   (`G2-QUOTE-FABRICATED` / `G2-QUOTE-TRUNCATED` / `G2-NO-EVIDENCE-CHAIN`)。
   与漏洞 #11(判据只认一种字段名)同族。
2. **刻意不设任何人工状态字段**(如 `verified_stale: true`)——
   任何「由人写、能让检查闭嘴」的字段都是一个新后门(漏洞 #10 的教训)。
   **stale 与否由测量导出**;卡片只记录「断言是哪天作出的」这个事实。

**首跑结果**:`25 张有 verified_by:无锚点 0 / 锚点不实 0 / 声明与实测矛盾 7 / 点名工具但不可复核 2 / 一致 16`。
那 **7 张**正是 PHASE5 遗留的「甲类」清单的同一批卡 —— C10 让它们**自己从卡片里浮出来**,
不再依赖有人记得去看台账。

**定稿前先跑一次全量门禁对照**(改了 25 张卡的 frontmatter,必须证明门禁没被搅动):
G1 **62/62 不变**、G2 **16/16 不变**、G3 **67/67 不变**,**逐卡判定 0 张不一致** ——
新增字段对三道门禁**完全无影响**(它是给「声明」用的,不是给「断言」用的)。

**实现**:`c10_verified_claims(dates, gates, today)` 的日期表与门禁表**可注入** ——
否则「C10 真的会报警」无法被证明(造一个 git 仓库与时间机器都不现实)。
`--selftest` **新增 11 条用例**,**变异 6/6 抓住且各由对应用例抓住**。

> 📌 **开发中我自己连犯 3 次同族的错,三次都不是判据错而是「作用域/口径」错**:
> ① 去重用例复用了装着 `2099-01-01` 样本的 vault ⇒ `critical` 被它**合理地**抬起;
> ② `total` 写成五项相加 ⇒ 25+7 报成 32;③ 加了 `unspecific` 桶却漏进求和 ⇒
> 「朴素和」恰好等于去重值,用例当场报红。
> **共同结构:判据没错,我测的东西不是我想测的东西。** 与「变异测试台复制到 /tmp 后
> `REPO_ROOT` 解析错」同一族 —— 故每次「用例报红」都要先问**是判据坏了还是样本脏了**。

> 📌 **另一个副产品:C2 的基线数字是过期照片。** 本行原写「有来源卡缺 venue 66 / venue_tier 74 /
> evidence_grade 78」。实测 **65/73/77**。已用**X3b 当天的 C2 代码**跑今日卡复核:
> 同样报 77 ⇒ **是卡片变了,不是判据变了**(`1c9949d` 按铁律 1 给 `Skill-Auto-Skill-Synthesis`
> 补了 `venue: SIGIR 2026 (Industry Track)`,它**变完整了**)。风险 N6 又中一次。


---

## 附：K1 首轮暴露的缺陷（2026-09-12）

> 本节 2026-09-13 因 CLAUDE.md 逼近 workspace instruction 预算（S1 要往 Key Files 加行）
> **逐字节搬出**，内容未改；CLAUDE.md 留一行指针。

首轮报出「8 个语法错误」,**逐个人工核对后发现全部是 markdown 围栏结构问题,不是代码缺陷**——
这个区分很重要:用 LLM 阅读永远发现不了,只有执行才暴露。

| 缺陷类 | 数量 | 实例 | 修复 |
|--------|------|------|------|
| 伪代码被标成 ```python | 9 处 | `Skill-AB-Experimental-Design`(ASCII 框图)、`Skill-Memory-as-Action`(函数签名) | 改标 `pseudocode` / `yaml` |
| 代码块内含嵌套 ``` | 1 张 | `Skill-Argos-Agentic-Anomaly-Detection`(f-string 里嵌 markdown) | 外层改 **4 反引号**(Markdown 标准做法);此前该块被截成两半、后半段整个丢失 |
| 代码围栏**未闭合** | 2 张 | `Skill-Session-Based-Recommendation-SR-GNN`、`Skill-Knowledge-Graph-for-Skills-Management` | 在代码真实结束处补闭栏;此前 ④技能关联 等正文被当作 Python |
| 真代码笔误 | 1 处 | `self._ Aspects = None`(标识符中间多空格) | 改 `self._aspects` |

**~~仍待修(9 张)~~ —— 已于 2026-09-12 撤销,这 9 张不是卡片缺陷。**

原判定:9 张卡 `import` 了「仓库内**根本不存在**」的本地模块,判 `ORPHAN_DEP`,列入必须修清单。
**逐个人工 `ls` 核对后全部推翻** —— 这 9 个模块**都真实存在于仓库**:
`autotag_self_evolving`、`review_quality_scoring`、`nps_driver_analysis`、
`behavioral_intent_tree_parsing`、`crosslingual_semantic_alignment`、
`crosslingual_sentiment_transfer`、`dialogue_to_action_graph`、
`product_attribute_graph_parsing`、`voc_semantic_blueprint`
全部位于 `paper2skills-code/nlp_voc/<mod>/`(`07-NLP-VOC` 迁出后保留的代码模板镜像)。

**根因是门禁自己的一条 `continue`**:`repo_local_module_index()` 里
`if "nlp_voc" in p.parts: continue` —— 索引阶段就把镜像排除掉,于是分类器
只能把它归成 ORPHAN。判定文案写「模块不存在」,**而这个描述是假的**。

**为什么这个误判有代价**:`ORPHAN_DEP` 与「镜像不可在此运行」的**补救动作相反** ——
前者要改卡片,后者要改环境(或接受它不可验证)。混为一谈会把 9 张没有缺陷的卡
送进「必须修卡片」队列,而真正该做的是承认这批卡在本机无法验证。

**修法**:新增第四类判定 `MIGRATED_DEP`(🟠)。模块存在于 `paper2skills-code/nlp_voc/`
→ 判 MIGRATED,计入未验证分母但**不计入失败、也不算卡片缺陷**;
只有模块在仓库内**确实找不到**才判 `ORPHAN_DEP`。
`--selftest` 用四个用例锁定三类判定互斥可区分(样本名从索引现取,不写死 —— 写死会腐烂,
第一版就因写死 `causal_inference` 报了一次假失败)。

> **实测效果**:全量 L3 扫描 `ORPHAN_DEP 9 → 0`、`MIGRATED_DEP 0 → 9`,退出码从 1 变 0。
> 这是本项目第三次「提升来自修门禁而非改卡片」—— 与 46.2%→52.5%→54.8% 那次同源。
> 教训:**判某个东西「不存在」之前,先 `ls` 一次。**

---

## #28–#45:S1 续批撰写人集体撞出的 18 条(2026-09-13)

### 这一批的形状,比单条缺陷更重要

29 位契约撰写人**各自独立**撞出了同一批缺陷,而且编号互相冲突(两位都叫它「#28」)。
按本仓库的规矩,编号由台账认定 —— 下表是**归并去重后的规范编号**。

**一个必须写下来的观察**:这 18 条里 **14 条集中在 J13 与 J9 这两个判据**,
而它们判的正是「契约有没有给出本业务取值」——**v2 模板存在的全部理由**。
共同结构只有一句话:

> **义务的覆盖面比判据宽。** 规范要求「必须给取值 / 缺口必须带处置 / B 侧也必须不许对冲」,
> 判据只认其中一种写法。这与漏洞 #11/#14/#16/#17/#18 是**同一个结构**,
> 只是这次不是「某一种字段名」「某一种引号」,而是**某一种句式**。

### #28–#35:J13(「必须给本业务取值」)

| # | 缺陷 | 方向 | 症状(实测) | 状态 |
|---|---|---|---|---|
| **#28** | `has_value()` 的**取值标签不锚定行首** | 🔴 **假绿** | `VALUE_LABEL_RE.search(ln)` 是子串匹配 ⇒ 标签出现在行内**任何位置**都算。B 模板 §1 **按规范必须**列「不得移植」的卡面数字清单,而那份清单几乎必然是「阈值/分档/默认值 + 数字」⇒ **把 §1 的四条真取值行全部掏空、只留清单,仍 exit=0**。⚠️ 危险方向明确:它**奖励**把卡面数字抄进契约。四位撰写人各自独立撞出 | 🔧 修复中(D2):标签锚定到结构前缀之后 |
| **#29** | 数字可来自**格号** `FLOW-0x/STG-0y` | 🔴 **假绿** | 139 份契约的 §3/§1 **天然都写格号** ⇒ 这条会让 v2 核心义务静默失效。复现:`- 阈值：按 FLOW-02/STG-06 的接收门禁判定；本业务暂无独立取值,接入后替换。` ⇒ **exit=0**;**反向对照**:只把格号换成「该阶段接收门禁」⇒ **exit=1**。红与绿之别**只来自格号里的数字** | 🔧 修复中(D11) |
| **#30** | 数字可来自**跨段转指**与**条款号** | 🔴 **假绿** | `- 阈值：见上文的数据要求（近 2 个完整年度）` ⇒ **exit=0**(那个「2 个」来自 §2);`- 取值：见 §4 第 2 条` 同理 | 🔧 修复中(D4/D18) |
| **#31** | 五维「缺值处置」格只要求**含任意数字** | 🔴 **假绿** | `见 §2 第 3 条` / `暂无,详见上表 3 处` / `见 2026-09-13 的会议记录` 三者都过闸 —— 既没有替代取值也没有替换条件 | 🔧 修复中(D5) |
| **#32** | `GAP_TOKENS` 漏词 ⇒ **判据整条不触发** | 🔴 **假绿** | 缺口措辞不在 `暂无/未定名/不可得/拿不到/取不到/尚未/待定/未知` 里(如「本业务**没有**该数据」「**缺失**」「**未建**」「**无从取得**」),第三列**留空也 exit=0** —— 不是判错了,是**根本没检查** | 🔧 修复中(D6) |
| **#33** | J13 §2「替代路径」是**整段计数**而非逐条配对 | 🔴 **假绿** | 台账 **#22 已登记过一次,当时判「改动面大、登记不修」**。本次四位撰写人又各自撞出并给出三种绕过:①删一条箭头、在别条补写「替代路径」四字;②某条正文写「本条没有替代路径可写」;③一条写两个标记给另一条顶账。**「登记不修」的代价被如实收到了。** §2 的替代路径正是「不可达 ⇒ 模型不会停在缺口上」的唯一保证 | 🔧 修复中(D1) |
| **#34** | **J9 被关在 A 模板里** | 🔴 **假绿** | `if meta["template"] == "A"` 把 J9 **连同「临时默认值」的全文扫描**一起关进了 A 分支 ⇒ **66 份 B 契约上「待标定/需重新标定」一条判据都没有**。复现:B 契约 §1 写成 `- 取值：净增量阈值待标定,标定前先按大于 0` ⇒ exit=0 | 🔧 修复中(D15) |
| **#35** | J9 的豁免被「取值来源」行自己满足 | 🔴 **假绿** | J9 判红要求「有对冲词 **且** §3 内无命名数据源」,而 `- 取值来源：自有埋点（评估台账）` **本身就把数据源放进了 §3** ⇒ 对冲句**自带豁免**。A 契约 §3 只剩该行 + `- 取值：阈值待标定,标定前先按 0.05` ⇒ **exit=0**。⚠️ 与 #33(J9 误杀禁令句)是**同一判据的两端**,两条必须各配独立反向控制 —— 本仓库已实测过一次「两条判据共用一份反向控制 ⇒ 删掉任一条自检照样全绿」 | 🔧 修复中(D16) |

### #36–#39:J3(「R/D 格不许说成算法接入」)

| # | 缺陷 | 方向 | 症状(实测) | 状态 |
|---|---|---|---|---|
| **#36** | `MOUNT_RE` 第二分支漏 `上线`/`运行` | 🔴 **假绿** | 第一分支含这两词、第二分支(动词在前)没有 ⇒ `上线模型做自动分档` / `运行算法出分档` 都 **exit=0**,而 `模型上线` / `挂载算法` 判红。**「上线模型」是本语料最常见的写法之一**,而风险 N4(D/R 格出现算法模型即判错)全靠这条判据 | 🔧 修复中(D8) |
| **#37** | `NEG_TOKENS` **整行豁免、跨分句生效** | 🔴 **假绿** | `- FLOW-02/STG-02 不得改参；该格已接入算法模型做有界分类` ⇒ **exit=0**;删掉「不得改参」⇒ exit=1。前半句的否定词豁免了后半句的**真接入断言** | 🔧 修复中(D9) |
| **#38** | 否定词表漏**审查词** ⇒ 同义两写法两结论 | 🟡 **假红** | `…接收门禁**核对**…外接模型的接入合规性` ⇒ 判红(这是**审查**他人的接入是否合规,没有断言本格挂模型);同句把「不校验」换成表里已有的「不接入」⇒ 判绿。**同一语义、两种写法、两个结论** | 🔧 修复中(D10) |
| **#39** | 对**裸 `STG-0x`** 不设防 | 🟡 潜在洞 | `FLOW-02/STG-02 上挂分类模型` 判红,**去掉 `FLOW-02/` 前缀**的同一句话 ⇒ exit=0。⚠️ **实测 96 份已撰写契约里利用数 = 0 条** ⇒ 属潜在洞,非现存缺陷。**不为一个 0 利用率的洞制造假红** | 🔧 修复中(D14,先测量) |

### #40–#45:其余

| # | 缺陷 | 方向 | 症状(实测) | 状态 |
|---|---|---|---|---|
| **#40** | `_dim_row` 不认加粗/全角 | 🟡 **假红** | `\| ① 获取路径 \|` 改成 `\| **① 获取路径** \|`(**内容一字未改**)⇒ `[J5] 数据要求缺维：获取路径`。是 #17 修复的残余 | 🔧 修复中(D13) |
| **#41** | J13 §2 条目正则不认 `1)`/`（1）`/`①` | 🟡 **假红** | 六条不可达写成 `1) 不可达①…` 且六条箭头一条不少 ⇒ 报「§2 没有列出任何不可达条目」。**同族不一致:`1.` 认、`1)` 不认** | 🔧 修复中(D7) |
| **#42** | J12「卡真实存在」**依赖本机 HOME** | 🔴 **假红(整片)** | 图谱 `cards[]` 只含精选线 146 张,**已装线 slug 全靠 `~/.dsh/skills/<slug>/SKILL.md` 一条分支**判绿。`HOME=/tmp/empty python3 … --file <某契约>` ⇒ **exit=1 报 3 条找不到**;正常 HOME ⇒ exit=0。⇒ 换机 / CI / S12 独立核对器上,**引用已装线 slug 的契约会整片转红,而契约没错**。属 CLAUDE.md 铁律「判据用错了信息源」同族 | 🔧 修复中(D17):「卡不存在」与「我没能力查」必须可区分,**后者按 exit 2 处理,绝不静默降级成判红** |
| **#43** | **环境级**:`uniq` 把只差中文的行静默合并 | 🔴 **假仪器** | `en_US.UTF-8` 下 `printf 'a\n可写\n待卡\n可写\n待卡\n' \| sort \| uniq -c` ⇒ **`4 可写`**;`LC_ALL=C` 才给出 `2 可写 / 2 待卡`。**父代理实测**:2 行不同中文输入,`uniq` 输出 1 行(以 `wc -l` 的 ASCII 输出判定,排除显示层干扰)。**真实代价**:`grep -h "^status:" A/*.md \| sort \| uniq -c` 打出「73 份全部 `可写`」,与事实相反(实为 可写 61/待卡 6/方法待启用 6)。**方向的危险性与已知假绿灯家族完全一致:静默合并 ⇒ 数字看起来更好看。** 父代理一度差点把「139 份全部 status: 可写」当成实测事实写进报告 | 📌 **不改仓库代码,改纪律**:凡按中文取值做统计,一律 `LC_ALL=C` 或 `awk`/`python3`;`grep -c`/`awk` 不受影响;`check_contracts.py` 是 Python 实现,判据自身不受影响 |
| **#44** | 门禁拦的是**参数移植的对冲措辞**,不是移植的**事实** | 🟡 设计缺口 | §3 明写 `取值：抽检一致率 = 0.873,直接取自代表卡的自动根因分类准确率（论文实验条件）` + `阈值 = 0.65（照抄卡内论文实验参数）` ⇒ **exit=0**；同一段只要把「直接取自…」改成「标定前当**临时默认值**」就立刻 J9 红。**而 CLAUDE.md 写的恰相反:「A 拦参数移植…正是 A 模板要拦的东西本身」。** 值 0.873/0.65 就是卡内论文实验参数,一个字都不会被判红 | 📌 **登记不修** —— 真要拦需在 §3 引入**来源标注义务**(取值行必须能追到 §2 的五个命名源之一),那是**字段设计**变更,不属本次修复面。⚠️ 本条的教训必须留住:**门禁的绿灯只覆盖它测的那一维** |
| **#45** | `build_contracts.write_skeleton()` 的 status/cards **不读作业包** | 🟡 口径互斥 | 骨架一律写死 `status: 待卡` + `cards: []`(`fm_block` 的默认值),而批次文件与 workpack 给的初值是「可写(有候选卡)」⇒ 撰写人若只改 `cards` 不改 `status`,**会被 J12 判红**;若只改 `status` 不改 `cards`,同样判红。多份契约的撰写人各自手工对齐。**同一件事有两个事实源,且互斥** | 📌 登记不修(改生成器要动已落盘的 139 份骨架口径);**但作业包与骨架必须有一个被声明为事实源** |

### 卡侧事实(非门禁缺陷,但会被下游当事实用)

| 编号 | 事实 | 实测证据 | 处置 |
|---|---|---|---|
| **F-1** | 卡面声明的 `references/implementation.py` 行数**恒比包内实测少 5 行** | 三位撰写人各自独立实测:**7/7**、**9/9**、**4/4** 全部命中,差额恒为 5 ⇒ 是打包脚本在代码前加的 **5 行来源注释头**(`# dsh-paper2skills · 完整实现（恢复自 paper2skills 语料 vault）` + 卡 id/出处/核对/生成 4 行),**不是抽样误差** | 📌 登记为**一条体检项**,不逐卡改数。已有多份契约按「卡面记 N 行(包内实测 N+5 行)」双写 |
| **F-2** | `p2s-multicarrier-parcel-tracking-fusion` 卡面自报节选「**60 行,已顶到上限**」 | 实测该代码围栏**只有 59 行**(= `implementation.py` 去掉 5 行头后的 `[5:64]`)⇒ **比自报少 1 行** | 📌 疑似**真缺陷**,已登记 |
| **F-3** | `p2s-wms-exception-action-trigger` 卡面节选**语法不完整** | 卡页自记 `ast.parse` 失败:**第 47 行 `[` 未闭合**;`references/implementation.py` 完整实现体无此问题 | 📌 该卡当 K1 素材时须换用 `references/implementation.py` |
| **F-4** | 卡面自相矛盾 | `p2s-personabot-rag-profiling` ① 段「每 SKU ≥ **50** 条评论」vs ③ 段「每 SKU ≥ **500** 条评论」 | 📌 登记,**不替它选一个** |
| **F-5** | **多张卡共用一个 arXiv 号** | `2305.12345` 被 **19** 张卡共用;`2106.09876` 被 **4** 张共用;`2310.11823`、`2406.12847` 各被 **3** 张共用。另有多张卡的 ⑧ 段**自承**卡页 arXiv 号指向无关论文(视为无论文来源) | 📌 与 S10(venue 回填)直接相关;凡自承不可采信者,**不得据其编号推断 venue** |
| **F-6** | **范本契约的样板句被批量继承** | 「口径归属」第三列的样板句 `…字段清单暂按口径并逐条标注「口径未签发」；契约签发后按版本号替换…` 在**约 20 份**已撰写契约里出现 **≥18 字完全相同**的窗口。**来源正是范本契约** —— 撰写人「照范本密度写」,把范本的句子一起抄了。而 common §2.1 的**同质化禁令**(同一句处置在多份里重复 ⇒ 视为未撰写)**没有任何机检** | 🔧 处置中:新建跨契约同质化检测器 + 逐份改写被点名单元格 |

> ### 这批缺陷最值得留下的一句
> **#28–#35 全是同一个结构:「义务的覆盖面比判据宽」。**
> 规范写「必须给本业务取值」,判据只认「带某个标签的行里有任意一个数字」——
> 于是**格号、条款号、跨段转指、禁令清单、卡面数字**五种东西都能冒充取值。
>
> 而它们能长期存活,是因为**判据的绿灯报告的是一个它从未检验过的结论**。
> 这与 #23(读空却打出「已挂契约 0 / 未接线 1338」的正确样子)是同一件事:
> **假绿灯不报错,它报一个合理的数字。**

> ### ⚠️ 因此,关于「139 份全绿」的一句必须写下来的口径
> 撰写人 **ea1c4175** 在交活时把这条量化了,比缺陷本身更重要:
>
> > 全库 139 份**当前都含「带标签的真取值行」,空转通过 0/139** —— 缺陷是**潜伏**的,不是已经在骗人。
> > 但它意味着 **`--all` 报「139/139 全绿」不能读成「139 份都给了取值」**,
> > v2 的核心正面义务(R9)实际上没有被判据抓住;后续任何一份只要小标题带编号,删光取值也照样绿。
>
> 这正是本仓库反复付学费的那件事:**绿灯的适用范围,等于它真正测过的那个子集,
> 而不是它看起来覆盖的那个全集。** 引用「139/139 全绿」时,必须同时说明它**测了什么、没测什么**。


---

## #46–#53:J13/J3 门禁修复批的**收口实测**(2026-09-13,修复者回填)

> 末号核对:本批落笔前台账末号为 **#45**;#28–#42 已由主控登记了 D1–D17 的**缺陷**,
> 本节的 #46–#53 只登记**尚未入账的四条**及其**收口读数**(不重复登记已入账的)。
> 全部实现在 `paper2skills-research/scripts/check_contracts.py`(md5 前 `82768f6f…` → 后 `ae5de448…`)。

| # | 缺陷 | 方向 | 症状(实测) | 状态 |
|---|---|---|---|---|
| **#46** | ① 的枚举词被**否定句**劫持 | 🔴 **假绿** | `any(e in access for e in ACCESS_ENUM)` 是子串匹配 ⇒ `\| ① 获取路径 \| 本企业无自有埋点,实际全靠人工估算 \|` ⇒ exit=0。与 #17 同族、方向相反(那条是标签被子串劫持,这条是**枚举词被否定句劫持**) | ✅ 已修(逐子句判定,枚举词前不得有否定词) |
| **#47** | **转指/版本号**是 #30 的第二个载体 | 🔴 **假绿** | `- 阈值：本业务暂无此数据` ⇒ exit=1(正确);**只加四个字** `（第 2 版口径）` ⇒ **exit=0**。变体 `- 取值：待定（见第 2 节）` 同样 exit=0 | ✅ 已修(剥 `第 N 版/节/条/项` + 转指成分**连同它指向的东西一起剥**) |
| **#48** | `gap_without_disposal()` 被 `has_value()` **段级短路** | 🔴 **假绿** | §3 ＝ `- 取值：容差档 5%。` ＋ `- 本业务暂无季节性先验来源。` ⇒ exit=0 —— **段内任意一处取值解除了全段的缺口义务**。主控原判「修好 #47 后自然收敛、不要单独加判据」,**后经复现撤回**,升级为正式判据 | ✅ 已修(**颗粒度＝条目块**,与 B §2 逐条配对同颗粒度;紧邻条目只认**显式处置语**、不认裸数字) |
| **#49** | J13 五维**只扫「取值」列** | 🔴 **假绿** | 撰写人把缺口陈述写进**处置列**(`\| ① 获取路径 \| 需授权 \| **缺口在本维**：〈海关与合规条款库〉尚未接入。 \|`)⇒ exit=0。缺口声明与「先用什么+何时换」的义务**一起失效**,且处置列非空也不校验它到底是不是处置 | ✅ 已修(两列都扫) | ⚠️ **追记（D28，同批收口）**:按列判**又制造了 8 格假红** —— 撰写人常把「缺口+处置」合并写进取值格、第三列留空(实测 B-032/033/034/036/039/040 六份 8 格,④ 行里直接写着 `缺口：…处置：…`)。改为**按整行**判(先把陈述缺口的句子摘掉,免得缺口句救自己) | ✅ 已修 |
| **#50** | 合并标签行 `取值来源与取值：…` 被**整行跳过** | 🟡 **假红** | #18 的修法是「标签后紧跟『来源』二字就整行跳过」,不是「这行到底给没给取值」。实测 §3 写 `- 取值来源与取值：自有埋点 × AGT-045 指标契约；Service 可用性 ≥ 99%、Task 完成率 ≥ 95%` ＋ 算式行 ⇒ **exit=1** —— **这一份其实给了取值**。`取值来源与阈值：`/`阈值来源与取值：` 同样中招 | ✅ 已修(改成「锚定 + 紧跟冒号」两条独立条件；合并标签行的标签短语里若还有另一个取值标签词则照常按冒号后数字判) |
| **#51** | 缺口词**语境判据**缺位(D6 补词的副作用) | 🟡 **假红(整片 24 份)** | #32 补词后全库实测撞出 **25 条判红 / 24 份契约**,机制三桶:①词在≠意思在(`缺失率`是指标名、`有没有`是疑问构词、`未知`是引号里的枚举值、`许可缺失…的，一律…`是条件从句);②处置句在,只是被中间那句顶出窗口;③**判据不认它自己模板规定的词** —— A-049 的处置就在同一句里(`**不可得** 的是…,**替代取值**为…`),而 `DISPOSAL_TOKENS` 收的是「替换」**不收「替代」**,可 B 模板强制要求的写法字面就是 `→ 替代路径：` | ✅ 已修(语境判据 + 条目块颗粒度 + 词表补 `替代/已登记/登记进/工单/退到`);收口后 **--all 回到 exit=0** | ⚠️ **追记（D27，主控反向控制第二次报出）**:桶②的「邻块/邻句豁免」只认**词**、不认**话题** ⇒ 任意一条带「替换/改用」的**无关样板话**都能救活缺口句(B-040 §1 末尾插真缺口 ⇒ exit=0)。实测 **63/66** 份 B 契约的 §1 末句是「换卡时只替换方法实现…」这一族样板。**修法**:豁免收紧到**同一条目块**,与 §2 逐条配对同颗粒度;**未用样板话黑名单**。⚠️ **口径说准**:B §1「缺口句自身无处置」条目数 = **0**、句级 14 条但全在同块内有处置、**只靠样板豁免的 = 0 条** ⇒ 这是**潜伏的洞,不是已经在漏** | ✅ 已修 |
| **#52** | 补 `上线\|运行` 的**新增命中**不是 0 行 | 📌 复测读数 | 主控实测 0 行;**本机复测 1 行** —— `CTR-A-072-容量管理.md:82` 的 §3 数据来源行 `…（Agent 运行台账）＋ 平台后台（模型服务与云资源用量…）`,「运行台账）＋ 平台后台（模型服务」正好落进 12 字窗。该行**当前不触发 J3**(J3 只判带格号的行),属**潜在假红** | ✅ 已修(间隔里排除并列分隔符 `＋ + ｜ \|`);修后新增命中 **0 行** |
| **#53** | 裸 `STG-0x`(#39)的实测读数 | 📌 复测读数 | 主控在 96 份上实测利用数 0 条;**本机在 139 份上复测仍为 0 条**(裸 `STG-0\d` + 接入语义 + 无否定/审查词 = 0 行) | 📌 **登记不修**(不为 0 利用率的洞制造假红) |

| **#54–#57** | 材料引文：契约声称「材料『X』」而 X 查无实据 | 🔴 **假绿（模板级扩散）** | 11 个词 / 受影响 32 份。**污染源分四类**：①范本种子 `季度经营策略`（材料真词是「月度经营复盘」）；②**二手来源被标成材料原文**（`§F.3`/`§F.5` **材料里根本没有这两个章节号**，那是我方综述 `_survey_org_model.md` 的章节）；③改写后仍称「原文/原话」；④本项目决策（`holdback`＝决策 Q10）被标成材料。⚠️ **数字随语料变过**：初测 108 处/67 份 → 同质化整改后 **30 处/24 份** —— **是契约变了不是判据变了**。⚠️ **模板是干净的**（`TEMPLATE-A/B` 实测无这些词），种子只在**冻结**的 `contracts/v2/` 样本里 —— **冻结不等于无害：被设计成会被抄的东西，留在原地就会继续被抄**。 | ✅ W3 已落地（独立门禁 L4e + 端到端反向控制 3/3）；W1/W2 逐份改标进行中 |
| **#58** | **新门禁自己的变异测试施不上力**（`run_phase6_gates.py`） | 🟡 **假仪表** | `--mutate` 首跑报 **0/3 抓住**，看起来像「三条端到端用例是摆设」—— **实际是仪器坏了**，两个独立缺陷：① `cli()` 里**硬编码**了原文件路径，`checker=` 参数根本没被用上 ⇒ **变异体从没被跑过**；② 变异锚点用裸 `    if bad:\n`，而该文本在文件里**出现两次**（打印分支在前、退出码分支在后），`replace(...,1)` 只改到**打印**那处 ⇒ 变异没施上力。另有一条 `... in o_red or True` 的**恒真断言**（本仓库第 4 次抓到这一形态）。修后 **3/3 抓住**。⚠️ **教训（与 S4 缺陷⑥、F2 的 M3/M5/M6 同族）**：**先证明变异改变了真实取值，再谈判据有没有劲** —— 否则你会去修三条本来正确的用例。 | ✅ 已修，`--selftest` 11/11、`--mutate` 3/3 |
| **#59** | **溯源账答不出它存在的那个问题**（S10 venue 回填） | 🔴 **账实不符** | 覆盖率验收达标（venue_tier **25/146 → 146/146**），但账本身错在两处：① **`old_value` 恒等于 `new_value`（146 条改动数 = 0）**，而 git 旁证 `Skill-Persona-Based-AB-Simulation` 确实 **`top` → `CCF-B`** ⇒ `old_value` 是**写盘之后**读回来的，**这张账因此答不出「哪些卡被改过、从什么改到什么」**，而它正是 venue 判定的**溯源凭证**（铁律 1）；② **121 张卡的 `reason` 是假的**：git 实测 HEAD 有 `venue_tier` 的只有 25/146 ⇒ 121 张是本次**新赋值**，而账里 132 条标 `as-is / 已是规范档，逐字保留`（抽 `Skill-Uplift-Modeling`：HEAD 根本没有这个字段）。⚠️ 与仓库既有教训同族：**账自己描述错了它记的那件事**。⚠️ 反向检查**过了**（要写进报告）：账的 `source`/`evidence` 里没有 `fulltext`/`正文`/`底本`/`body` 作为 venue 依据 ⇒ **铁律 1 确实守住了**。⚠️ **修法边界**：只改账的取数方式（改从 `git show HEAD:<path>` 或改动前快照取），**不许为了让改动数好看回头去改卡** —— 卡上的值是结果，账是凭证。 | 🟡 **①已修实、②只修了半列**（主控复核，见下） |

> **#59 主控独立复核（不复用被复核者的任何脚本，改用 git 当独立仪器）**
> · **① 打回已销**：`git grep -h '^venue_tier:' HEAD -- 'paper2skills-vault/**/Skill-*.md'` 独立测出 HEAD 有该字段的恰 **25** 张，逐档 **9 preprint / 5 CCF-A / 4 CCF-B / 4 `top` / 2 `workshop` / 1 `demo`**；账里 `change_summary.by_change_status` = **as-is 18 / retiered 7 / backfilled 121**（18+7=25 ✓），且 `old_value` 的取值分布与 git **逐档相等** ⇒ **`old_value` 现在真的是写盘前取的了**。`by_new_value`（preprint 68 / non-paper 48 / CCF-A 15 / CCF-B 14 / field-top 1）也与主控直接扫 146 份 frontmatter 的实测**完全一致**。
> · **② 只修了机读列、没修人读列**：`old_value` 为空（HEAD 无该字段、本次新赋值）的 **121** 行里，**111 行的 `reason` 仍写着「已是规范档，逐字保留」**（抽 `Skill-DiD-Difference-in-Differences` / `Skill-IV-Instrumental-Variables` / `Skill-Intelligent-Attribution-Causal-Forest` 均为 `status: as-is` + `change_status: backfilled` + reason「逐字保留」）。⇒ 机读列已经诚实，**人读列还在对一件从未发生过的「保留」作证**。与 **#18** 同族：**判据是真的，但报告把它翻译成了另一句话**。已下达：`backfilled` 行的 `reason` 改成它实际做的事，只在 `as-is`/`retiered` 行保留「逐字保留」。
> · **③ 一条留待判定的实质问题**：`by_new_value` 里 **UTD24 = 0、FT50 = 0**，而 registry 侧记着 UTD24 8。若是「精选线 146 张里确实没有商科顶刊 venue 的卡」⇒ 那是**关于精选线本身的实质发现**（正对应 S10 的「分域定线」：商科实证类在主战场缺席），必须写出来；若是被折叠进了别档 ⇒ 是缺陷。已要求用 `evidence` 列实测作答。

| **#60** | **J9 依据行是「位置锚」** ⇒ 上游插一行就整片假红 | 🔴 **假红（整片）** + 🟡 **判据无测试** | S13 给 146 张卡插 `l1_id/l2_id/l3_*` frontmatter 后，`build_card_classification.py --check` **整片判红**：「依据行已变（登记 sha1 `adaca6c1…` ≠ 现算 **`da39a3ee5e6b`**）—— 登记失效，须复核后重登记」。`da39a3ee5e6b` 是 **sha1("")** ⇒ 判据取到的是**空行**。机制：原实现 `lines[ln-1]` —— **先按行号取行再比 sha1**，于是任何在依据行**之上**插内容都会把它整体下移。⚠️ **最坏的下场不是白干**：提示语写着「须复核后重登记」，照着做的人会把 sha1 改成**空行的**，锚点**从此失去意义**。与 #11「判据只认一种字段名」同族：**判据依赖位置而不是内容**。⚠️ **J9 此前一个 selftest 用例都没有** —— 一个会发出误导性指令的判据，此前无人守。修法：先按登记行号验收（快路径）；不符时**回捞整份文件**找「strip 后 sha1 相等」的那一行 —— 找到 ⇒ **判通过**，只把行号漂移记进 `misplacement_evidence_line_drift`（**一等输出**，不静默）；整份文件都找不到 ⇒ 才是「内容真的变了」判红。**实测**：L3c **exit 1 → exit 0**；新增 selftest **4 条**（原样绿 / 上方插 3 行只记漂移**不得判红** / 内容真改写**必须判红** / 漂移必须进一等输出）；`--mutate` **2/2 抓住**。⚠️ **`--mutate` 首跑 0/2**，原因是**变异体根本没跑起来** —— `REPO = Path(__file__).resolve().parents[2]` 从脚本自身位置推出，扔进 `/tmp` 后路径全断（`FileNotFoundError`）。**与 #58 是同一个坑，一天内第二次**：**先证明变异体真的跑了，再谈判据有没有劲。** | ✅ 已修 |

| **#61–#66** | **S2 收割器自己的六个仪器缺陷**（`harvest_legacy_solutions.py`） | 🟡🟡🔴🟡🟡🟡 **四假一真一虚** | 六条全部**先报红后修**，无一条是事后总结。**I1（#61，坐标假绿）**：`_pos` 是**切片内**下标却拿去对**全文**算行号 ⇒ 218 条锚点的行号整体前移，最大那批落进**侧边栏** —— 即「样板当正文」的坐标版。**I2（#62）**：`strip_tags` 写成 `<[^>]+>`，而语料里有**裸小于号**（`接受率<40%时…`）⇒ 行区间回查被截成半句。**I3（#63）**：锚点区间取「下一块起点−1 / 文件末」⇒ 实测一个区间为 `L102–L285`（区间退化成整段）。**I4（#64，与 #58/#60 同一个坑，一天内第三次）**：`REPO` 由 `__file__` 推而变异体跑在 `tmp` ⇒ 变异体**不是只差一行**，变异测试当场失效。**I5（#65，变异分数虚高）**：**四条判据抢同一份篡改样本**（J0a∩J3、J5∩J6、J5∩J8、J2∩J3）⇒ 一条守卫生效会让四条一起变红，**变异分数因此虚高**（这与「一条判据没有专属样本」是**方向相反**的两种病）。**I6（#66）**：报告数字块基准错取**产物**而非**现场真值** ⇒ 报告的「实测值」其实是在复述自己的输出。 | ✅ 已修，见 §S2 收口读数 |

| **#67** | **材料引文核验器的假绿**：判据报 exit 0「全部有据」，而同一批契约里同类残留仍在 **104 处**（颗粒度是「行」而声称的颗粒度是「条目块」） | 🔴 **假绿（整片）** + 🟡 **判据的适用范围没写出来** | **发现经过**：S1-W 交付时**主动披露**「我 32 份内清了、范围外 49 处 / 40 份**没碰**」。主控随即**不信任何一方的账**，用两条独立仪器复测：(1) `grep -rl`（定串）测 `季度经营策略`；(2) **逐号对材料核实**章节号 —— 材料《AI组织变革》真实存在的编号章节只有 `1–12` 与 `R01–R05`（共 18 个），而契约声称的 `F.3 / F.5 / E.2 / E.3 / E.4 / E / 1.2` **材料里一个都没有**（我方综述 `_survey_org_model.md` 才有 `## E.`/`## F.` 两节）。**实测残留**：`季度经营策略` **40 份**（S1-W 独立测 49 处/40 份，两份仪器同值）、`材料 §X` 一族 **38 处 / 23 份**。⚠️ **机制（逐条实测，不是推测）**：`extract()` 的扫描单元是**行**（`for ln_no, line in enumerate(...)`），且三重收窄 —— ① `材料` 必须与引号**同一行**，引号落在 blockquote **续行**上即 `pos < 0` **静默跳过**（实测 `CTR-A-048:39` 全行没有「材料」二字）；② `head` 再按 `[。；\n]` 切片，**枚举里的 `；` 会把管辖它的头部切掉**（实测 `CTR-A-067:35`）；③ `run` 一旦含引号就 `continue`（那条守卫是为「一行多引号」加的）⇒ **`材料已给出的数（A、B、C）` 这种枚举里只有紧随材料的第一个引号会被判，后面全是盲区**。⚠️ **与 D27 互为镜像**：D27 是「豁免只认**词**不认**话题**」⇒ 收紧到**条目块**；本条是「声称只认**同行**不认**条目块**」⇒ 该放宽到**条目块**。**同一条颗粒度教训，两个相反方向**；而本仓库已两次因颗粒度（列 vs 行 vs 块）翻面判决，**两次都是假的那一面**。⚠️ **主控自己在修这条时踩了三个坑（全部先报红后修，这才是本节最该留的部分）**：**(a) 整块口径过宽** ⇒ 首版报 78 处/59 份，逐条眼睛看下来大部分是**假阳性**（「这个商品该归哪个编码」「这份证书能不能过」「允许／禁止」都是**本契约自己的缺口问句**）；**(b) 括号方向取反** ⇒ 收紧成「材料处**已开着**的括号」后，`材料已给出的数（…）` 恰好**没有开着的括号**（`（a）` 已在它之前闭合）⇒ **真缺陷被丢掉、只剩假阳性**（13 处/6 份，`季度经营策略` 消失）；正确取法是「材料**之后**很近处那个左括号」；**(c) 去归属语窗口过宽** ⇒ `决策 Q11` 是**并列的枚举项**却在「材料→引号」整段里被当出去归属，又一次把真缺陷判掉；收紧到**材料前后各 12 字**的邻域后，`本契约把材料未命名的…` 仍被正确去归属、`决策 Q11`（距材料 18 字）不再误杀。**修法（分两步，不许一步到位）**：① **不是改判据** —— 先把 L4e 的收尾句从「全部材料引文有据」改成「**在本判据适用范围内**有据」并把盲区写出来（**一条测不出来的政策不是政策；绿可以，但绿不许读成「一处不剩」**），L4e 的判定与 selftest **11/11 未动**；② 另立**独立**仪器 `scripts/check_material_residue.py`（`--selftest` **7/7**，含 4 条反向控制：干净夹具必须 exit 0、去归属语在块内不得算声称、材料根不存在 exit 2、契约目录 0 份 exit 2），把残留变成**看得见的数**。⚠️ **它故意还没接进门禁**：**家族一（24 个词 / 66 处）精度未定标** —— 已确认的真阳性是 `季度经营策略` ×40 份，其余大部分眼睛看下来仍是假阳性，**把一个未定标的仪器接进门禁 = 制造噪声，而噪声会被忽略**；**家族二（38 处 / 23 份）已逐号对材料核实，是精确的**，可随时接。 | 🟡 **部分修**：L4e 的适用范围已写明并已落地；残留账已可见（exit 1）；**残留本体未清**（工单见下） |



### 本批的收口读数(可复核)

- `--selftest`:**52 → 123 用例全绿**(每条修复各配正反两份样本,**无两条判据共用同一份反向控制**)。
- 变异测试:**21/21 抓住**(逐条把修复改回旧逻辑,自检必红;每条由对应用例抓住)。
- `--all`:改前 exit=0、改后 exit=0。**逐份对照 139 份 ⇒ 判决变化的文件 0 份**(D27/D28 收口后复测仍为 0)
  (两份契约语料期间从「已撰写 125」长到「139」,那是**并发撰写**所致,与判据变更无关)。
- 退出码四态、`--file`/`--all`/`--selftest`/`--json-out` 四种入口**均未变**。

### S2 收口读数（2026-09-13，主控复核）

`harvest_legacy_solutions.py`：`--check` **exit 0**（15 条判据对现场重算全过）· `--selftest` **exit 0**
（31 项用例 · **17/17 变异抓住** · 4 条 exit-2 端到端 · 3 条反向控制 · `scripts/` 跑前跑后无新增文件 32→32）。
已接进验收面：**L7a / L7b**（主控接线，见 `run_phase6_gates.py`），全表 **20 → 22 条门禁**。

**判定式是成对的**（这是 #65 的修法）：原版对篡改样本必须 `exit 1`，**且**变异体（守卫改 `if False:`）对**同一份**样本必须 `exit 0` —— 只有同时成立才证明这条判据在承重，而不是被别的判据兜住了。

**三条实测，比数字本身更重要**

1. **落格只能走产品侧 `classification.json`，走精选线 146 张是死路。** chip → 精选线 **16/181 = 8.8%**（7 个页面 0 命中）；chip → 产品侧 `classification.json`（`items[].id`）**179/181 = 98.9%**。完整链每一跳都是已有事实源：`chip href → items[].id → items[].l3[] → graph l3[].name → l3[].role_id → roles[].flows[] → cells[]`。⚠️ 这条**同时是一条对 S5 的预警**：精选线在产品侧的可见度只有 8.8%，正是 S5（换底）要解决的问题；在 S5 落地之前，任何「按精选线落格」的写法都会造出第二套连接。
2. **「旧方案页覆盖 24/24 M 格」没有区分度**，不得当供给结论用 —— 实测每个 M 格被 **14–19** 个方案页可达。有区分度的是 L3 层：**87/151 落格，64 个 L3 未被任何旧方案页触及**。这一层才是缺口图。
3. **源侧的缺口如实登记（不编造）**：路线图的**进入/退出条件在源里根本不存在** —— 72 个 Phase 只有阶段名/时长/动作/预期产出，显式进入条件标记 **0** 条、退出条件标记 **0** 条（这两个数是**探测器算出来的**，并用构造样本自证探测器能命中）；分层架构只有三元组（层号/层名/描述），无边界字段、无层间关系字段；`sol-counterfactual-pricing.html` 的「三大陷阱」段是**空的**（h2 声称 3 条，`.sd-trap` 计数 0）。

**任务书三处口径与实测不符（已登记，均为任务卡写错而非判据错）**：①「L1–L6 架构」实测每页 **4–7** 层、最高 **L7**；②「194 卡位索引」复算**成立**（181 唯一 + 13 个跨页重复）；③陷阱 **54 = 18 页 × 3**，**不是** 19×3。

**入库处置（主控）**：`legacy-solutions.json` 原先被 `.gitignore:93`（`data/**/*.json`）挡住 ⇒ **不补例外就等于没交付**（重算需 `playbook/solutions/` 20 个 HTML 的现场指纹）。已补 `!paper2skills-research/data/legacy-solutions.json`，理由与 `gap-ledger.json` 同：**它是一份账，不是中间态** —— 194 条被丢弃的卡位与 54 条陷阱，丢了文件就都从账上消失了。


### #67 的工单（残留本体未清）

| # | 工单 | 规模 | 说明 |
|---|---|---|---|
| W-67a | **家族二 · `材料 §X` 逐处改标** | **38 处 / 23 份** | 已逐号对材料核实：材料真实编号章节只有 `1–12` 与 `R01–R05`；`F.3/F.5/E/E.2/E.3/E.4/1.2` **材料里一个都没有**（那是**我方综述** `_survey_org_model.md` 的 `## E.` / `## F.` 两节）。改法沿用 S1-W 的**类 2**：改标「本项目综述 §X 的判定（**二手来源，不是材料原文**）」，**正文一字不删**。⚠️ **可机械判定** ⇒ 可随时接成门禁。 |
| W-67b | **家族一 · `季度经营策略` 逐份改标** | **40 份**（另有 49 处） | 与 S1-W 清掉的 32 份同法（改标「**业务侧默认**：材料未给季度粒度 ＋ 逐份替换条件」）。⚠️ **不得**换成材料真词「月度经营复盘」—— 两者**不是同一粒度**，换词会改掉契约的业务语义（A-004 的 13 周样本量、B-063 的回放窗、A-061 的滚动预测窗都建立在季度上）。 |
| W-67c | **家族一其余 23 个词定标** | 66 处 / 53 份 | 现测 24 个词里只有 `季度经营策略` 是独立确认的真阳性。**先人工定标**（逐词判真阳性/假阳性），**再**决定是否接门禁；未定标前 `check_material_residue.py` 家族一**不得**进门禁。 |
| W-67d | **把 L4e 的判据本身放宽到条目块** | —— | 治本。但要**先有 W-67c 的定标**，否则放宽后无法区分「新抓到的」「新造出来的假红」。D28 就是这么自己造出假红的。 |

> ### #67 的教训（比这一条修复更重要）
> **「某个东西不存在」的结论，要先问「我用的仪器能看见它吗」。** 本条里这个错犯了**三次**，
> 且三次都在**主控自己手里**：① 我先信了 L4e 的 exit 0；② 我用 `grep` 查材料章节号时
> **把 `.` 当成了正则通配符**（`grep "F.3"` 匹配到 `F13`/`F 3` ⇒ 报「材料里有 9 个文件命中」，
> 差点据此判「章节号不算残留」）—— 改 `grep -F` 后是 **0**；③ 我修仪器时两次**把真缺陷改丢、
> 只留下假阳性**（括号方向取反、去归属语窗口过宽），两次的读数**都看起来很正常**。
> ⇒ 与本仓库铁律 1（venue 不得用底本正文判定）**同一条**：**先问仪器能不能看见，再下结论。**


## #68–#70：验收面自查批（2026-09-13，主控）

这一批**不是**由上游任务撞出来的，是主控做「验收面覆盖审计」时自己撞出来的 ——
问法只有一句：**F1–F7 / S1–S13 每一项的验收，真的被 `run_phase6_gates.py` 跑着吗？**

### #68 🔴 一条**永远不可能失败**的判据（J9 的结构洞）

| 项 | 内容 |
|---|---|
| 位置 | `route_backlog.py` → `check_input_freshness()`（J9 输入新鲜度） |
| 形态 | 把「逐行内容核对」写在 `if info["stale"]:` 分支**里面** ⇒ `声明 sha == 当前图谱 sha` 时**提前 `return fresh`，一行内容都不看** |
| 错在哪 | **把自述当成了核验**。`_meta.graph_sha256` 是**生成者自己写下的**「我读的是哪张图谱」，**不是缺口账内容的指纹**。内容被改过、而 `_meta` 仍写着当前 sha 的缺口账 **畅通无阻**；反倒是「老实声明读的是旧图谱」的那种会被逐行核对 |
| 一句话 | **越是看起来新鲜的，越没人查。** |
| 实测证据（不是推理） | `git log` 逐提交复算 `graph sha` vs `declared`：`4e1b498`(F3) · `59429b9`(F4/F5) · `bb06b74`(S5) **三处 `stale=False` ⇒ 内容核对是死代码**；只有 `dfe9eff`(S1) 一处 `stale=True` 时它真跑过 |
| 连带后果 | `--selftest` 里那条「篡改：过期底本 + 排序依据被改 ⇒ 判红」**从 `dfe9eff` 起就在失败**（41 条 / 1 败），**没有人看见** —— 因为它当时**还没被接进验收面**。两条纪律的交叉：**「判据的适用范围被默认成了全体」×「交付了但没人看得见」** |
| 修法 | 内容核对**与 sha 状态无关，每次都必须跑**；sha 只决定**归因**（`fresh` / `stale_immaterial`），不决定**判不判**。判决名改为 `material_mismatch`（不再叫 `stale_material` —— 它未必是「过期」） |
| 锁定 | selftest 42/42：**新增一条专打结构洞的用例**（`sha 与现状一致 + 排序依据被改 ⇒ 必须判红`），并保留原「sha 过期 + 内容被改」用例与反向控制。「sha 一致时不得误报」仍在 |
| 劲道证明（对拍，非自述） | 同一份篡改样本（`复购实验 n_legacy 51→999` + `_meta.graph_sha256` 写成当前值）：**旧逻辑 errors=0（没抓住）· 新逻辑 errors=1 `material_mismatch`（抓住）** |

### #69 🔴 四条**已交付却没接线**的门禁（与 #23 同族，这次一次四条）

做覆盖审计时把「有 `--check`/`--selftest` 模式的脚本」与验收面清单**机械对差**，差出四条：

| 缺的门禁 | 归属 | 接上时的实况 |
|---|---|---|
| `route_backlog.py` | S3 extract backlog 接管 | **`--check` exit 1（红的）** —— 产物是 S5 换底前的照片 |
| `rebase_p2s_cards.py` | S5 p2s 换底 | 0（三个模式全绿） |
| `holdback_power.py` | S9 独立站 holdback | 0（三个模式全绿） |
| `build_venue_tiers.py` | S10 venue 三词表统一 | 0（`--selftest` 默认带变异，14.3s） |

⇒ 与 **#23**（`check_card_identity.py` 交付时就是 exit 1 而没人接线）**完全同型**，
且**同一批**里又犯了一次 —— 说明「交付后接线」不是一次性的补救，是**每次交付都要问的一句**。
已接线为 **L9a/L9b · L10a/L10b/L10c · L11a/L11b/L11c · L12a/L12b**，验收面 **30 → 40 条**。

**其中 `route_backlog --check` 那条红的根因（已复核，不是判据坏了）**：
产物是 S5 换底前的照片 —— 产品侧分类 **1338 → 1390**（S5 推的 52 张），逐 L3 供给数跟着变。
**决策部分逐条相同**（12 条接管序、L3 落点、档位、占检索预算的 3 条全不变），
变的只有供给读数：9 条 `n_legacy` **全部上移 +1..+5**（「只增不减」与「只增了 52 张卡」互为佐证）。
⇒ 是**真过期**，按正路 `--write` 重生成；`--check` 复绿 0。

### #70 🟡 G3 把**元数据**当成业务场景信号（S13 撞出，主控裁定并实测）

| 项 | 内容 |
|---|---|
| 位置 | `gate_check.py` → `gate_g3()`：`prose = strip_code(text)`，**没剥 frontmatter**（而 `gate_g2` 剥了 —— 漏洞 #12 的修法） |
| 症状 | S13 新加的 `l3_business` / `l3_all` 是**能力名**，恰好命中 `CONCRETE_SIGNALS` 关键词表 ⇒ **一个纯记账字段在替卡片自己刷「场景具体」的分** |
| 实测（146 张全量） | 5 张卡各多出 1 个信号；`Skill-Dialogue-to-Action-Graph` 3→2 —— 多出来的那个来自 `l3_all: 售后处理 / 客诉分诊` 里的「客诉分诊」，**`G3-NOT-CONCRETE` 整条红线消失，而卡正文一个字节没动** |
| **主控裁定的准确口径** | S13 记「G3 红 93→92，差的那条 = ④ 的 G3 泄漏」。**这个数是对的**，但单位要说清：那是 `red_findings`（红灯**条数**）而不是**判红的卡数**。复算：**卡数修前修后都是 79 红 / 67 通过 = 45.9% 不变** —— 因为那张卡**本来就已经红**（`G3-NO-DATA-REQ`）。⇒ 泄漏的真实后果是**抹掉了 1 条红灯记录**（92，修后回到 93），不是把卡从红变绿。**两个口径并列登记**，免得下一个人拿 79 和 92 互相打架 |
| 修法 | ① 正文扫描一律用 `strip_frontmatter(text)`；② **`skill_relations` 不跟着剥** |
| 为什么②相反 | 段④「技能关联」在这批卡里大量用**技能名**（`Time Series Forecasting`）而不是 `Skill-*.md` ⇒ 全文里唯一机读得到的关联声明往往就是 frontmatter `related:`。实测：若跟着剥，**8 张卡掉到 0 条**、`G3-FEW-RELATIONS` 会多出来 —— **那不是修好，是把判据换成看不见**（漏洞 #11 同族）。⇒ 保留 `related:`、但**只认 `related` 这一个字段**（`venue_source` 里也含 `Skill-*.md`，那是**溯源**不是关联，实测 4 张卡各虚增 1 条），并把 `skill_relations_body` / `skill_relations_frontmatter` **分开报** |
| 无新假红 | `G3-FEW-RELATIONS` **65 → 65（差 0）**；全库 79 红 / 45.9% **不变** |
| ⚠️ 量它时我自己又踩了一次 | 先用 `vault/*/Skill-*.md` 扫，只看到 **92 张**（全库 **146**；另 54 张在 `07-NLP-VOC/00-知识库-Skill卡片/` 这类**嵌套目录**），于是「因 frontmatter 跨门槛的卡 = 0」。**判据的 glob 一变，结论就翻面** —— 与「仪器指错地方」同族，**这次发生在主控自己手里** |

> ### 这一批的教训（一句话）
> **「交付」不是「接线」，「声明新鲜」不是「内容没被改」，「我能扫到 N 张」不是「一共有 N 张」。**
> 三条都是同一个错：**把自己这一侧的读数，当成了对象那一侧的事实。**


---

## #71–#75：凭证扫描第三缝批（2026-09-13，主控）

> 现场记录：**`paper2skills-research/reports/PHASE6-F8-S8-凭证扫描第三缝.md`**（含全部读数与复现命令）。
> 本节的五条都是**同一条纪律**的不同面，一句话在文末。

### #71 🔴 「推送前必跑」的门禁**从未接线**，而且当时**正红着**（2026-09-13）

| 项 | 内容 |
|---|---|
| 位置 | `paper2skills-skills/paper-维护/scripts/scan_secrets.py` 与 `run_phase6_gates.py` 之间 |
| 事实 | `CLAUDE.md` 明写它是「推送前必跑」，但它**从来没进过验收面**。而它当时的读数就是 **exit 1**（3 条，全部由上一轮新加的 `check_key_exposure.py` 的**固件字面量**造成） |
| 谁发现的 | **人肉覆盖审计**，不是判据 —— 与 **#23**（`check_card_identity` 交付时即 exit 1 而无人接线）**完全同型**，**两天内第二次** |
| 代价 | 上一轮报告里的「凭证扫描 3174 文件 0 命中」在**写下的当时是真的**，之后**悄悄变成假的**，而没有任何读数会告诉下一个人 |
| 处置 | 接成 **L15a / L15b**；固件改 `_pem()` 拼接（见 #72） |
| 一句话 | **上一轮刚写下「『交付』不是『接线』」，这一轮又要重新发现一次 —— 差别只是这次是被一条新写的门禁发现的。** |

### #72 🟡 教训写在了**隔壁文件**里，于是等于没写

`check_key_exposure.py` 的三处自测固件写成完整 PEM 头字面量；
而 `scan_secrets.py` 的 docstring 早就写着这条教训（「本文件被自己扫出命中 ⇒ 门禁恒红 ⇒
**永远红的门禁等于没有门禁**；首版实测 12 处自命中」）。
**同一个坑，隔壁文件里犯了第二次。** 处置：抽 `_pem(kind)` 助手拼接，并在注释里点名这件事。

⚠️ **第三次就发生在写下这条教训的同一轮里**：本轮三个新产物（白名单的 `reason` 文本复述了 5 个占位值、报告正文抄了两处 PEM 头）各自又把字面量写了一遍，`scan_secrets.py` 当场判红 4 处。**根因不是疏忽，是「说明某物无害」的最自然写法就是把它写出来。** 处置改为机械化：`scan_secrets.py --selftest` **用例 7b「整个门禁家族都必须干净」**（另两个扫描器源码 + 两张判据表 + 家族文件缺失也判失败），配构造样本证明有劲。

### #73 🟡 墓碑（`***REMOVED-…***`）被当成了**新凭证**

三份 `playbook/skills/*.html` 的旧修订命中 `GENERIC_SECRET_ASSIGNMENT`，命中值就是 filter-repo
的清理标记。不处理会产生荒谬读数：**清理做得越彻底，门禁越红。**
处置**在规则层**（不是按文件豁免）：`scan_text` 先归一化墓碑再匹配，并单独暴露 `tombstones()`
（「曾经有过凭证」是**读数**，不该被归一化吃掉）。
⚠️ 反后门（漏洞 #10：**新增豁免本身就是一次放水**）：新增 `--selftest` **用例 21**，
主体是反向控制 —— 21c「**墓碑旁边的真 key 必须照样命中**」、21d「墓碑模式必须精确」。

### #74 🔴 固件里的「真值」把真凭证留在了公开仓库里；而**其中一条实测仍然有效**

| 项 | 内容 |
|---|---|
| 机制 | `scan_secrets.py` 的既有写法是**把真值拼出来**当自测固件（docstring 明说「运行时构造出的样本仍然是真值」）。拆开只骗得过**模式扫描器**，骗不过读源码的人：一行 `+` 即可还原 |
| 后果 | **防泄露的固件机制，把真值以「一行可还原」的形式留在了公开仓库里，而 `scan_secrets.py` 报「✅ 未发现凭证」** |
| 实测（3634 blob） | `sk-` 全形态 **0** ✅；去前缀的值 **2 个 blob**（含 **HEAD 版本**）；飞书 hook UUID **1 个 blob**（**HEAD 版本**）；墓碑 25 个 blob（filter-repo 确实清过裸值） |
| ⭐ **死活实测**（只读 GET + **反向控制**） | 对照（全 0 UUID）`HTTP 200 code=19001「token invalid」`；现场 `HTTP 200 code=19002「params error, msg_type need」` ⇒ **令牌通过校验** ⇒ **webhook 至今有效**。不是「未轮换（未知）」，是**实测仍然有效** |
| 修了什么 | 固件真值 → **同形假值**；先证明「值身份不产生检出能力」（规则是形状判据，换完 8 组用例全绿）。分工：**用例管形状，登记表管值** |
| 修不了什么 | **历史**。值已在公开远端上，唯一修法是重写历史，而**轮换本身才是真正的修法** |
| 处置 | 判据 C + 登记表（`data/exposed-credential-registry.json`，**只存 len+sha256**）；新增 **L16a/L16b/L16c** |

### #75 🟢 **活凭证不许豁免**（判据 C 的不对称规则）+ 三个自抓的仪器缺陷

- **规则**：`measured_status == "live"` 的条目，白名单里写了理由也**不生效**，门禁照红。
  理由：豁免的语义是「已知且**无可行动作**」；活凭证**有**可行动作（后台轮换），
  豁免它＝把一条真实暴露静默掉。已失效且无可行动作的条目可豁免（须写 `evidence`）。
  ⇒ **L16a 现在预期判红**：它对应一个真实的、有行动主（所有者）的未闭环项。
- **自抓缺陷 ①**：变异锚点被变异表**自己抄了一遍**（S11 的坑）⇒ 锚点计数 2 ⇒ 变异施不上力。
  处置：锚点拼接 + 加「锚点在源码里**恰好出现一次**」的断言。
- **自抓缺陷 ②**：同一判据**两处实现**（loader 标空 reason + 消费方 `bool(reason)`）⇒
  **没有任何单点变异能改变行为**，M4 端到端翻不了面。这不只是可测性问题 ——
  两处实现正是 **S12 风险 N2**。改为**只在 `load_whitelist` 判定一次**。
- **自抓缺陷 ③**：判据 C 第一版 O(k³) ⇒ 语料上 **9 s → 162 s**，超掉验收面可接受时间。
  改为**按边界取窗**（值长 n 跨边界 b ⇒ 必落在 `S[b-n+1 : b+n-1]`），与组大小/值长均线性**且不漏** ⇒ 16.6 s。

### #71–#75 的教训（一句话）

> **「交付」不是「接线」，「自述」不是「指纹」，「能扫到」不是「一共有」，
> 「没入库」不是「安全」，「工作区里没有」不是「仓库里没有」，
> 而「测试里放着真值」更不是「测试更可信」。**
>
> 六条都是同一个错：**把自己这一侧的读数，当成了对象那一侧的事实。**
> 而 #71 是其中最刺眼的一条 —— 它说明**上一轮写下的这条教训本身，也需要一个判据来守**。

---

## #76–#78：材料归属改标批（2026-09-13，主控；#67 的 W-67a 收口）

> 这一批的形状：**三条都不是「判据不够严」，而是「判据把一件对的事读成了错的」，
> 以及「一条精确的判据被一条笼统的豁免一起挡住了」。**

### #76 🔴 去归属标记**自己含 `材料` 二字** ⇒ 被 L4e 读成「材料原文」

| 项 | 内容 |
|---|---|
| 位置 | `check_material_citations.py::extract()`（门禁 **L4e**） × 语料里通行的去归属标记 `（**二手来源，不是材料原文**）` |
| 形态 | `extract()` 取「同一分句里**最后一个** `材料`」，再判它与引号之间的限定语。而那句去归属标记**自己就含着 `材料`** ⇒ 取到的正是**标记里的那一个**，`run` 成了 `原文**）` ⇒ **命中 `RUN_ATTRIB`** |
| 错在哪 | 一句**明说「不是材料原文」的话，被仪器读成了「材料原文」**。它不是「判据不严」，是**判据把否定读成了肯定** |
| 实测代价 | **1 条假红**（`CTR-B-020:69` 的 `「缺陷收敛可用可靠性统计量化，但放行判定是治理门」` —— 它**逐字来自我方综述 §F.3 第 694 行**，不是杜撰）+ **3 种限定语形态直接变成 `unclassified` ⇒ exit 3**（`原文**）与跨手册通用的工单规则`、`章节号**）`、`原文**）—— §F.5 把它列进`） |
| 为什么以前没炸 | 旧措辞下这些引号落在 **20 字距离窗之外**（`len(seg) - (pos+2) > 20` ⇒ 静默跳过）⇒ **不是判据对，是判据没看见**。改标把标记挪近了，仪器才开始看见它们 —— 这正是「先问仪器能不能看见」的又一次现场教学 |
| 修法 | **遮掉否定/缺失形态**：`DEATTRIB_NEG` 把 `(不是\|非\|并非\|不算)材料(原文\|章节号\|…)` 与 `材料(未给\|没有\|未命名\|…)` 换成**等长**占位符（等长是为了不破坏引号偏移），随后走常规路径；该行若再无别的 `材料`，这条引号**本就不该算材料引文**（与既有 `non_attrib` 同义） |
| 反向控制 | `--selftest` **11 → 15 用例**，新增三条：⑫ 否定形态不计为引文（也不得 `unclassified`）· ⑬ **`（材料原文）：「X」` 且 X 不在材料里 ⇒ 必须照旧判红**（证明遮罩只对否定形态生效，不是万能豁免）· ⑭ 正常归属路径不得被关掉 · ⑮ 遮罩必须**等长** |
| 读数 | 语料上 **88 条引文 / 39 词 · exit 0** —— 与改标前**逐项相同**（改标前 88/39，中间态 89/40 + 1 红 + exit 3）⇒ **是语料被读对了，不是判据被放宽了** |

### #77 🔴 我自己的改标补丁**把同质化门禁弄红了**（是仪器的假红，不是）

| 项 | 内容 |
|---|---|
| 位置 | `CTR-B-004` 的五维表 ③/⑤ 两格 × `check_contract_dedup.py` 的 **D1** |
| 形态 | 我把来源标记插在「迁移条件**之后**」（`…迁移条件（**二手来源，不是材料原文**）暂按口径…`），这个 `）` 与 `CTR-B-030` 同名格里的 `（账号／店铺／商品集合）暂按口径并逐条标注「口径未签发」，AGT-045 签发后替换；替换前` **拼出一条 34 字公共窗口**（D1 阈值 n=18）⇒ 跨契约同维重复 **1 对** |
| 判断 | **不是假红**：插入的括号**真的**把两处样板句连成了同一段文本，而「跨契约同维重复」要防的正是这个 |
| 修法 | 标记**前移**到 `§E.2／§E.3` 之后（`本项目综述 §E.2／§E.3（**二手来源…**）已定名的状态名与迁移条件…`）⇒ D1 回到 **0 对**；`patch_material_attribution.py` 的锚点表**同步订正为最终文本** |
| 顺带 | 订正锚点这件事本身抓出了补丁脚本的一个缺陷：它只有「锚点对不上 ⇒ 补丁过期」一态，于是**手工挪过标记的两处会被读成「补丁过期」**。已改为**三态**（待应用 / 已应用 / 过期对不上）—— **「该不该再跑一次」与「还有没有效」是两件事** |

### #78 🟡 「交付不是接线」**第四次**，而根因是**豁免的颗粒度太粗**

| 项 | 内容 |
|---|---|
| 位置 | `check_material_residue.py`（#67 交付）从未进 `run_phase6_gates.py` |
| 与 #23/#69/#71 同族 | **两天内第四次**：一个已交付的门禁不在验收面里，而且**接上时就是红的**（家族二 **38 处 / 23 份**） |
| 但这一次的根因**不同** | 它不是「忘了接」。工单**明确写了**「家族一未定标 ⇒ 不得进门禁」，理由是「把一个未定标的仪器接进门禁＝制造噪声」。**这条判断本身是对的** —— 错的是它的**颗粒度**：仪器有两族判据，只有一族未定标，而豁免被加在**整台仪器**上 ⇒ 那一族**精确的**判据跟着一起隐形了 |
| 一句话 | **豁免的颗粒度就是判据的颗粒度。** 本仓库已因颗粒度翻面过三次（列 vs 行 vs 块、`red_findings` vs 判红卡数）—— 这次是**仪器 vs 家族** |
| 修法 | 给残留器加 `--family {all,terms,sections}`：**判定与退出码按族分离**，未判的那一族**在输出里明写「只扫不判」**（未判定 ≠ 干净）。接成 **L4k（`--family sections`）+ L4l（自检）**；L4k 接上时红、改标后归零 |
| 连带 | 新门禁必须自证有劲：`run_phase6_gates.py --selftest` 新增 **5 条端到端用例**（注入 `材料 §F.5` ⇒ 1 · 干净夹具 ⇒ 0 · 材料根不存在 ⇒ 2 · **只有家族一残留时 `--family sections` ⇒ 0**（不越界判）· 同一夹具 `--family all` ⇒ 1），并新增 **R1–R4 四条变异**（恒绿放行 / 材料根放行 / 判据失效 / **越界判**），**7/7 抓住** |

> ### #76–#78 的教训（一句话）
>
> **一条明说「这不是材料说的」的话，被仪器读成了「材料说的」；
> 一条为「别制造噪声」而设的豁免，让 38 处精确残留隐形了两天；
> 而我自己的修复，先把同质化门禁弄红了。**
>
> 三条都指向同一件事：**判据的边界在哪里，必须与它要回答的问题同宽** ——
> 宽一寸则噪声，窄一寸则漏测，**加在整台仪器上则两者同时发生**。

---

## #79 🔴 两个脚本被注释声明为「同尺」，**实际不同尺** —— 家族一 20 个词里 13 个因此是假阳性

| 项 | 内容 |
|---|---|
| 位置 | `check_material_residue.py::normalize()` vs `check_material_citations.py::normalize()`（门禁 L4e） |
| 形态 | 残留器的 docstring 明写「**与 `check_material_citations.py::normalize()` 同尺**（那两个脚本的结论要能对比）」。实测：**引文器还剥 markdown 强调符**（`*_`>#|~`），**残留器不剥** —— 只有 NFKC + 去空白 |
| 后果（实测，逐词核过） | 家族一报出的 20 个词里，**13 个在材料里逐字就有**。例如契约写 `材料「**复用效果和过期知识控制**」`，材料里是 `metrics 复用效果和过期知识控制` —— 去空白后带 `**` 的串当然不在语料里 ⇒ 被判「查无实据」。同类还有 `每项记录**统计窗和新鲜度**` · `**未解释差异和关账时效**` · `**质量由 AGT-018 担任结果负责人**` · `**缺陷复发与处理时效**` · `**不能把少数评论当作整个市场需求**` · `**对外联系、付费和合同仅通过明确范围和动作策略执行**` · `**不能为对齐报表而覆盖原始交易事实**` · `**AGT-048 将通过评估的经验变成能力版本**` … |
| 为什么危险 | 若不核就照单改 60 处契约，**改的是本来正确的东西** —— 这正是 W-67c「先定标再决定接不接门禁」要防的；本条把「未定标」变成了**可复核的定标结论**：**假阳性 13/20** |
| 修法 | **不另抄一份**：`from check_material_citations import normalize`（判据只有一处实现）。selftest **12 → 15**：⑯ 断言两个 `normalize()` **逐字节相等**（「同尺」不能靠注释声明，要有判据）· ⑰ 强调符包裹的**真引文** ⇒ exit 0 · ⑱ **反向控制**：强调符包裹的**假引文**必须照旧 exit 1 |
| 读数 | 家族一 **60 处 / 49 份 / 21 词 → 50 处 / 47 份 / 12 词**（10 处 / 9 词当场消失） |
| 自抓的夹具缺陷 | ⑱ 首版落空 —— 夹具写成「**本契约**的数取自材料…」，`材料` 前 12 字内有去归属语 ⇒ 走 DEATTRIB 分支、**根本到不了判据**。⇒ **夹具自己也要先证明它落在被测判据上**（与「用例是摆设」同族） |

### 家族一剩余 12 个词的粗定标（**登记，不重判**）

| 类 | 词 | 处数 | 判定 |
|---|---|---|---|
| 真阳性 | `季度经营策略` | 37 份 | 材料里连「季度」二字都 **0 次** ⇒ 确属业务侧默认（转 W-67b） |
| 真阳性 | `没有、可以建、但需先建` / 变体 | 3 | **决策 Q10 的语言**被写成了材料的话（`可以建`/`需先建` 材料里都没有） |
| 近似引文 | `新工具失败时恢复已知可用流程并保留影响范围，不重写原始事实凑齐报表`（材料有后半段·命中 53%）· `缺关键输入时进入等待，不把缺失值当作零`（材料有两段·命中 56%/57%） | 4 | 是**改写/拼接**，不是凭空 ⇒ 应按「近似」处置，不按「伪造」 |
| 归属错位 | `Amazon 65% / 独立站 15% 之外的其余 20% 渠道构成未明` | 1 | 65/15 是材料的（F-004），**「构成未明」是我方开放事实 O1** |
| 假阳性（判据范围） | `这份材料能不能作为证据`（`材料` ＝**证据材料**，普通名词）· `OEM 能不能做出来`（契约自己的问句）· `引的那句话在不在底本里`（契约自述）· `这一轮验证过的经验`（对材料原句的转述） | 4 | 机制是**归属跨度**把「材料」后的一段文本全算成了它的声称 |

⇒ **定标结论：家族一 ≈ 45 处真阳性 + 5 处假阳性**（不是 60 处都要改）。家族一**仍未接门禁**，
理由从「未定标」变成「**还差 5 处假阳性的范围收窄 + 45 处本体未清**」。

---

## #80 🔴 去归属语只查 `材料` 附近，**不查词自己附近** —— 4 处假阳性

| 项 | 内容 |
|---|---|
| 位置 | `check_material_residue.py::scan_file()` 的家族一分支 |
| 形态 | 判据先用**括号跨度**给每个 `材料` 划一个「声称范围」，再在该范围里找引号。而 `材料已给出的数（…）` 的那个括号是个**语法容器** —— 它把**整个枚举**都算成了材料的声称。**但枚举里每一项自带归属**，而且归属就写在**词自己旁边**： |
| 实测假阳性（4 处） | ① `独立站 holdback「没有、可以建、但需先建」**＝决策 Q10**；`（`CTR-A-052` / `CTR-B-050` / `CTR-B-057`，**3 处**）· ② `「…其余 20% 渠道构成未明」**＝开放事实 O1**）；`（`CTR-A-069`） |
| 判断 | **契约写得是对的**（它明说了这是决策 Q10 / 开放事实 O1）—— 是判据的**锚点选错了**：它只查 `材料` 周围 ±12 字，不查**词**周围 |
| 修法 | 两处都查：`材料` 邻域 **+ 词自己 ±12 字**。一句话 —— **容器说是材料的 ≠ 容器里每一项都是材料的** |
| 反向风险（本仓库亲手踩过，D27） | 豁免只认词不认话题 ⇒ 假绿。故窗口**只有 12 字**，并配 **selftest ⑲/⑳ 一正一反**：带 `＝决策 Q10` 的不报 · **把这三个字去掉后必须照旧报** |
| 读数 | 家族一 **50 处 / 47 份 / 12 词 → 46 处 / 44 份 / 9 词** |

### 同批：5 处**删节/改字引文**按「恢复逐字」处置（不是改标）

比对材料原句后，这 5 处的正确处置**不是**改归属，而是**把引文补回逐字**：

| 契约原写法 | 材料原句（`docs/06-playbooks/PLAYBOOKS.md`） | 处置 |
|---|---|---|
| `「缺关键输入时进入等待，不把缺失值当作零」`（A-027 / A-043） | `缺关键输入时进入等待**或自治异常**，不把缺失值当作零。`（:7） | 补回被删掉的 `或自治异常` |
| `「新工具失败时恢复已知可用流程**并**保留影响范围，不重写原始事实凑齐报表」`（A-073 ×2 / B-060） | `新工具失败时恢复已知可用流程**，**保留影响范围，不重写原始事实凑齐报表。`（:91） | 连接词改回材料原文的逗号 |
| `「若数据口径未统一保留无法比较的结论」`（B-001） | `若数据口径未统一**，**保留无法比较的结论；`（:119） | 补回逗号 |

⇒ 这三族**一字之差**（一个分句 / 一个连接词 / 一个逗号）就足以让「笔误级差异（更正即可）」与「编造」**分开**。
**家族一读数 46 → 41 处**，且这 5 处**不是被豁免掉的，是被改对的**。

## #81 🟡 变异副本写到别的目录 ⇒ **ImportError ⇒ 变异体根本没跑起来**，读数却像「用例是摆设」

| 项 | 内容 |
|---|---|
| 位置 | `run_phase6_gates.py::mutate()` 的 R 组：把 `check_material_residue.py` 的变异副本写进 `TemporaryDirectory()` |
| 形态 | #79 之后该脚本要 `from check_material_citations import normalize`（同尺要求）。副本落在临时目录 ⇒ **找不到同目录的兄弟模块 ⇒ ImportError ⇒ 退出码 1** ⇒ 用例①（期望 1）**照样通过** ⇒ 读数变成「**没抓住**（该用例是摆设）」 |
| 危险之处 | **它给出的结论与真相相反**：判据和用例都是好的，坏的是**变异体没跑起来**。R1/R3 从 7/7 掉到 5/7 就是这么来的 |
| 与前例同源 | 本仓库记过的「**变异体因硬编码路径从没被跑过**」—— 同一族第二次 |
| 修法 | 变异副本**写在原文件旁边**（`_mutant_<name>.py`）、`try/finally` 用完即删，并**断言不留残件**（`--mutate` 现在会打印「变异副本残留：0 份」，非 0 直接判红 —— 与 S13「scripts/ 下残留 0 份」同一纪律） |
| 读数 | `--mutate` **7/7 抓住**（R1–R4 全部归位） |

---

## #82 🔴 **「不删一字」被写成了「只做插入」—— 代理指标冒充了纪律本身**

| 项 | 内容 |
|---|---|
| 位置 | `patch_material_attribution.py`（家族二补丁）立的纪律：用 `difflib` 断言 opcodes 里**不出现 `delete`**；W-67c 要给 49 处「季度经营策略」改标时**照抄了这条** |
| 形态 | §0 的**（a）业务处境或材料已给出的数**里摆着 `「季度经营策略」＝1 个自然季度`，读法就是「材料给过这一档」。而材料里**没有这一档**（材料定名的经营节奏只有「月度经营复盘」＝1 个自然月）。改标的正确写法是把它**搬到 (b)「业务侧默认」**（先例见 `6a3a215` 的三份）—— **搬家必然产生删除**。把「只有 insert」当纪律 ⇒ 搬不了，只能原地贴标记 |
| 后果（实测，全是**为了绕开代理指标**挤出来的坏结构） | ① (b) 类项**留在 (a) 枚举里** ⇒ 抬头写「材料已给出的数」、项自己说「材料没有」，**类目自相矛盾**；② 嵌套括号 `（「季度经营策略」（**业务侧默认**…））`（`A-021` / `A-031`）；③ 粘字 `改取实测24 个自然月扩窗`；④ 最坏的一类：**标了归属却仍留着错话**——`13 个自然周（**业务侧默认**…）与每月 1 次来自材料已定名的经营节奏与阶段边界`（`A-005`）、`周转窗 = 1 个自然季度（…）（材料已定名的经营节奏）`（`B-029`） |
| 修法 | 纪律改写成可执行的三态：**`move`**（被删的内容归一化后必须逐字重现于插入文本）· **`insert`**（不许有删除）· **`reword`**（改述删字，**机器证不了**⇒ 单列成表 + **处数写死成常数并断言**，原句并排打给人看）。本批 **37 搬家 + 7 插入 + 3 改述** |
| 一句话 | **「不删一字」的判据是「所删必现于所增、改述必须点名」，不是「只有 insert」**。代理指标会在需要改述的地方**逼出坏结构** |

## #83 🟡 同质化闸门把「改动后整行」当判据 ⇒ 把**原本就有的**共享字样算到本次改动头上

| 项 | 内容 |
|---|---|
| 位置 | `patch_material_quarter_tier.py::check_dup()`（本批新增的自查闸门，n=18） |
| 形态（首版） | 拿**改动后的整行**两两比。而 §0 的 (b) 行在 139 份契约里**本来就长得差不多**，正文句架 `每 1 个自然季度（「季度经营策略」）复核一次` 也已经共享 17 字 ⇒ 一改就报 3 对，**全是假红** |
| 修法 | 判据对象改成**本次新增的文本**（`difflib` 的 `insert/replace` 段），并叠一条**更强**的：新增文本与**别的契约原有正文**之间也不许有 ≥18 字窗口（防复制粘贴别人的散文） |
| 遮罩缺陷（同批自抓） | 术语与取值（`「季度经营策略」＝1 个自然季度`）是**公认的共名**，跨契约本来就该逐字一样 ⇒ 按本仓库已有的**专名遮罩**思路遮掉。首版把词形写成 `1 个自然季度`（**带空格**），而遮罩是在 `content_of()` **之后**做的（那里空白已被去掉）⇒ **一次都没遮上**，两条本该无事的尾巴被判成 18 字同质化。⇒ 与 `check_material_citations.py` 记过的 `str.translate({c: None})` **静默不做事**同族：**遮罩自己也要有反向控制**（selftest ⑪/⑪b 一正一反） |
| 同文件对 | 同一份契约里同一档的同一替换条件**本来就该是同一条** ⇒ 跨文件才判同质化；同文件另设 ≥30 字自查（那才说明有一处是复制粘贴） |

## #84 🔴 豁免的**提示**落在绿门禁的 stdout 上 ＝ 没人看得见

| 项 | 内容 |
|---|---|
| 位置 | 家族一接进门禁（新 **L4m**）+ `data/material-residue-baseline.json`（4 处逐处定标的假阳性） |
| 形态 | 本仓库既有纪律是「豁免必须可见、带到期条件，**转绿时提示「请取消豁免」**」（`check_card_identity.py` 就是这么做的）。但**验收面里绿门禁的 stdout 根本不打印**（只打 `→ exit=0`）⇒ 在绿门禁里喊这句话**等于没喊** |
| 修法 | **过期豁免判红**：baseline 里任一条**用不上**（判据收窄到不再报它）⇒ `exit 1` 并点名要删哪一条。豁免的**内容**（`why`）逐条写进 baseline，并在门禁输出里逐条打印 |
| 读数 | 家族一 **41 处 → 4 处**（37 处「季度经营策略」全部改标，另 4 处逐处复核为假阳性：`材料` 作普通名词 / 契约自述）⇒ **L4m 真判**（不再是「只扫不判」）。验收面 **50 → 51 条** |
| 为什么现在才接 | 台账 #67 当时不接**是对的**：20 个词里 13 个是假阳性（#79 同尺、#80 锚点），照单改会把**本来正确的东西改错**。⇒ **先定标再决定接不接**；#82 把「本体未清」这一半也清了 |

---

## #85–#88：W-67d「统一形态」批（2026-09-13，主控；所有者裁定「统一」）

> 这一批的任务是**把两种形态统一**；四条缺陷全是**判据自己的毛病**，
> 且四条**同属一个母题**：**判据只认一种写法**（漏洞 #2 / #11 / #14 / `ORPHAN_DEP` / C2 同源）。
> 完整叙述见 `reports/PHASE6-F8-S1W-材料归属改标与残留门禁.md` §10。

## #85 🔴 用**关键词**去数**形态** ⇒ 25 处被数成 13 处

| 项 | 内容 |
|---|---|
| 位置 | 上一轮把「就地短标」形态登记为 **13 份**（已写进报告 §9.5 与交接） |
| 形态 | 那个数来自 `grep "改记（b）"` —— 而另一半用的是别的动词（`并归` / `归` / `转` / `并记` / `本档转`）。**要判的东西恰恰是「一句话写在哪一栏」，而我是按一句话里的一个关键词去数的** |
| 修法 | 改**结构判据**：§0 的 (a) 跨度里出现**非子句标记**的 `（b）`。重数得 **25 份**，并顺带扫出**第三种形态**（`CTR-A-054`：§0 一个字都不提 —— 它既没搬错位置也没留指针，**两次关键词对差都看不见它**） |
| 读数 | 13 → **25**（+形态三 1 份）；家族三合计 **76 处 → 0** |
| 教训 | **结构判据是唯一能扫出「什么都没写」的仪器。** 关键词判据的盲区不是「漏一些」，而是**漏掉整个形态** |

## #86 🔴 判据拿 `（b）` 当**子串**找 ⇒ 4 份同行写的**子句标记**被判成指针（假红）

| 项 | 内容 |
|---|---|
| 位置 | 家族三 `pointer` 判据首版：`for m in re.finditer("（b）", a_span): 判红` |
| 形态 | 实测 **4 份**（`A-018` / `B-017` / `B-018` / `B-054`）把 `（a）…）；（b）业务侧默认…；（c）…` 全写在**同一行** —— 那个 `（b）` 是**子句标记**，不是指向 (b) 的指针。首版会把这 4 份全打成**假红** |
| 修法 | `（b）` 的**前一非空字符**是 `；`/`。` 才算子句标记。⚠️ 中途插进一个**更险**的版本：改用带 `^` 的多行正则判，在只回看 1 个字符的切片上 **`^` 恒真** ⇒ `改记（b）` 被判成子句标记 ⇒ **指针一处都抓不到**（**假绿**方向，比假红危险）。终版**连正则都不用**：只一条规则，没有第二种解释 |
| 读数 | 指针判据 **25 份**（不是 29）· 4 份反向控制进 selftest |
| 附带 | 同一个「只认一种写法」在**取栏**上又犯一次：**3 份**把「业务侧默认」排在 **(c)**（如 `CTR-A-062`）⇒ 按 `（b）` 硬取会把 A-062 判成「账没收」，而**它的账收得好好的**。改为取「含 `业务侧默认` 的那一子句」，不认字母 |

## #87 🔴 豁免判据拿 `lcp_ge` 的**第一个**窗口去比 ⇒ **恒报过期豁免**（假红）

| 项 | 内容 |
|---|---|
| 位置 | 补丁脚本的 `DUP_ALLOW` 可见豁免（B-056 ↔ B-033 的存量条件句重复） |
| 形态 | `lcp_ge()` 返回的是「**第一个** ≥18 的公共窗口」，实测给的是契约模板的样板句 `templateBtemplatev` —— 与豁免点名的那个窗口毫无关系 ⇒ 豁免**永远匹配不上** ⇒ **每次运行都报「过期豁免」** |
| 修法 | 两处都改成「**查那一个窗口**在不在」：① 抑制重复报告时逐个窗口判、只报**第一个不被任何豁免覆盖**的；② 过期判据直接 `win in content_of(text)` 查两边 |
| 读数 | 豁免生效且**活体探针**（`stale_exemptions({}) == []`）进 selftest ㊲；构造一个不存在的窗口 ⇒ 判过期（㊲b 反向控制） |

## #88 🔴 把「**本次没命中**」当成「**过期**」⇒ 「已应用」与「过期」被混成一件事

| 项 | 内容 |
|---|---|
| 位置 | 同上，过期豁免的判据 |
| 形态 | 首版用「本次 delta 里没命中」当过期。但**补丁落盘之后再跑，那一行根本不在 delta 里**（它已是「已应用」）⇒ 全落盘时豁免**永远**判不出过期（豁免腐烂成永久后门）；而**混合状态**下（本批实测：25 处已落盘、10 处未落盘）又被**误判**过期 |
| 修法 | **豁免的到期条件是对着「语料」判的，不是对着「本次改了什么」判的。** 直接问「这个重复**现在**还在不在两边」 |
| 读数 | 混合状态与全落盘状态**都**判对（这一条是被 `--check` 当场撞出来的，不是想出来的） |
| 教训 | **判据的对象是语料的性质，还是本次运行的性质 —— 这两者会被同一个词（「命中」）掩盖。** 写豁免条款时必须先问清它问的是哪一个 |

---

## #91 🔴 「登记」被写成了**刻石** —— 漂移被消除之后，那句登记自己变成了假话

| 项 | 内容 |
|---|---|
| 位置 | `build_search_queries.py` 的报告正文（S4 段），上游图谱漂移的那条登记 |
| 形态 | 首版是**冻结的一句话**：不论两侧指纹是否相同，都写「上游图谱在本任务执行期间被改动过 ⇒ **工单可能已滞后于图谱**」，末尾还断言「实测确认：`build_gap_ledger.py --check` **现在确实判红**」。<br>**2026-09-14 我消除了那次漂移之后**（图谱新增 `closed_items` ⇒ 重生成缺口账），重生成 S4 报告，于是它写出了：<br>「工单账记的是 `ecda4970…`，而现在是 `ecda4970…` ⇒ **工单可能已滞后**」—— **自相矛盾**；并且继续断言 `--check` 判红，而它**已经绿了** |
| 后果 | 报告里留着一句**假话**，且方向是最坏的那种：**声称还有未清的账**（会让人去查一个不存在的问题），同时**掩盖了漂移已被消除**这个事实 |
| 修法 | **条件化**：`_drift = _graph_sha_now != _ledger_sha`（在 dict 之前算好），漂移时写原登记，无漂移时改写「✅ **无漂移（本条前身是一条真实漂移登记，现已消除）**」并**保留前身的那次漂移记录**（说明「漂移是被消除的，不是被忽略的」） |
| 反向控制 | 往图谱里写一个字节 ⇒ 必须**重新写成漂移登记**（实测 1 处）；还原后再生成 ⇒ 回到「无漂移」（实测 1 处）。**两个方向都验过**，且图谱已还原到与已提交版**逐字节相同**（sha256 `ecda4970bbb7b56b`） |
| 附带缺陷 | 首版把 `_graph_sha_now = …` 三行**写进了列表字面量**里 ⇒ `SyntaxError: invalid syntax. Maybe you meant '==' or ':=' instead of '='?` —— **报告正文是一个列表，里面放不了赋值语句**。修法：提到 dict 之前 |
| 读数 | `build_search_queries.py --check` exit 0；门禁 **L6a** 绿 |
| 教训 | **「登记（不重判）」的前提是「这件事仍然成立」**。一件登记的成立条件变了却不改它，它就从事后记录变成了**假警报**。<br>与 **#23/#24（账落后于实物）**、**#88（把「本次没命中」当「过期」）**同族：**三条都是「判据/账的对象变了，而它的话没跟着变」**。⇒ 凡是写进产物的**状态性叙述**，都必须由**当下的读数**决定，不能是常量字符串 |
| 报告 | `reports/PHASE6-S4-缺口驱动检索式.md` §判据与登记 |

---

## #98 🔴 `--check` 把产物**改对再报绿** —— 一条恒真表达式关掉了整条 check 路径（2026-09-14，主控）

| 项 | 内容 |
|---|---|
| 位置 | `paper2skills-research/scripts/build_search_queries.py`（验收面 **L6a**：`["build_search_queries.py", "--check"]`） |
| 形态 | `main()` 里写着 `as_json=not args.check or True` —— **恒真表达式**，于是 `--check` 与默认动作走**同一条**代码路径；而那条路径的尾部是 `json_out.write_text(...)` / `md_out.write_text(...)` **无条件执行**。⇒ 「比对现状」实际做的是「按现状重写一遍」 |
| 后果 | **它永远不会报出它本该报的那件事**（产物滞后于上游）。上游（缺口账 / 图谱 / 词库 / `candidate_filter.py` / 契约）一变，L6a 先把检索式与报告改对，再宣称「与工单/图谱一致」并 exit 0。产物里的 `_input_fingerprints` 也被一并悄悄更新 —— **恰好把「自述新鲜度」这件事抹掉**。这是**假绿**：红会有人去修，假绿没人知道 |
| 证明（改前） | 把已入库产物 `coverage.generated` 改成 `999` 并加篡改标记 ⇒ 跑 L6a 的原命令 ⇒ **exit=0**，产物被重写回 `28`、标记消失、md5 与备份逐字节相同 |
| 证明（改后） | **同一个实验**：`exit=1`，且盘上 `generated` **仍是 999**、篡改标记**仍在** ⇒ 既报了出来，又没有顺手改写。另：改完脚本而未重生成时，`--check` **自己**报出「人读产物滞后」（这正是一条判据该有的样子） |
| 修法 | `run()` 新增 `check` 形参：`check=True` 时**只比对、绝不写盘**；判据抽成纯函数 `artifact_staleness(pairs)`（逐字节比，只报不改）；不一致 / 产物缺失 ⇒ `judge_errors["ARTIFACT"]` 非空 ⇒ **exit 1**。`main()` 改为 `run(check=args.check, ...)`，恒真表达式删除。新增 `_rel()`（**门禁缺陷 #13 的第二个副本**：比对要在 temp 夹具上跑，`relative_to(REPO)` 对仓外路径抛 `ValueError` 会把门禁崩成 exit 3） |
| 为什么可以逐字节比 | 前提是**本脚本无时间源、无随机**（实测 `grep datetime\|time.\|random\|uuid` 全空，且 J4 已把「两次构建逐字节相同」钉成判据）。姊妹脚本 `build_capability_graph.py` 必须剔 `_meta.generated` 再比，是因为**它有后者、没有前者** —— **同一句「逐字节比」在两个脚本里的合法性不同，不能互相抄** |
| 反向控制 | ① 干净夹具必须 exit 0（否则判据过严 ⇒ 会被当坏工具绕过）；② **⭐ 判红之后盘上必须仍是坏值** —— 少了这条，同一个缺陷可以再长回来而不被任何断言发现；③ 产物不存在 ⇒ exit 1（不是「没东西可查 ⇒ 绿」）；④ **自证**：整个变异节不得改动仓库真产物（前后 `_sha16` 相等） |
| 读数 | `--selftest` **24 用例 / 28 变异样本**（原 20/26）· `--mutate` 值变异 13/13 · 删除型 5/5 · **产物一致性 5/5** · `--check` exit 0（11/11）· 验收面 **L6a/L6b 全绿** |
| 同族普查 | 验收面里用 `--check` 的门禁共 **17 条**。逐条核对：`build_capability_graph` / `build_gap_ledger` / `build_contracts` / `route_backlog` / `harvest_legacy_solutions` 都是「读盘上产物 → 现场重算 → 比对」；`holdback_power` 把重算结果写进 **temp**；`rebase_p2s_cards` 只在显式传 `--json-out` 时才写（**L10a 没传**）；`build_venue_tiers` 文档写明「`--apply` 写、`--check` 逐字比对」。⇒ **只有 L6a 在 check 路径上写盘（1/17）** |
| 教训 | **同族缺陷已经被修过一次，而第三例没有被那次修复覆盖。** `build_card_classification.py:941` 自己写着：「首版 `--check` 只重建、**不与盘上产物比对** ⇒ inbox 改了而忘了重生成时它照样绿（实测撞到）……姊妹脚本 `build_gap_ledger.py --check` 就是比对的 —— 这里补齐，**同尺**」。<br>⇒ ① **「同尺」如果只写在注释里，它就只是一句愿望** —— 第三个脚本不会因为前两个改好了而自动变好。要它变好，只能去**普查**。<br>② 与 **#23/#24（账落后于实物）**、**#88**、**#91** 同族，共同结构是：**判据的动作与它声称的动作不是同一件事**。<br>③ 该纪律的**机械形式**：凡门禁声称「比对现状」，就必须有一条断言证明「它**不写**」—— 本条的 ② 就是这个断言的载体 |
| 报告 | `reports/PHASE6-S4-缺口驱动检索式.md` §7。⚠️ **#92–#97 记在 S13 报告（`reports/PHASE6-F8-S13-*.md`）**，不在本台账内（本台账正文止于 #91）—— 追号时两处都要看 |

---

## #99–#104：域名唯一事实源批（2026-09-14，主控；PHASE6 P1）

> 起因是 S4 报告 §7 里一条**已经登记过、但一直没修**的行：「`arxiv_harvest.py` 的域标签与
> `candidate_filter.py` 有 4 处实测不一致」。当时按纪律「只登记、不动上游」——
> 本批把它在**上游**修掉，并顺手把「为什么会静默」这一层挖出来。

### #99 🔴 域名「一处多名」⇒ 判据**静默整篇放行**（本批最重的一条）

| 项 | 内容 |
|---|---|
| 位置 | `paper2skills-research/scripts/candidate_filter.py` 的 `filter_pool()`；上游是 `arxiv_harvest.py` 的 `QUERY_GROUPS` 键 |
| 形态 | 域名（`09-DataAgent-LLM` 这样的串）在仓内**至少 7 处各写一份**，且互不相同：harvest 用旧名（`07-VOC舆情`/`09-DataAgent`/`15-营销投放`）、filter 用新名、`build_registry`/`rank_candidates`/`make_bundles`/`scoring_config.json` 用旧名、`build_venue_tiers` 用新名、`build_search_queries.harvest_domain_of` 是**第三份手写桥接表**。而 `filter_pool` 里写的是 `domains = [g for g in groups if g in DOMAIN_DIR]; if not domains: kept.append(it)` —— 一行把**两件完全不同的事**合并了：①「这些标签我一个都不认识」（**仪器瞎了**）②「这篇真的没有域标签」（数据如此） |
| 后果 | 1046 篇候选里 **141 篇**（89+37+9+6）的 `query_groups` **全都**认不出来 ⇒ 被**整篇放行**，该域的负向词与约束词**一条都不生效**，**且报告上没有任何读数**。`00-电商Agent` 更彻底：它在 `DOMAIN_DIR` 里**根本没有**。这是**假绿**：红会有人去修，假绿没人知道 |
| 证明（改前） | 直接跑 `candidate_filter.py --report` ⇒ 「保留 776 篇」，各域保留数**逐域列出却从不出现这 4 个域**；`776 − 141 = 635` 才是真正判过的；逐篇对拍确认 141 篇的判定为 `未判定` |
| 证明（改后） | 判定 **905 → 1046**、保留 **776 → 772**、**静默放行 141 → 0**。141 篇里：**135 篇终于被判且保留**、**6 篇被判丢**（逐篇复核：化学催化剂 1、社交媒体机器人检测 1、多智能体辩论 1、临床数据 2、无电商信号 1 —— **全部是真污染，0 误杀**）、另**救回 2 篇**（`01-因果推断` 的论文，它们的**第二个域标签**此前同样隐形，一直被误丢） |
| 修法 | ① 新建 `domains.py` 作为**唯一事实源**（17 规范域，canonical 以 **vault 顶层目录**为准，`--check` **双向**对账：注册了却没目录 ⇒ 红；有目录却没注册 ⇒ 也红）；② 产出端 `arxiv_harvest.py` 改产规范名并加 **fail-loud 守卫**（非规范名 ⇒ `SystemExit`，不许在产出端写一个过滤器认不出的名字）；③ 盘上产物一次性迁移（`migrate_domain_labels.py`）；④ `filter_pool` 改**三态**：`仪器瞎了` 与 `真的没有标签` **分开计数**，前者 ⇒ **exit 2**（没测到 ≠ 通过） |
| 反向控制 | ① `--selftest` 里 **② 退休名 ⇒ 认不出** 就是那 141 篇的形态；② 反向控制「标签正常时『没测到』必须为 0」；③ 认不出的标签必须**逐个点名**且论文 id 可复核；④ 端到端（`run_phase6_gates.e2e_domain_label_gates`）：夹具里塞退休名 ⇒ `candidate_filter --check` **exit 2**、`migrate --check` exit 1、干净夹具 ⇒ 两条都 exit 0 |
| 读数 | `domains --mutate` **8/8 打红 · 8/8 红在正确的判据上 · 8/8 已生效于探针**；`candidate_filter --mutate` **8/8 / 8/8 / 8/8**；`migrate --selftest` 含前缀碰撞 6 例；验收面新增 **L18a–L18f**（55 → **61** 条） |
| 教训 | 与 **#11/#23/#24/#98/#101** 同族，共同结构是：**判据的适用范围被默认成了全体，而它其实只覆盖一个子集**。这条的特殊之处在于，**仪器自己就是那个子集** —— 它看不见的东西，它也报不出来。⇒ 本仓库那条铁律「判『某个东西不存在』之前，先问仪器能不能看见它」在这里第一次指向**仪器自身的输入** |

### #100 🔴 「兼容」与「发现」是同一枚硬币的两面 —— 别名表一度把修复本身变成了新的假绿

| 项 | 内容 |
|---|---|
| 形态 | #99 的第一版修法是加一张**别名表**：旧名经它**静默折到**规范名，还用「别名使用次数归零就提示删除」当到期条件。迁移做完后立刻暴露这条路是错的：**别名表一归零，下一次标签漂移就再也不会被发现**（新写的旧名会被悄悄折掉，而不是让过滤器 `exit 2` 大喊） |
| 修法 | 表改名为 `RETIRED`（**已退休的旧名**）并**从解析路径上摘掉**：`resolve()` 不看它，退休名走 `unknown` ⇒ 判红。它只剩两个用途：给报错一句**可行动**的提示（「这是退休的旧名，规范名是 X」）、给一次性迁移脚本当依据。`--check` 的判据随之从「别名用量归零」改成「**盘上不许出现退休名**」（出现即判红） |
| 反向控制 | `--selftest` 里三条锁：`09-DataAgent`/`07-VOC舆情`/`15-营销投放` **必须判 `unknown`**；M7（把退休名塞回解析路径）与 M8（认不出就猜一个规范名）必须打红 |
| 教训 | 一句可以直接抄进纪律的话：**你替它兼容，你就不会再发现它**。「加一层兼容」看起来是稳妥，实际是把一个会响的判据换成一个不会响的 |

### #101 🔴 变异实验「红得不是地方」—— 8/8 全红，而**一条判据都没验证到**

| 项 | 内容 |
|---|---|
| 形态 | `domains.mutate()` 首版把变异体写到 `/tmp` 跑 ⇒ `REPO = parents[2]` 指到临时目录的上级 ⇒ `check()` 一律报「vault 目录读不到」⇒ **8 个变异体全红**，屏幕上显示 **8/8 ✅**。红的是**同一件与判据无关的事** |
| 第二次 | 改用「写进本目录跑」修第一版时，写成了 `_cf_mutant_domains.py` 却去跑 `candidate_filter.py` 本尊 ⇒ **跑的根本不是变异体** ⇒ 8/8 全绿 |
| 修法 | ① 变异体放临时目录但把仓库根用 **`P2S_REPO`** 钉住；② 每条变异带一个 **`expect`**：**首行 ❌ 里必须出现指定字样**，并把「打红」与「红在正确的判据上」**分开计数**；③ 锚点必须在**变异表之前的正文**里**恰好出现一次**（首版 6/7 条锚点因为被变异表自己抄了一遍而出现 2–3 次 ⇒ 没施上力） |
| 附带发现 | `expect` 一上线就抓出**一条死判据**：`RETIRED` 的「指向自己」规则排在「不是规范名」之后，而恒等映射必然同时满足后者 ⇒ 它**永远轮不到执行**。已把更具体的诊断前置 |
| 教训 | 与 **#81**（变异副本写到别的目录 ⇒ 变异体根本没跑起来，读数却像「用例是摆设」）、**#25/#88** 同族。本批把它推进一步：**「红了」不等于「验到了」**，中间还差一句「红的是不是那条判据」。⇒ 报告里「N/N 打红」这个数**单独出现时不可信**，必须并列「红在正确的判据上」 |

### #102 🔴 同一个环境变量在两个文件里是两个意思

| 项 | 内容 |
|---|---|
| 形态 | 为让变异体能在别处跑，给 `domains.py` 与 `candidate_filter.py` 都加了 `P2S_REPO` 口子。`domains.py` 定义成**仓库根**（`parents[2]`），`candidate_filter.py` 首版写成**本文件往上两级**（= `paper2skills-research/`）—— **同一个名字，两个意思** |
| 发现方式 | **不是**人肉看出来的：验收面新增的端到端用例在夹具上跑 `candidate_filter --check`，报「候选池读不到」⇒ 当场暴露 |
| 修法 | 全仓统一：`P2S_REPO` = **仓库根**；`ROOT = REPO / "paper2skills-research"`。另：基线只属于**本 checkout 的冻结池子**，`P2S_REPO` 指向别处时不比基线 |
| 教训 | 这正是本批在修的那个缺陷**本身**（一处多名 / 一名多义），只不过换到了环境变量上。它由**端到端用例**而不是由阅读发现 —— 「新加的口子必须有一条用例穿过它」 |

### #103 🟡 `rank_candidates` 的产物含**墙钟项** ⇒ 不可复算，「重跑生成器」的 diff 里必然混进与改动无关的漂移

| 项 | 内容 |
|---|---|
| 形态 | `total = tier + code + business + method + fresh + gap`，而 `fresh = max(0, 8 − 距今天数/12)`。于是重跑一次 `rank_candidates.py`，1046 篇的 `score` **全都**会变 0.1–0.4，而 `s_*` 各维**一个都不变** |
| 影响 | 迁移域名时如果顺手「重跑生成器更新产物」，就会把**时间漂移**混进「改域名」这个提交里 —— **用一个真实的改动掩盖另一个真实的改动** |
| 处置 | 派生文本产物（`recommendations.csv`/`.json`、`shortlist.md`、`bundles/*.json`、`bundles/_index.json`）**一律原地只改标签**，不重跑生成器；并用**逐字还原**证明「除标签外一字未动」。已登记的欠账：`rank_candidates.py` 本身仍不可复算，**未修**（改它就得重取全部排序基线，是另一件事） |
| 取证 | 同一时钟下跑「迁移前池子」与「迁移后池子」两遍：**1046/1046 分数完全相同** ⇒ 标签迁移对评分**零影响**，差异 100% 来自墙钟 |

### #104 🟡 前缀当子串找：退休名是规范名的**前缀**

| 项 | 内容 |
|---|---|
| 形态 | `_count_labels` 用 `text.count("09-DataAgent")` 计数。迁移完成后文本里是 `09-DataAgent-LLM` —— **包含** `09-DataAgent` ⇒ 计数**不为 0**，`--check` 会**永远**判「还没迁完」 |
| 修法 | 建负向先行断言：`re.escape(name) + "(?!" + "|".join(各规范名相对它的后缀) + ")"`。**计数与替换共用同一个正则**（两处各写一套必然漂移 —— 本仓库 #79「注释声明同尺、实际不同尺」的教训） |
| 反向控制 | `--selftest` 锁 6 例：`09-DataAgent-LLM` 计 0、`09-DataAgent` 计 1、两者混排计 1、替换后不产生 `-LLM-LLM` |
| 教训 | 「判据只认一种写法」家族的又一例，这次是**前缀 vs 全词**。凡「把 A 换成 B」而 B 以 A 开头，就必须先想清楚匹配边界 |

### 本批仍未做（在册不藏）

- **L2a 判红与本次改动无关**：`build_capability_graph.py --check` 在**本批开工前的 HEAD（`623de0d`）上就已经是红的**（实测：在 HEAD 的独立 worktree 里跑同样报 `顶层差异：['roles']`）。语义是材料侧 `AI组织变革/` 的 `roles` 变了而图谱未重生成。**重生成图谱会级联到缺口账 / 契约 / 检索式**，属另一件事，**本批不动**。
- **`rank_candidates.py` 的时间源**（#103）未修。
- **`filter_pool` 只记「首个命中域」的丢弃原因**（`reasons[verdicts[0][1]] += 1`，代码内已注明）：一篇论文同属 3 个域而都被拒时，另两条原因不计入直方图。**保留/丢弃的结论不受影响**，受影响的只有「为什么」的归属。改动会动到 `关键词库-v2.md` §3 的丢弃原因基线表，故只登记。
- **`domains.ALIASES` 已不存在**：别处若还留着这个名字，说明是旧引用。
