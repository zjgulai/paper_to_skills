{
  "_purpose": "判据 A/B 的可见豁免（`check_history_secrets.py`）。判据 C（已知暴露值还原）**只有登记表 `measured_status=dead` 的条目**才允许在这里豁免 —— 见 `data/exposed-credential-registry.json` 的 `_waiver_rule`。",
  "_rules": [
    "每条必须写 reason + evidence + expires_when；缺任一项 ⇒ 不生效且判红（永久豁免＝台账 #5 那种腐烂）。",
    "豁免的是**具体对象**（blob sha / 路径 / 已知暴露值的 sha256），不是规则。",
    "**仍然有效（live）的凭证不许豁免**：写在这里也不会生效，门禁照红。",
    "门禁每次运行都会把生效中的豁免逐条打出来 —— 豁免是「可见」，不是「忘记」。",
    "本文件用 `!` 规则提升入库：不入库＝新克隆没有它＝等于没交付。",
    "⚠️ **本表里不许复述被豁免的值**（哪怕是为了说明它无害）—— 复述就等于把值又写了一遍，`scan_secrets.py` 会当场把它抓出来。**这是本轮实际踩到的一次**：本表初版把 5 个占位值逐个引了一遍，于是门禁判红。"
  ],
  "entries": [
    {
      "kind": "blob",
      "key": "762c39f750a6db83726d8390d619370b2e521fdc",
      "reason": "`scan_secrets.py` 的**旧版本**（提交 d9186b2 引入，a7b6e47 已修）。命中的 7 条全部是**公开文档里的占位值**：AWS 官方文档的**示例**访问键、GitHub 文档的示例 token、Slack 文档的示例 webhook、连接串的文档示例、一段 stub PEM（正文是 `MIIEow==`）、以及该文件自测用例里的 `sk-test-` 占位串 —— **逐条已回读原文确认，一个真凭证都没有**。**一个真凭证都没有。**",
      "evidence": "同提交的修复提交信息即「scan_secrets 恒红 —— 固件改为 _fx() 拼接 + 新增『扫描器自身必须干净』元级用例」；逐条命中已按规则名回读原文确认均为占位值",
      "expires_when": "若将来对本仓库做 `git filter-repo` 重写历史、该 blob 不再可达时删除本条目"
    },
    {
      "kind": "blob",
      "key": "3689bdde8702de64538e0f127c1b38372c7b8e68",
      "reason": "`check_key_exposure.py` 的旧版本（提交 bd205a5 引入）：三处自测固件写成完整 PEM 头字面量（`xxxx` 占位正文，不是真密钥）。**已在本轮修好源码**（改为 `_pem()` 拼接）。这个 blob 修不掉 —— 它已经推送到远端，只能靠豁免记账。",
      "evidence": "`git log --find-object=3689bdde8702 --oneline` → 仅 bd205a5；修复提交见本轮 `chore(PHASE6/S8)`",
      "expires_when": "若将来重写历史、该 blob 不再可达时删除本条目"
    },
    {
      "kind": "registry",
      "key": "bafdf1f9c0614d2a5d925105bd823bb85576688af74092cbc37236a3a7ebcf32",
      "reason": "DeepSeek API key 值 —— **已实测失效**（GET api.deepseek.com/models → HTTP 401），照原值去用不会有任何效果。且它的可还原形态存在于**已推送的**历史 blob 中，改源码不能让它消失。",
      "evidence": "登记表 `measured_status: dead` + `measured_evidence`：2026-09-13 HTTP 401",
      "expires_when": "若将来重写历史、对象库里再也还原不出该值时删除本条目（届时登记表条目也应一并删除）"
    }
  ],
  "_not_waived_note": "⚠️ **飞书 webhook UUID 刻意不在本表内**：2026-09-13 只读 GET 实测它**仍然有效**（code=19002，对照不存在的 UUID 为 19001）。活凭证有可行动作（飞书后台轮换），豁免它等于把一条真实暴露静默掉 —— 故判据 C 对它**照红不放**，直到轮换完成。这是 S8 的未闭环项。"
}
